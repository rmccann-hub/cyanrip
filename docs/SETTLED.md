# Settled — read this before deriving anything

**Why it exists.** Facts established in this project lived only in the prose of
whichever handshake lap or commit message established them. Fifteen lap files is
not an index, so the cheap move was always to re-derive — and a re-derivation can
come out wrong. That is how a hedge written into round 14 lap 11 §J7 became an
unhedged claim to the operator within the hour, and how the same three facts were
re-checked four times in one session.

**How to use it.** Look here first. If a fact is here, cite it and move on. Only
derive something that is not here, and add a line when it settles.

**Rewritten in place, never appended to.** It is a claim about *now*, the same
rule as `docs/handshake/STATUS.md`. It is **not** a record — the laps and the
commits are the record, and consolidation applies to documentation and never to
evidence.

**Every row carries a `check`**, so this file cannot rot quietly. Run them all:

    python3 tools/check-settled.py

**A row with no command must say WHY it has none**, because three very
different confidences were looking identical:

| tag | means | how much to trust it |
|---|---|---|
| `— past:` | a measurement taken once, on hardware or in a session that is gone | the artifact is cited; it cannot be re-run, only re-read |
| `— theirs:` | a fact about a machine or repository we cannot reach | **only as good as the lap that told us**, and laps get corrected |
| `— peer-source:` | **read from their public repository at a pinned commit**, cited `platterpus@<sha>:<path>:<line>` | re-readable by anyone; stronger than `theirs:` because no lap is in the chain. Earns no command because a check that reaches the network is not evidence about this program |
| `— structural:` | true of our own code but **not observable from any fixture here** | read from source; a reader could check it, no command can |

**This distinction is the same `none` versus `unknown (reason)` rule this project
applies to every log line, turned on its own index.** A bare em dash said "not
checked" and hid whether that was *cannot*, *has not*, or *nobody can*.

**`peer-source:` was added 2026-09-18 because there were three tags and four
confidences.** `CLAUDE.md` used to say we *cannot* read Platterpus's source;
that clause was false and was corrected on 2026-09-13, and the rule since then
is to cite `platterpus@<sha>:<path>:<line>`. A fact obtained that way is not
`theirs:` — nothing was relayed and no lap can have got it wrong — but it is
not runnable here either. It had nowhere to sit, and a row with no home gets a
tag that overstates or understates it.

---

## The pin and the release

| fact | check |
|---|---|
| **The gate cross-checks our transcription against the peer's actual lap.** Declaring `HANDSHAKE-PEER-VERDICT: GO` in our own lap does NOT close a round: it refuses with *"the newest peer lap we hold declares OPEN"* until a filed inbound lap itself declares `GO`. "They did not object" cannot become "they agreed" | — past: dry run on a throwaway clone, 2026-09-04. **Not checkable from this tree**: the message only appears when our lap transcribes `GO` while the newest peer lap does not, and our lap 10 transcribes `OPEN`. Reaching it needs a state the record does not contain, so a check here would test nothing |
| **The release path is dry-run and works end to end.** With a simulated inbound `GO` filed: gate closes round 15, `gen-release-manifest.py` emits seq 22 `0.9.4-rc2+platterpus.12`, both channels resolving, `round_closed=true`. It also refuses a ledger row whose commit is not a sha | — past: dry run on a throwaway clone, 2026-09-04; the real tree was untouched and verified clean afterwards |
| Round 14's pin is `d9c058c` = `0.9.4-rc2+platterpus.10`, seq 20, channel `beta` | `grep -m1 HANDSHAKE-PIN: docs/handshake/round-14-lap-15.md` |
| **`stable` is `978f9b0` = `0.9.4-rc2+platterpus.11`, seq 21**, authorised by round 14 closing `GO`/`GO`. The version string carries upstream's `-rc2` and is stable anyway — order by `release_seq`, read `channel`, never parse the version | `python3 tools/gen-release-manifest.py --check release-manifest.json` |
| Nothing in `src/` changed between `+platterpus.8` and `+platterpus.10` | `git diff --stat 796df32 d9c058c -- src/` |
| Tag pushes and branch **deletes** are `HTTP 403` from this proxy; branch create/update works | — past: probed with a throwaway tag, `docs/handshake/README.md`. **Deliberately no test** — a check that reaches the network is not evidence about this program |

## Our own binary

| fact | check |
|---|---|
| **`-Ddeclare_released=true` MUST NOT BE SET ON A TEST OR RIG BUILD.** `meson_options.txt` calls it *"release path only… recorded as declared, never verified"*, and only `tools/` sets it during a release. A build carrying it writes logs that claim to be a published release; a rig build is in no ledger row, so the claim would be false in every artifact of the session. Nearly shipped in a verbal install instruction for round 16 | ``grep -q 'release path only' meson_options.txt && cat tools/*.py \| grep -qc declare_released`` — the option says so and `tools/` is where it is set. Piped, not `grep -c` over a glob: that prints one count per file |
| **THE FIFO'S THREE `pthread_cond_wait()` SITES ARE `while` LOOPS NOW, AND THE SINGLE `if` WAS A SEGFAULT.** POSIX permits a spurious return, and this program makes it concretely reachable: it installs SIGINT and SIGTERM handlers and its encoder threads sit in these waits. With the `if`, `fifo_pop()` fell through to `out = ctx->queued[0]` with `num_queued` still 0 — a read of a slot nothing had written — then decremented the count to **-1**; `fifo_peek()` cloned the same slot; `fifo_push()` merely broke its own bound. **It cannot deadlock**: the end-of-stream sentinel is a NULL push, which really does increment `num_queued`, and the loop re-reads `block_flags` each iteration. Upstream's — found as audit lead (k) 2026-09-05 and re-derived here | `./build/tests/fifo_test` — signals `cond_in` with the queue empty, which to the waiter is indistinguishable from a spurious return. Restoring the `if` makes it **SIGSEGV, exit 139**, not fail an assertion |
| `-Y` / `--verify-log` is in P1, generated from the binary's `--help`, so it cannot lapse for a new pin | `grep -n 'verify-log' PROVIDER-CONTRACT.md` |
| `signal()` appears at exactly **one** site in all of `src/`, installing `on_quit_signal` for `SIGINT` and `SIGTERM`; nothing restores either disposition | `grep -c '[^_a-z]signal(' src/cyanrip_main.c` |
| **A single SIGTERM cannot terminate cyanrip** since `+platterpus.7`: the handler sets a flag and returns, and nothing reads it once the rip loop is past. **THE OPERATIONAL CONSEQUENCE, which is what actually bites: bare `timeout(1)` cannot bound this program.** GNU `timeout` without `-k` sends exactly one SIGTERM and then waits, so a bound written to contain a hang hangs with it — `tools/rig-round16.sh` shipped that way for an hour. Use `timeout -k`. A second SIGTERM would also do it (the handler `_exit`s when `quit_now` is already set), but SIGKILL does not depend on the program cooperating | — structural: `src/cyanrip_main.c:1155`. The mid-rip half IS covered by `sc_interrupt`, which signals a live rip and asserts a graceful exit; **the half after the rip loop is a millisecond-wide window on an image**, so a timing test would be flaky and a flaky test is worse than none |
| `crip_diag_record()` has **one** call site, at the top of `cyanrip_vlog()`, which ends `vprintf` + `fflush(stdout)` unconditionally — so a message in the `-j` array **is** proof it reached fd 1 and was flushed | `grep -rc 'crip_diag_record(' src/cyanrip_log.c` |
| The `-j` record is written from `atexit`, so a run that produced one reached `exit()` | `grep -n 'atexit(crip_diag_write)' src/diagnostics.c` |
| `PROBE_MAX_SECTORS` is **2048** — a ceiling of ours, not a drive limit | `grep -n 'PROBE_MAX_SECTORS' src/cache_probe.c` |
| `print_cache_model()`'s drive branch is unreachable from every fixture: all three image drivers take the image arm | `grep -n 'DRIVER_BINCUE' src/cyanrip_log.c` |

## Hardware — what has and has not run

| fact | check |
|---|---|
| **RESOURCE USE IS STILL UNMEASURED AT DISC SCALE, and the small-disc figure does not settle it.** The 2026-09-05 audit's lead (l) reports 672 MiB at defaults against 160.7 MiB with `-K` (`--no-replaygain`) on a full disc — a plausible mechanism, since ReplayGain needs the album's samples to compute album gain. Measured here on `pregap.cue` (3 tracks, 8 seconds): **5180 KiB with ReplayGain on, 4988 KiB with `-K`** — a 192 KiB difference, essentially nothing. **That neither confirms nor refutes the lead**: an 8-second fixture cannot exercise a mechanism whose cost scales with disc length, and this environment has no disc. **Needs the rig.** A first attempt reported 77 MiB and was wrong — the sampling loop read a subshell's `VmHWM` rather than cyanrip's, the same sample-the-right-PID error as `pgrep -f` matching your own tools | — past: measured 2026-09-07 by polling `/proc/<pid>/status` `VmHWM` for the rip process itself. Re-runnable, but only ever answers the small-disc question |
| **`-x -I` completed on a drive**, 2026-08-25, PIONEER BD-RW BDR-209D: exit 0, 15.9 s, drive returned, `at least 2048 sectors … search ceiling reached` | — past: `docs/handshake/inbound/artifacts/round-14-acceptance-20260825/transcript.txt` |
| **`-x` alone has never been shown to return a drive.** Different claim, same flag | — structural: an absence, and a different claim from the row above |
| **C1 is `-j`-associated, cause NOT determined.** The controlled pair, same drive/disc/day: `-N -l 1` = 4.9 s exit 1; `-j -D -o -u …` = 1800 s and SIGKILL. Narrowed to which flag, not to where | — theirs: their lap 16 §D, with our transcript §P2 as the other half of the pair |
| `Pregap source: sub-channel (not signalled by TOC)` on 13 of 14 tracks, track 1 `lead-in`, LSN arithmetic consistent, on `d9c058c` | — past: same transcript, §P |
| **T1 IS DONE.** Secure re-read on hardware, 2026-08-26, build `d9c058c`, 14-track disc: `-Z 2 -r 3` gave `Secure re-read:  converged after 3 reads` on all 14 tracks | `grep -c "converged after 3 reads" docs/rig-2026-08-26-d9c058c/rips/secure-reread.log` (14) |
| **A non-zero `Read stalls:` is no longer hypothetical**, and its populated singular rendering matches the format string and `tests/stall.c:370` | `grep -c "1 read exceeded 10s; longest 11s (track 3, LSN 37086)" docs/rig-2026-08-26-d9c058c/rips/cancel-me.log` |
| The `Cache model:` **sector** arm has now been seen on a real drive: `1200 sectors (drive cache size not probed)`. Every image fixture takes the image arm and the golden reference runs `-P 0`, so no committed artifact had shown it | `grep -c "Cache model:    1200 sectors (drive cache size not probed)" docs/rig-2026-08-26-d9c058c/rips/secure-reread.log` |
| **`-Y` verifies a log written by a DIFFERENT build.** All six 2026-08-26 logs, written by `d9c058c`, verify exit 0 against a later binary | `for f in docs/rig-2026-08-26-d9c058c/rips/*.log; do ./build/src/cyanrip -Y "$f" >/dev/null 2>&1 \|\| exit 1; done` |
| The corrected paranoia claim, re-checked on that disc: three reads a track put `READ` at ratio **3.02**, and **`FIXUP_EDGE` summed to 0 per-track against a disc total of 2** — the sharpest case there has been of why the per-track blocks must not be summed | — past: the table in `docs/rig-2026-08-26-d9c058c/README.md`, recomputable from `rips/secure-reread.log` |
| **The 2026-08-26 `cancel me` / `after cancel` rips do NOT show a cancel.** Both were invoked with a narrowed `Tracks to rip:` and both footers read `Rip completed:  yes`. A folder name is not evidence | `grep -h "^Rip completed:" docs/rig-2026-08-26-d9c058c/rips/cancel-me.log docs/rig-2026-08-26-d9c058c/rips/after-cancel.log` |
| **Never run anywhere:** C2 reporting (drive says unsupported), `-f`, damaged media, CD-TEXT from a physical disc, `-x` alone on a drive that goes on to rip | — structural: an absence. No fixture can produce any of these, which is the claim. **Four came off on 2026-09-03**; see the rows below |
| **CC-1 IS NOT MET, and we said it was.** What is measured: **two** whole-disc rips on the released pin completed cleanly — `Tracks to rip: all`, `Ripping errors: 0`, `Rip completed:  yes (14 of 14 tracks)`, `Log FUN512` intact, `-Y` exit 0. What is NOT established is the thing CC-1 asks for: their acceptance **run** timed out in section F at `10800.1`s and its ARCHIVAL section H produced no evidence at all. **We concluded the pass from the rips inside it** — "I verified the list you sent" is not "I verified your inventory" | `grep -c "^Rip completed:  yes (14 of 14 tracks)" docs/rig-2026-09-03-978f9b0/rips/full-acceptance-angle-bracket.log` — this checks the RIP, which is all it ever checked. — theirs: the run's failure is their lap 6 §C1, which we cannot re-run |
| **`0.6.34` is `dba2ab2`**, and we declared it `unknown` in a lap while holding it: the bundle's own per-rip JSON carries `generator.build_fingerprint: dba2ab2`. Confirmed independently by their lap 5's `HANDSHAKE-OUR-PIN` | — theirs: their lap 5 wire header. The bundle JSON that also carried it is not filed (19 MB); its hash is in `docs/rig-2026-09-03-978f9b0/SHA256SUMS` |
| **Six consecutive digest values agree across two independent implementations.** Their laps 4–9 declare `1ad28e7744de3d6b/3`, `ddc0d8a741f76b60/4`, `09268d7203773872/5`, `60a7c64dc252b1fa/6`, `44e14b452950ebb0/7` (ours, confirmed by their lap 9 §B2) and `35b861f25abfa69c/8`. They built `scripts/round_digest.py` from our written spec having never read our code | `python3 tools/round-digest.py 15 --exclude round-15-lap-09.md` (= `35b861f25abfa69c over 8`) |
| **`0.6.37` is `f3b60a0`** and is the agreed app half of round 15, accepted in our lap 8 §1 after their half moved four times, each move declared | `grep -m1 'HANDSHAKE-OUR-PIN' docs/handshake/inbound/round-15-lap-09.md` |
| **The abort footer and a diagnosed non-zero exit, together and on hardware.** `cyanrip -N -l 1` exited **1** having printed `Offset is unset!…` at column 0, then `Rip completed:  no (aborted, 0 of 14 tracks)`. It also settles one P5a `goto end` row by running it, which is what the legend said those rows needed | `grep -c "Rip completed:  no (aborted, 0 of 14 tracks)" docs/rig-2026-09-03-978f9b0/session/DIAGNOSTICS.txt` |
| **`Secure re-read:  did NOT converge after 3 reads (repeat limit hit)`** — the non-converged arm, three tracks on each whole-disc rip. Only the converged arm had ever been produced | `test $(grep -c "did NOT converge after 3 reads" docs/rig-2026-09-03-978f9b0/rips/secure-reread.log) -eq 3` |
| **THE ACCURATERIP RESPONSE PARSER RUNS IN THIS SANDBOX, WITH NO DRIVE.** `CLAUDE.md` and round-16 lap 1 §0 both said clause 1 *"is the one we cannot reach at all here … there is no network in this environment"*. **Both halves are false now**: `accuraterip.com` answers, and the query is driven by the TOC alone — so an image with the reference disc's offsets gets a real 200 and the rewritten parser reads it. 2026-09-10: `AccurateRip: found`, `max confidence: 200`, and 200 is the maximum confidence in that disc's own rig log. **NOT the per-track MATCH path** — the audio is silence, every checksum is `00000000`, so clause 1 is de-risked and not closed. **The network result is a PAST measurement, filed verbatim at `docs/accurip-probe.log`** (2026-09-15, `found`, confidence 200) — it is no longer re-run by this file, because an HTTP conversation inside a gate took 80.2 s on 2026-09-13 and 38.1 s on 2026-09-15 and has timed `Settled facts` out. **The check below asserts the CLAIM against the ARTIFACT**, which is the direction that catches something: edit this row's numbers without re-running the probe and it fails. A `--toc-only` check was tried first and revert-proved as NEAR-VACUOUS — every session's log is the same disc, so it passed against a different session's log too | ``grep -q 'AccurateRip: found' docs/accurip-probe.log && grep -q 'max confidence parsed: 200' docs/accurip-probe.log`` |
| **THE INTERRUPT FOOTER, ON HARDWARE, AND THE LOG STAYS ATTESTED.** 2026-09-10 on test pin `ddc1e8c`: a SIGTERM arriving **mid-read** produced `Ripping errors: 1`, `Rip completed:  no (interrupted by SIGTERM, 0 of 14 tracks)` and `Interrupted at: track 1, mid-read`, with `Log FUN512:` present and `-Y` exit **0** — a complete, attested record of an incomplete rip. **The ABORT arm was already settled (2026-09-03); the INTERRUPT arm is a different path and had never run.** `Interrupted at:` had never appeared in any rig log | `grep -c 'interrupted by SIGTERM' docs/rig-2026-09-10-ddc1e8c/rips/cancel-me.log` and `./build/src/cyanrip -Y docs/rig-2026-09-10-ddc1e8c/rips/cancel-me.log` (exit 0) |
| **A CANCEL REACHED cyanrip FOR THE FIRST TIME IN FOUR SESSIONS**, and this does NOT contradict the row above about a single SIGTERM: that one is precise about *"once the rip loop is past"*, and this signal arrived **mid-read**, where `quit_now` is read. 2026-08-26 and 2026-09-03 `cancel me` rips finished normally; 2026-09-07's ran 15m33s past the cancel because the signal went to a distrobox wrapper. Opposite sides of one window | `grep -h '^Rip completed:' docs/rig-2026-0*/rips/cancel-me.log` — three `yes`, then one `no (interrupted by SIGTERM…)` |
| **`-x` ALONE RETURNS A DRIVE — the other half, after twelve rig sessions.** 2026-09-11 on test pin `ddc1e8c`: `-x -l 1 -o pcm` direct to the container binary, **exit 0 in 3m51s**, `Rip completed:  yes (1 of 14 tracks)`, and the `Cache model:` line took its `-x` arm — *"(drive cache probed separately, see \"Cache probe:\")"* — which is in the contract at P2 and had never been printed by hardware. `-x -I` was settled 2026-08-25; **this is the modifier that goes on to rip**, and they are different claims | `grep -c 'drive cache probed separately' PROVIDER-CONTRACT.md` — expects the P2 row to exist; the hardware half is the 2026-09-11 x-probe log |
| **THE CACHE NUMBER IS STILL A FLOOR WE SET, NOT A MEASUREMENT OF THE DRIVE.** `at least 2048 sectors, upper bound unknown (… search ceiling reached)` — `search ceiling reached` is our own `PROBE_MAX_SECTORS`, exactly as on 2026-08-25. Two successful probes, neither bounding the drive. **A retired risk is not a measured quantity.** What IS newly measured is the timing pair: `uncached read 363.0 ms, cached read 81.8 ms`, a 4.4x ratio | `grep -n PROBE_MAX_SECTORS src/cache_probe.c` — the ceiling is ours, so the figure is ours |
| **RUN A MET ROUND 16'S CLOSE CONDITION.** 2026-09-11, `tools/round16-accept.py` at `9ec722e` over the run's `$OUT`: **0 FAIL, 0 UNPROBED, exit 0**, "All three clauses settled by this run." Clause 2 — `-H` with de-emphasis on hardware — had never run before | `python3 tools/round16-accept.py --out docs/rig-2026-09-11-runA-ddc1e8c 2>&1 \| grep -c 'FAIL'` — expects `0`. **Exit here is 2, not 0**, and that is correct: clause 2 is UNPROBED in the filed copy because the `.pcm` never travelled. The exit-0 verdict was taken on the rig, against the audio |
| **EVERY RIP IN RUN A COMPLETED; THE SCRIPT WAS WRONG ABOUT ALL FIVE.** `timeout -k` killed the **distrobox wrapper** and the containerized cyanrip ran on. Measured from mtimes in the run's own output: `acsum-01.txt` written 21:12:48, `plain/PCM/plain.log` written **21:36:08** — 23m20s later — and `plain.json` records `exit_code: 0`. Five `exit 137 in 630s` lines describe wrappers, not rips. Same mechanism as the 2026-09-07 cancel that ran 15m33s past its signal | `python3 -c "import json;print(json.load(open('docs/rig-2026-09-11-runA-ddc1e8c/plain.json'))['exit_code'])"` — expects `0` |
| **ALL FIVE `-Y` EXIT CODES PRODUCED BY THE INSTALLED BINARY.** 2026-09-11, `ddc1e8c`, direct to the container binary, no drive: `valid`→**0**, `modified`→**2**, `no footer`→**3**, `trailing data`→**4**, `directory`→**5**, matching `src/fun512.h:70-74` and P4 exactly. This was Platterpus's ask 2 — one code per verdict, never a shared `1` — and **only 0 and 3 had ever been seen from a real build**. A consumer telling "incomplete rip" from "someone edited this log" keys on these numbers | `grep -c 'CRIP_LOG_EXIT_' src/fun512.h` — expects 5 enumerators; the hardware half is the 2026-09-11 ytest run |
| **P1 ARGV COVERAGE, the three S-11 numbers, derived rather than assumed.** 41 documented option pairs: **21 boolean** (no value to malform), **20 take a value**, of which **13 are argv-probed** with valid and invalid input and **7 are not** (`-j -D -F -L -M -R -Y`) — four of those seven are covered elsewhere (`-D/-F/-L/-M` by the naming tests, `-j` by `diag_repeated_flag`), which is *documented-untested*, not a hole. `--gate`: **111 probes, 0 crashed, 0 refused-without-a-message, 0 silently ignored, 47 refused** | `python3 tools/probe-argv-surface.py --gate` |
| **A ROUND CLOSES ON A SIDE'S GATE ONLY WHEN THAT SIDE HOLDS A LAP NAMING THE OTHER'S VERDICT — AND THE TWO GATES REACH THAT DIFFERENTLY.** This row read *"whichever side sent the last lap, and both implementations have that property"* and **the second half was false about theirs**, corrected 2026-09-21 on Platterpus's report in `docs/handshake/inbound/status-2026-09-21-v0.6.52.md` and **verified from the record we hold before accepting it**: rounds **9, 10, 13, 14 and 16** all had us sending the last lap (our max lap 11/5/8/19/17 against their 10/4/7/18/16) and their laps in all five declare `GO`, so "only the last speaker's gate can close" cannot be true of theirs. **Their stated property fits all 15 rounds and ours does not:** their gate closes only when they hold an own-side lap numbered **after our first `GO`** — true in every round 8–16 and in 22, false in 17–21, which is exactly where the divergence showed. **Ours is the last-lap property and that half stands**, which is why round 22 needed our lap 5: their lap 4 closed it on their gate and could not on ours. **The defect is ours as much as their omission.** They derived their §7.5b on 2026-09-12 and never sent it — but this row asserted a mechanism in *their* code while its own re-check command greps only *our* tree, so it could never have verified the half it got wrong. That is the rule about never stating a mechanism in the other side's code without citing where it was read, broken inside the index built to stop re-derivation | ``python3 - <<'EOF'`` over `docs/handshake/` and `docs/handshake/inbound/`, comparing each round's max declared `HANDSHAKE-LAP` per side against our first lap declaring `HANDSHAKE-VERDICT: GO`; ours: `grep -n 'def stale_peer_verdict' tools/release-gate.py`. **Their half is not checkable from this tree** and must be cited to their status or their `docs/cyanrip-handshake.md` §7.5b, never asserted here |
| **THE PUBLISHED PAIR WORKS END TO END.** 2026-09-12, `platterpus 0.6.47` driving `platterpus-fork-gfe4d2c4`: eight rips, `AccurateRip: found` on all eight, `Read stalls: none` on all eight, seven `Ripping errors: 0`, and **all eight logs verify against our own `-Y`, exit 0**. Filed at `docs/rig-2026-09-12-fe4d2c4/`. **It is evidence about `fe4d2c4` under `0.6.47` and NOT about the approved pair `(fe4d2c4, 0.6.46)`**, which no run has exercised | `` for f in docs/rig-2026-09-12-fe4d2c4/rips/*.log; do case "$f" in *eac*) continue;; esac; ./build/src/cyanrip -Y "$f" >/dev/null \|\| exit 1; done `` |
| **THEIR LOG-VERIFY RACE IS FIXED, AND THE COUNT IS THE PROOF.** `ripper.log_verify_failed` appeared **2, 2, 0** across the 2026-09-10, 09-11 and 09-12 sessions. Their round-16 §C1 fix sat unmerged through two rounds; `0.6.47` carries it. The defect read a complete signed log 6.1 s and 7 s before cyanrip finished writing it and reported it as carrying no checksum | `! grep -q log_verify_failed docs/rig-2026-09-12-fe4d2c4/session/DIAGNOSTICS.txt` — **negated deliberately**: `grep -c` exits 1 when the count is 0, which is the very result this row asserts, so counting would report the fix as a stale fact |
| **CLAUSE 2 CARRIES FROM `ddc1e8c` TO `fe4d2c4` BY DERIVATION, NOT BY A SECOND RUN.** The 2026-09-12 session invoked `-H -E` and its `-H -W` control on `fe4d2c4` but wrote `.flac` rather than `-o pcm` and **none of that output travelled** — so it shows the invocation runs and prints its banner, which is the SETUP and not the clause. What makes Run A's decoded-sample result stand for this build is that `12f2081` is the only `src/` commit between them, a single hunk in `main()`, touching nothing in the encode path, filter graph, checksums, naming or cue writer | `test "$(git show 12f2081 --name-only --format='' -- src/)" = src/cyanrip_main.c` — the ONE file under `src/` that commit touches |
| **THEIR LOG CHECK REPORTS NO FOOTER AND NO SIGNATURE ON LOGS THAT HAVE BOTH, AND THE MECHANISM IS A RACE — SETTLED, TWICE.** The conclusion is false and the cause is that they read the file **before cyanrip finished writing it**. 2026-09-10: verified 02:02:08.902, `Ripping finished at` 02:02:15 — **6.1 s early**. 2026-09-11: verified 15:17:36, finished 15:17:43 — **7 s early**. Both `cancel-me.log`s verify. **This row previously said the mechanism was NOT established, and reasoned from an app log "ending 01:48:46 UTC" against a rip "finishing 02:02:15 UTC"** — both stamps are local `-04:00`, so the difference never existed, and the cancel was in the `zz-applog-rotations` file all along. Their round-16 lap 10 §C1 supplied the mechanism from the timestamps we misread; our lap 11 §2b accepted it. Their fix is at `81ca989`/`c394229`, unmerged and **not in `0.6.45`**, which is why 09-11 reproduced it | `./build/src/cyanrip -Y docs/rig-2026-09-10-ddc1e8c/rips/cancel-me.log && ./build/src/cyanrip -Y docs/rig-2026-09-11-ddc1e8c/rips/cancel-me.log` — both exit 0, against their `signature ABSENT` |
| **`-Z` NON-CONVERGENCE IS NOT DETERMINISTIC — every filed session, not the four this row first named.** Same disc, same `-Z 2 -r 3`, **ten** sessions across five builds: 08-26 **none**; 09-03 tracks **3, 4, 5**; 09-07 **3, 5**; 09-10 **3**; 09-11 **3, 5**; 09-12 **3, 5**; 09-15a **5**; 09-15b **3**; 09-17 **3, 5**; 09-22 **3, 5**. Per-session failure counts run 0, 1, 2 and 3, and one whole session converged on all fourteen. A track's convergence is a property of the READ, not of the disc. **The check no longer pins the list.** It was ``= 0,3,2,1,2,2,1,1,2`` — a literal that every new rig session falsifies, so the row's maintenance was a number incremented rather than derived, the defect this file records three times elsewhere. It now asserts the CLAIM: the counts are not all equal | ``test "$(grep -c 'did NOT converge' docs/rig-*/rips/secure-reread.log \| cut -d: -f2 \| sort -u \| wc -l)" -gt 1`` |
| **A cancel can be SENT and never ARRIVE, and the log is right either way.** 2026-09-07: Platterpus SIGTERMed `/home/rmccann/.local/bin/cyanrip` while the real process ran as `/usr/local/bin/cyanrip` inside a distrobox; `quit_now` was never set, the rip ran 15m33s past the cancel and signed off `Rip completed:  yes`, and a second cyanrip read the same drive for 10m of it. **Not our signal handling** — compare the two paths before blaming the handler | `grep -m1 '^Invoked as:' docs/rig-2026-09-07-978f9b0/rips/cancel-me.log` vs `grep -m1 'rip starting' docs/rig-2026-09-07-978f9b0/session/platterpus-app-log.txt` |
| **THE LOG AND `-j` NOW AGREE ABOUT `ripping_errors`, AND `Frame retries:` IS `Retry limit:`.** Was: under a 32 KiB write cap on `mixed.cue`, `-j` said **2** and the process exited **1** while the log said `Ripping errors: 0` and `Rip completed:  yes` over files truncated to 32768 bytes from 253742 — `cyanrip_log_finish_report()` ran before the encoder-status collection loop. **Both shipped in round 21**, each agreed in round 20 and neither shipped inside it: the footer moved below the loop (still inside `end:`, so round 14's twenty-four-`goto` property holds), and the label is now `Retry limit:    N (per frame, and per whole-track re-read)` with `-j`'s key `retry_limit` at schema **`cyanrip-diagnostics/6`** — the rename took it to `/5` and §4b's `cache_probe` block took it to `/6` inside the same round, and this row carried the first number after the second had shipped. `git show 3952c03:src/diagnostics.c │ grep cyanrip-diagnostics/` is the artifact. **What the fix made visible and did NOT fix:** `Rip completed:  yes` now sits beside a non-zero count, and `Track N ripped and encoded successfully!` still prints over a `File(s):` list built from the request — both in `docs/KNOWN-ISSUES.md`, the first put to Platterpus in round 21 lap 1 | `python3 tests/rip_images.py ./build/src/cyanrip tests/fixtures encode_failure_reaches_the_log` — asserts the two records are **equal** and, separately, **non-zero**, so the property cannot be satisfied by both being 0 |
| **THE PER-TRACK BLOCK REPORTS THE READ AND THE FOOTER REPORTS THE ENCODE — round 22, `89a57d6`.** Was: `Track N ripped and encoded successfully!` printed immediately after the flush signal went to the encoders and long before any was joined, so under a 32 KiB write cap it sat above a 253742-byte file truncated to 32768. **The fact it asserted did not exist when it printed**, which is a different defect from an unchecked one and admits no reword: no line printed at time T can report a fact that comes into being at T+1. Now `Track N read successfully!` / `Track N read with errors.`, with a new `Encoder errors:` line directly below `Ripping errors:` carrying the encode outcome in three arms — `none; N tracks encoded`, `M tracks failed (…); N tracks encoded`, `not applicable; no track was encoded` — because `none` over an unstated population is absence-of-evidence read as evidence-of-absence. **`File(s):` is NOT fixed**: it prints from the same pre-join moment, so it is the identical fixpoint one level down and is open in `KNOWN-ISSUES.md`. `-j` unchanged, schema still `cyanrip-diagnostics/6`. Revert-proved four times, one fix at a time, each with a green build | `python3 tests/rip_images.py ./build/src/cyanrip tests/fixtures encode_failure_reaches_the_log` — asserts the old string is **absent** rather than merely joined by a new one, and compares the tracks `Encoder errors:` names against the truncated files on disk rather than against the log's other half |
| **AND THE 2026-09-15 ACCEPTANCE RUN WAS NOT A PASS, THOUGH IT REPORTED 241/0.** Platterpus wrote no MP3 and no WavPack at all; their `gates.derived` read `"ran"` beside a null `verification.derived` block on both. **The absence was readable in the bundle we already held** — `MANIFEST.txt` lists `.wav` files refused for the `derived wav` album and lists NO `.mp3` or `.wv` for the other two. We quoted that file and did not ask it what was missing | ``test $(grep -c 'derived wav .*\.wav  *excluded' docs/rig-2026-09-15-fe4d2c4/session/MANIFEST.txt) -eq 2 && test $(grep -c 'derived \(mp3\|wavpack\).*\.\(mp3\|wv\)  *excluded' docs/rig-2026-09-15-fe4d2c4/session/MANIFEST.txt) -eq 0`` |
| **THE SAME PIN RIPS THE SAME DISC THE SAME WAY — THREE SESSIONS IN FOUR DAYS.** `fe4d2c4` ripped the reference disc under `0.6.47` (09-12), `0.6.48` (09-15a) and `0.6.49` (09-15b), same drive, same `-s 667 -o flac -r 3 -Z 2`. **13 of the 14 tracks carry the identical `EAC CRC32`, `Accurip v1` and `Accurip v2` in all three.** Scope: this compares what the ripper's LOG recorded; 09-15b's track 3 is superseded on disk by a Platterpus re-read (`59D352DD`), recorded in an addendum beside the log. Say it at the scope the evidence covers: every checksum cyanrip computed over the audio agrees. The FILES are not identical — `creation_time` differs between any two rips by the same binary — and no audio travelled in either bundle | ``test "$(cat docs/rig-2026-09-12-fe4d2c4/rips/secure-reread.log docs/rig-2026-09-15-fe4d2c4/rips/secure-reread.log \| grep -o 'EAC CRC32: *[0-9A-F]*' \| sort \| uniq -d \| wc -l)" -eq 13`` |
| **A SUPERSEDING ADDENDUM STILL CARRIES NO TIMESTAMP — round 8 `J14`, re-confirmed against the newest artifact rather than remembered.** Platterpus `0.6.49` introduced `.platterpus-addendum.txt`, which supersedes a re-read track's values and deliberately leaves our log byte-exact so `-Y` still verifies. Measured 2026-09-15: **zero** date or clock patterns in either addendum. Our `creation_time` for a superseded track describes the read that was thrown away | ``test $(grep -chE '[0-9]{4}-[0-9]{2}-[0-9]{2}\|[0-9]{2}:[0-9]{2}:[0-9]{2}' docs/rig-2026-09-15b-fe4d2c4/rips/*.addendum.txt \| sort -u) = 0`` |
| **A RE-READ TURNED AN OFFSET-VARIANT PARTIAL MATCH INTO A GENUINE ACCURATERIP VERIFICATION, so tracks 3 and 5 are NOT one phenomenon.** 09-15b's log has track 3 `did NOT converge`, `v1 not found`, `450` matching at confidence 200. Platterpus re-read it: `59D352DD`, converged after 3 reads, `Accurip v1 3C8BDDD2 — accurately ripped, confidence 128`. Track 5 keeps the `450`-only shape across all three sessions, and only its `Accurip 450` checksum is stable, `4CCBCF89`. **Its whole-track `EAC CRC32` is not**: `E0036697` in four of those six rips and `6902BCF0` in two. So a stable one-frame checksum does not show the track is a pressing variant rather than a misread (Platterpus's round 27 lap 2, Correction 2). Track 3's shape vanishes once the track is read again | ``grep -c 'accurately ripped, confidence 128' docs/rig-2026-09-15b-fe4d2c4/rips/secure-reread.addendum.txt`` |
| **CONVERGENCE STATUS AND THE REPORTED CHECKSUM ARE INDEPENDENT — settled over EIGHT sessions, not the three this row first cited.** Track 3 has produced **five** distinct values across the ten filed `secure-reread.log`s; track 5 **three**. The commonest track-3 value `3D8FCF0C` is reported **converged** in two sessions and **NOT converged** in four. **A track can converge on a value it also produces without converging, and can fail to converge twice on two different values.** `src/checksums.h` is byte-identical across all four builds involved and no commit between them touches `last_checksums`, `total_repeats` or `max_retries`, so a value difference is a difference in the READ. This row first read *"the one track that differs is the one whose convergence differed"* — a correlation that held on the two samples anyone had looked at | ``test -n "$(for f in docs/rig-*/rips/secure-reread.log; do awk '/^Track [0-9]+ (ripped and encoded\|read) successfully!/{i=($2==3)} i&&/^  EAC CRC32:/{c=$3} i&&/Secure re-read:/{print c, ($0~/did NOT/)?"NOT":"conv"; i=0}' "$f"; done \| sort -u \| awk '{print $1}' \| uniq -d)"`` |
| **`did NOT converge` DOES NOT MEAN THE RESULT IS UNSTABLE, AND MUST NOT BE READ AS ONE.** The line says *no two reads within the limit agreed* and entitles a reader to nothing further. Track 5 has produced **three** values in ten sessions, and the commonest (`E0036697`, six sessions) is reported both converged and not; `6902BCF0` (three) and `4065BECC` (one, new on 2026-09-22) only ever non-converged. Track 3 is the other arm — five values, and non-convergence on four different ones. **2026-09-22 produced a value never seen before on BOTH tracks**, which is why neither check pins a count any more: each asserts that some one value carries both verdicts, which is the claim itself and survives every further session | ``test -n "$(for f in docs/rig-*/rips/secure-reread.log; do awk '/^Track [0-9]+ (ripped and encoded\|read) successfully!/{i=($2==5)} i&&/^  EAC CRC32:/{c=$3} i&&/Secure re-read:/{print c, ($0~/did NOT/)?"NOT":"conv"; i=0}' "$f"; done \| sort -u \| awk '{print $1}' \| uniq -d)"`` |
| **`Accurip 450:` IS A ONE-SECTOR CHECKSUM AND CANNOT SPEAK FOR THE TRACK.** `crip_process_checksums()` accumulates `acu_sum_1_450` over the 588 samples at sector 450 only, which is why tracks 3 and 5 carry the identical `450` value across both sessions while track 3's whole-track checksums differ. Read from the source before calling that a defect. **This row used to end *"the log already carries the qualifier (`track is partially accurately ripped`)"*, and that is FALSE**: the qualifier claims more than one sector can support. Round 26's real test has a track-1 read of `0E91CD1A`, wrong against the `B0D122E7` of five other reads, labelled *"partially accurately ripped"* on its 450 match alone, and the same wrong read is in the 2026-09-11 session's `derived-wavpack.log` (`docs/KNOWN-ISSUES.md`) | ``grep -c '451 \* (CDIO_CD_FRAMESIZE_RAW' src/checksums.h`` |
| **ALL SIXTEEN 2026-09-15 LOGS VERIFY AGAINST A LATER BUILD** — both sessions, run at `411c80a`, 54 commits past the `fe4d2c4` that wrote them, and re-run by the check below at whatever `HEAD` is. The `0.6.49` session's addendum files exist precisely so the logs stay byte-exact and this keeps passing. Which re-confirms the cross-build `Log FUN512:` property and proves the filing altered no byte | ``for f in docs/rig-2026-09-15-fe4d2c4/rips/*.log docs/rig-2026-09-15b-fe4d2c4/rips/*.log; do case "$f" in *.eac.log) continue;; esac; ./build/src/cyanrip -Y "$f" >/dev/null 2>&1 \|\| exit 1; done`` |
| **The PLURAL `Read stalls:` rendering**, `5 reads exceeded 10s; longest 11s (track 1, LSN 8322)`. 2026-08-26 produced only the singular | `grep -c "^Read stalls:    5 reads exceeded 10s" docs/rig-2026-09-03-978f9b0/rips/after-cancel.log` |
| **Every 2026-09-03 log verifies against a LATER build.** `-Y` exits 0 on all seven, which re-confirms the cross-build property and proves the filing altered no byte | `for f in docs/rig-2026-09-03-978f9b0/rips/*.log; do case "$f" in *.eac.log) continue;; esac; ./build/src/cyanrip -Y "$f" >/dev/null 2>&1 \|\| exit 1; done` |
| **The 2026-09-03 `cancel me` rip does NOT show a cancel** — `Rip completed:  yes (3 of 14 tracks)`, third session running. But the session **does** hold evidence one happened: an exit 3 `No FUN512 checksum found`, a rip killed before it writes its signature. The interrupted artifact existed and is not in the bundle | `grep -c "No FUN512 checksum found" docs/rig-2026-09-03-978f9b0/session/DIAGNOSTICS.txt` |
| **ROUND 21 CLOSED `GO`/`GO` ON 2026-09-18**, five laps, 32 days before its declared close-by, approving the pair cyanrip `fe4d2c4` + Platterpus `0.6.50`. Neither pin moved all round and the test pin `3952c03` **did not become a release** (§6a). **A close authorises a release and does not perform one** — `meson.build` is still `+platterpus.12` and the ledger's last row is seq 22. One of the three close conditions, *"your parser reads `Retry limit:`"*, **was never satisfiable as worded and that is ours**, explained in lap 5 §2 rather than ticked | ``python3 tools/release-gate.py \| grep -q 'round 21 .*: closed  (verdict GO, peer GO'`` — **the round's closure, which is permanent.** This row first checked `--release-gate` exits 0, which passed for exactly as long as no other round was open: round 22 opened hours later and the row went stale though nothing about round 21 had changed. **A re-check must assert the durable fact, not a state a later round can flip** |
| **THREE ROUND-21 DIGESTS AGREE ACROSS TWO IMPLEMENTATIONS THAT SHARE ONLY A WRITTEN SPEC** — `4c70113a594df502 over 3`, `34ee5bd1e7a3bf8e over 4` and `9e1bd1da1c5bb5c6 over 5`, each correct of the population it names as the laps were sent one by one. **The declared `over N` is what makes three different numbers read as arithmetic** rather than as a conflict, and this is the round where that first had to do real work | `python3 tools/round-digest.py 21` (= `9e1bd1da1c5bb5c6 over 5`, the final immutable set) |
| **THE `fast_verified` WHOLE-DISC PATH RUNS ON HARDWARE, ON THE TEST PIN** — round 21 §0.1's first close condition. 2026-09-17, `3952c03` under Platterpus 0.6.50 (`4bedb45`), BDR-209D, the 14-track reference disc: `rip_goal: fast_verified` with `Rip completed:  yes (14 of 14 tracks)`, `Ripping errors: 0`, 58m38s. **It had never run**: their §C found section F inherited its `rip_goal`, so the 2026-09-15 run proved one thing twice. **One GUI-driven rip, not an acceptance sweep**, which is the shape their lap 4 kept from our withdrawn draft. Filed at `docs/rig-2026-09-17-3952c03/` | `grep -c '^Rip completed:  yes (14 of 14 tracks)' docs/rig-2026-09-17-3952c03/rips/whole-disc-fast-verified.log` |
| **`Retry limit:` PARSES CLEAN ON A REAL LOG, and that is all §0.1 item 2 could ever have established.** Their report carries `log_parse: {"ok": true, "note": null}` and is fully populated from a log carrying the new label, so the risk the condition guarded — an unrecognised disc line tripping their completeness sweep on every rip — is retired. **The condition as WORDED ("your parser reads it") names a behaviour their code documents as deliberately absent**; see the `peer-source:` row below | `grep -c 'Retry limit:    3 (per frame, and per whole-track re-read)' docs/rig-2026-09-17-3952c03/rips/whole-disc-fast-verified.log` |
| **`cyanrip --version` hung twice on the rig through `~/.local/bin/cyanrip`** (0-byte P3 artifact; `timeout -k 10 60` → exit 137) while Platterpus got the banner from the same path 3 s earlier in under a third of a second. **NOT reproduced here and NOT attributed:** interleaved on this tree, `--version` / `-v` / `-V` are indistinguishable at 0.034–0.042 s | `for f in --version -v -V; do ./build/src/cyanrip $f >/dev/null </dev/null; done` (all return at once) |

## Things that are true and read as false

| fact | check |
|---|---|
| **ON A DISK-FULL FAILURE THE LOG SAYS `Ripping errors: 0` AND `Rip completed:  yes` WHILE THE `-j` RECORD FROM THE SAME RUN SAYS `ripping_errors: 3`, `exit_code: 1`.** Measured 2026-09-07 on a 64 KiB tmpfs: three `Error writing trailer/packet: No space left on device!` lines printed, exit 1, and the human-readable archival record recorded a clean complete rip. **The placement is DELIBERATE, not an oversight** — `cyanrip_main.c:2690` carries a comment putting `cyanrip_log_finish_report()` *"BEFORE the encoder-status loop so that `Ripping errors:` counts exactly what it counted before"*, and the encoder failures increment at `:2698`, after. The `-j` snapshot at `:2708` is later still, so it sees the true count. **Two surfaces, one name, one run, different numbers.** Not fixed and **must not be fixed unilaterally**: moving that call changes the value of a line Platterpus parses, which is a handshake change, not a drive-by. Carried to round 16 lap 3 | — past: reproduced on a size-limited tmpfs, which needs mount privileges; the portable near-miss (a directory where the .flac goes) fails at encoder INIT instead and is correctly reported, `Ripping errors: 1` and `Rip completed: no (aborted)`, so it does not discriminate |
| **THE `-j` RECORD NOW CARRIES TWO INSTANTS, AND THE SCHEMA IS `cyanrip-diagnostics/4`.** Was: no wall clock at all, so a run that opened **no logfile** — which is the record's whole reason to exist — could say what happened and never when. `started_at` is captured in `crip_diag_enable()`, `finished_at` from `atexit`; **two fields and never one**, because a single timestamp on a file written at exit names when the process ENDED while a reader takes it for when the rip HAPPENED. Round 15 lap 14 §5 item 7 named this half and `8d465f1` fixed only the other one. **The ask to Platterpus is open**: the last value we hold for their allowlist is `SUPPORTED_SCHEMAS = {1, 2}`, printed as integers in their round-11 lap 2 under a `[MEASURED]` heading, and their round-10 lap 4 says they have never consumed the file — **we do not claim to know what their build does with a `/4`** | `python3 tests/rip_images.py build/src/cyanrip tests/fixtures diagnostics` — asserts the schema, that each stamp names an instant, and that they order. Independence is asserted structurally (two `crip_iso8601_now()` sites); no rip here takes a second, so a behavioural proof is impossible and the scenario says so |
| **TWO GENERATED ARTIFACTS WENT STALE WHILE THEIR GUARDS PASSED, AND BOTH GAPS ARE NOW CLOSED.** `docs/golden-reference.log` kept a bare `creation_time: …T02:49:53` for three commits after the binary started emitting an offset, because its guard reads the banner's **version** and **cleanliness** and nothing about **format**; 69 of 69 tests passed over it. `docs/sample-interrupted.diagnostics.json` sat at schema `/3` from a build three releases back, because `--interrupted --check` shape-checked the **log** and never opened the **record** — and the contract derives its field table from the committed records, so it then published `started_at` as *not in every record*: true of the samples, false of the binary | `python3 tests/rip_images.py build/src/cyanrip tests/fixtures golden_reference_matches_a_fresh_rip && python3 tools/gen-golden-reference.py --interrupted --check` |
| **THE ASCII-APOSTROPHE DEFECT IS FIXED.** Was: `-a`/`-t` reach `av_dict_parse_string`, whose tokeniser treats `'` as a QUOTE — a bare apostrophe opened a run that never closed, so `1=title=Don't Stop:artist=A:isrc=I` gave `title: Dont Stop:artist=A:isrc=I` with artist and isrc unset and no diagnostic. `crip_escape_bare_quotes()` escapes an apostrophe **only when it is not already escaped**. That distinction is the whole fix: Platterpus escapes `'` as `\'` at `cyanrip_backend.py:699` across all eleven sites (their lap 15 §A2), so escaping unconditionally would double theirs and put a literal backslash in the record — fixing it for a hypothetical consumer by breaking the real one. Announced as round 15 lap 14 §5 item 3. Upstream's | `python3 tests/rip_images.py build/src/cyanrip tests/fixtures consumer_argv` — asserts **both** forms; reverting fails 3 checks on the bare one and none on the escaped one, which is the correct asymmetry |
| **`-H` NO LONGER DISCARDS DE-EMPHASIS.** Was: `init_filtering()`'s filter string was a ternary cascade — `hdcd ? "hdcd" : deemphasis ? "aemphasis=type=cd" : …` — so with `-H` the de-emphasis filter was never in the graph, and on `preemph.cue` `-H`, `-H -W` and `-H -E` were **byte-identical**: both the disable and the force flag inert. Meanwhile the log printed `(deemphasis applied)` from the SETTINGS and the cue dropped `FLAGS PRE` on the same reasoning, so **audio, log and cue were self-consistently wrong** — checking one against another found agreement. Now composed, and the two `aresample` bridges are load-bearing: libavfilter's `hdcd` filter calls `avfilter_graph_set_auto_convert(NONE)` in its own init and that setting is **graph-wide**, so `hdcd,aemphasis=type=cd` fails to configure outright (*"do not have a common format and automatic conversion is disabled"*) and `aformat` cannot fix it — aformat only CONSTRAINS a link and relies on the converter that is switched off. The three reporting sites now share one predicate, `crip_deemphasis_active()`; that half is a pure refactor with an identical truth table and **no test can discriminate it**. Announced as round 15 lap 14 §5 item 2. Upstream's | `python3 tests/rip_images.py build/src/cyanrip tests/fixtures deemph_with_hdcd` — restoring the cascade fails 2 checks; the input that separates a cascade from a composition is **both flags at once**, which `sc_filters()` never used |
| **EXACTLY ONE TEST DETECTS A BEHAVIOURALLY-INERT EDIT, AND `EXCLUDED_TESTS` NAMES IT.** A mutation score is vacuous if any test in the suite fails on the *edit* rather than the *defect*, because such a test kills every mutant; that is how `src/cyanrip_encode.c` once scored **100.0% over 125 mutants** and meant nothing. Probed 2026-09-16 at `d92c4e1` by appending a comment at EOF of `src/utils.c` in a detached worktree and running the whole suite: **84 OK, 1 FAIL**, the failure being `images - cyanrip:contract_build` — which is the only entry `tools/mutate.py`'s `EXCLUDED_TESTS` has. It had not been re-probed since three images scenarios were added; all three come out **OK**, so none of them detects an edit. **Second finding, not what the probe was for:** `cyanrip:Sanitizer sweep` also passed, so `tools/sanitize-run.py` no longer drags `contract_build` in behind it, which is the other edit-detector this repo has had | — past: `docs/inert-edit-probe.log`. Re-running it is a full build plus all 85 tests, about seven minutes, and it must happen in a worktree — so it is deliberately not a gate. The cheap half is checkable: `grep -A5 '^EXCLUDED_TESTS' tools/mutate.py` must still name `contract_build` and nothing else, and a second name there without a fresh probe is the thing to distrust |
| **`src/checksums.h` IS NOW SWEPT, AND SCORES 85.7%** — 7 mutants, 6 killed, **0 stillborn**, so the worry that header mutants would not compile was unfounded. It had never been swept because `mutate.py`'s `TARGETS` was `.c`-only, leaving the whole EAC CRC32 / AccurateRip v1/v2/450 arithmetic unmeasured. **One survivor, and it is a real gap, not an equivalence**: `checksums.h:74` `>=` → `>` on the AccurateRip **450-frame window boundary** (`(s->acu_mult - 1) >= (450 * (CDIO_CD_FRAMESIZE_RAW >> 2))`). No fixture lands exactly on that boundary, so nothing distinguishes the two. Killing it needs a disc whose length hits the window exactly — **not built** | `python3 tools/mutate.py --count src/checksums.h` reports 7; the full sweep is `python3 tools/mutate.py src/checksums.h` |
| **`artist` IS BACKFILLED FROM `album_artist` WHEN UNSET**, so asserting `artist` alone cannot prove a `-t …:artist=…` landed — on a single-artist disc the backfill produces the expected string whether the field parsed or was swallowed. Found 2026-09-05 by revert-proving `consumer_argv`: the apostrophe defect injected into track 1 failed on `title` and `isrc` and **not** on `artist`. `cyanrip_main.c`'s `album_artist && !artist` is also the sharpest mutation survivor the sweep found, and for the same reason — the input that separates `&&` from `\|\|` is a compilation | `python3 tests/rip_images.py build/src/cyanrip tests/fixtures consumer_argv` — track 2 carries a guest artist so the two sources are distinguishable |
| **INVALID UTF-8 IS NOW SUBSTITUTED, NOT TRUNCATED.** Was: `av_utf8_decode` failing made `crip_bprint_sanitize()` log and return, so `START<bad>TAIL` named a directory `START` and `<bad>TAIL` named nothing — and an empty **leading** component made a multi-component `-D` resolve to an **absolute** path (measured with the consumer's own `-D {album_artist}/{album}`: a rip landed in `/Some Album`, exit 0). Now yields U+FFFD and continues. Announced as round 15 lap 14 §5 item 4 before shipping. Upstream's | `python3 tests/rip_images.py build/src/cyanrip tests/fixtures utf8_naming` — four checks; the containment arm uses a **relative** scheme with `{album_artist}` first, because with an absolute prefix an empty component only yields `//` and the check could not fire |
| **A LOGFILE'S FIRST LINE IS THE FORK BANNER AGAIN.** The naming parse error went through `cyanrip_log()` before the header, so it became line 1, displacing the banner `PROJECT_FORK_ID` is read from — while `-Y` still returned 0. Their dispatcher reads exactly the first non-blank line, so such a log reached the wrong parser and yielded **zero tracks from a fourteen-track disc** with no error (their lap 15 §C1). Removed by substituting rather than logging. Announced as item 5; cleared by their §C1 | `python3 tests/rip_images.py build/src/cyanrip tests/fixtures utf8_naming` |
| **`docs/seam-commands.md` HAS TWO WRONG ROWS, AND THE CHECKER FOR §7 NOW EXISTS.** Line 504 publishes `-p '99=drop'` as **accepted, exit 0**; the binary refuses it (`Invalid track number %i for pregap, list has %i tracks!`, `cyanrip_main.c:2254` at the pin, control `-p '1=drop'` exits 0, so the probe discriminates). **Line 97 publishes `-D` as `directory` / `str, path` / `writable` / "output directory"; it is `folder_scheme`, *"Directory naming scheme"* (`cyanrip_main.c:1603`), a RELATIVE scheme with `-F` its per-track sibling.** Platterpus found the second one in round 15 lap 16 §B3, from our source at the pin, while deriving `-D` semantics for their own guard — **two is a pattern where one was a coincidence**, and it is why a generated table beats a shared hash: both sides have been attesting to these bytes for laps. The `-D` row is not incidental — its real semantics are exactly why held item 4 mattered, since an empty leading component made a multi-component scheme resolve **absolute**, and a reader who believed line 97 would not have looked. **Neither file is corrected**: jointly owned, so it takes a version bump both sides ship. Round 16 | ``grep -q "'99=drop'.*accepted" docs/seam-commands.md && grep -qF '`-D` \| directory' docs/seam-commands.md`` — passes while **either** stale cell stands. Spelled with the row's own cells on purpose: it exercises both of this file's escapes at once — the `\|` the row parser splits on, and the **double-backtick span**, without which a command containing a backtick was truncated at it and the fragment reported as a stale fact |
| **CI HAS NEVER EXECUTED ONCE.** `actions_list list_workflow_runs rmccann-hub/cyanrip` -> `{"total_count":0}`, while `list_workflows` -> 1 workflow, `state: "active"`, created 2026-08-17. `main.yml:11` names `platterpus-fork` in the `push:` trigger, added at `f5fe9d4` 2026-08-18, and **215 commits** have been pushed to that branch since. The YAML parses. **Cause is `unknown (not determinable from the tree or the Actions read API)`** — needs repository Settings -> Actions. Consequence: every gate this project owns has only ever run when a human ran it | `git rev-list --count $(git log --format=%H -S platterpus-fork -- .github/workflows/main.yml \| tail -1)..HEAD` — the commit count; the run count needs the GitHub API |
| **THE THREE ACCURATERIP RESPONSE-PARSER DEFECTS ARE FIXED**, and the path is unit-tested for the first time. Was: `accurip.c` `av_realloc` with no NULL check feeding a `memcpy` (SIGSEGV — reproduced, exit 139), `strcmp` on a `content_type` that is NULL when a 200 carries no Content-Type header, and `strstr` over a buffer built by raw `memcpy` and **never NUL-terminated**. `crip_find_html_marker()` is split out so it can be reached: every scenario passes `-N -A -U`, so `crip_fill_accurip()` returns first and none of this had ever executed under test — coverage agreed independently at **27.14% branch, the lowest real file in the tree**. **The NULL `content_type` guard is fixed but NOT pinned by a test**: it sits inside `crip_fill_accurip()`, which needs a live curl handle. Upstream's, all three | `./build/tests/arresp_test` — 18 checks; the realloc and bound arms each revert-prove individually |
| **THE `fun512.c` OVERFLOW IS FIXED.** Was: `fopen(".")` on a directory SUCCEEDS and `ftell` returns exactly `LONG_MAX`, so `len <= 0` passed and `len + 1` was signed overflow — reached by `cyanrip -Y <a directory>`, and survived only because the wrapped value made `av_mallocz` fail. Now bounded by `CRIP_LOG_MAX_SIZE` **before** the addition. Observable behaviour is unchanged on every input (`Couldn't read "."!`, exit 5). Upstream's | `./build/tests/fun512_test` — the oversize case uses a sparse file above the cap, so a revert changes the verdict `IO_ERROR` → `NO_CHECKSUM` in **any** build, not only under UBSan |
| **THE TWO BARE `return 1;` NOW RUN THE TEARDOWN.** `cyanrip_main.c`'s `cyanrip_log_init()` and `cyanrip_cue_init()` failure paths were the only two exits bypassing `end:`, so `cyanrip_ctx_end()` never ran — which also calls `cdio_cddap_close_no_free_cdio()` and `cdio_destroy()`, so **the failures on which the archival record could not be opened were the ones that never closed the drive**. Fixed by calling the teardown directly, **not** `goto end`: `end:` emits the completion footer and `rip_ran_to_completion` is still 0 there, so jumping would add a `Rip completed: no (aborted…)` line to a log that has none — contract-visible, and round-16 material. Upstream's | `python3 tests/rip_images.py build-asan/src/cyanrip tests/fixtures outdir_is_a_file` — reverting reproduces `12,865,195 byte(s) leaked in 36 allocation(s)`. The **default** build reports UNPROBED, because `crip()` captures the subprocess stderr the sanitizer writes to |
| **A TRUNCATED BACKING `.bin` DOES *NOT* PRODUCE A FALSE CLAIM — checked, because an audit lane said it did.** Two shapes, both correct: a `.bin` short enough that a later track STARTS within it is read as a **shorter disc** and reported accurately (`Total time: 00:04.50` for 350 sectors, both tracks carrying real audio, exit 0); a `.bin` so short that a track's start is **past EOF** is refused with `Unable to open device!` at column 0 and exit 1. No zero-byte track, no `Rip completed: yes` over missing audio. **A grep for `error\|fail\|warn` finds nothing in that second output and nearly produced a phantom "silent non-zero exit" here** — the message contains none of those words | — past: measured 2026-09-05 on truncations of `cdda.bin` at 350 and 250 sectors against `basic.cue`. No check: asserting an absence of misbehaviour needs the two rips, and the row exists to stop the re-derivation |
| **Per-track paranoia counters do NOT sum to the disc totals under `-Z`.** The per-track baseline is snapshotted after `repeat_ripping:`, so it describes the **last pass**; the disc counters sum **every** pass. Equality holds only when each track was read once | — past: round 13, measured 15+10+5=30 against a disc total of 90 |
| `Cache model:` reports what paranoia **models**, never what the drive has. Since 2026-08-26 it says `(drive cache probed separately…)` when `-x` ran | `./build/tests/diag_test` |
| `none` and `unknown (reason)` are different claims everywhere in the log, on purpose | — structural: a policy over every log line, not a single assertion |
| **`Done; (no matches found, but hit repeat limit of N)` is NOT an error.** It is the non-convergence *measurement*, followed immediately by `Track N ripped and encoded successfully!`. It sat in P5's failure inventory on `goto finalize_ripping` alone until 2026-09-03, and a consumer recorded five errors against a rip that completed 14 of 14 with none | `test $(grep -A1 "no matches found, but hit repeat limit" docs/rig-2026-09-03-978f9b0/rips/full-acceptance-angle-bracket.log \| grep -c "ripped and encoded successfully") -eq 3` |
| **A bare `goto` is not evidence of a failure path** — it is the absence of evidence plus a note about where control went. P5a carries the 7 rows in that state and claims nothing in either direction; `end:`, `end_meta:` and `finalize_ripping:` are all fall-through-reachable from success, so no rule separating them is about anything but the label's name | `python3 -c "import importlib.util as u,sys;s=u.spec_from_file_location('g','tools/gen-provider-contract.py');m=u.module_from_spec(s);s.loader.exec_module(m);e,o=m.partition_fatal(m.collect()[2]);sys.exit(0 if o and not any(m.GOTO_ONLY_RE.fullmatch(r[4] or '') for r in e) else 1)"` |
| **`ddf7ac3` is a cyanrip commit** (`0.9.4-rc1+platterpus.5`), not a Platterpus one, despite standing in `HANDSHAKE-PEER-PIN` through two closed rounds | `git log --oneline -1 ddf7ac3` |
| **A commit that RESOLVES is not thereby REACHABLE.** A clone holds every object its reflog still names, so `git log -1 <sha>` succeeds on a commit that `git commit --amend` orphaned — one that `git gc` destroys and a fresh clone never has. `merge-base --is-ancestor` is the only test that separates them, and `seam-check.py` now runs it on `HANDSHAKE-FROM-COMMIT`. Round 15 lap 3 declared such an orphan and every check passed it | `python3 tests/release_gate.py` — the regression test builds an orphan; **grading a well-formed lap does not discriminate**, since a reachable commit resolves too |
| **`HANDSHAKE-FROM-COMMIT` is the PARENT of the commit that adds the lap**, never the lap's own commit — the same fixpoint as a generated artifact naming its build. Every lap that declares a bare SHA obeys it | `n=0; for f in docs/handshake/round-*.md; do c=$(grep -c "^HANDSHAKE-FROM-COMMIT:" "$f"); [ "$c" = 1 ] \|\| continue; s=$(sed -n "s/^HANDSHAKE-FROM-COMMIT:[ \t]*\([0-9a-f]\{7,40\}\)[ \t]*$/\1/p" "$f"); [ -z "$s" ] \|\| { n=$((n+1)); git merge-base --is-ancestor "$s" HEAD \|\| exit 1; }; done; test "$n" -ge 10` |

## Upstream `cyanreg/cyanrip` — what it still lacks, for merge-back

Checked against `master` at `f8ebf48`, not recalled. **`-Y` was on this list from
memory and is wrong: upstream has it.** That is the whole reason the list is
checked rather than written.

| fact | check |
|---|---|
| Upstream still calls `cyanrip_log()` **inside the signal handler** — a mutex and stdio in a handler, the deadlock that hangs the process with the drive held | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_main.c) && printf '%s' "$u" \| awk '/on_quit_signal\(int/,/^}/' \| grep -q cyanrip_log` |
| Upstream handles **no SIGTERM at all** — 0 occurrences | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_main.c) && printf '%s' "$u" \| grep -q on_quit_signal && ! printf '%s' "$u" \| grep -q SIGTERM` |
| Upstream has `cyanrip_log_finish_report()` immediately **above** `end:`, so every `goto end` skips the completion footer | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_main.c) && printf '%s' "$u" \| grep -B1 '^end:' \| grep -q cyanrip_log_finish_report` |
| **Upstream still sets paranoia's cache model to 1 sector on image drivers**, `cyanrip_main.c:256` at `f8ebf48` — the size doubles as the c_block read chunk, so the verification logic is left no overlap and emits zeroes: **corrupted audio reported as `Ripping errors: 0`**. Ours is 16. A report is **drafted and not filed** in `docs/upstream-cachemodel-report.md`; Platterpus's round 7 H11 ruling was *"yes, and I would not wait"* | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && git show $r:src/cyanrip_main.c \| grep -q 'cdio_paranoia_cachemodel_size(ctx->paranoia, 1)'` |
| **Upstream still has no pregap detection — PR #115 is unmerged**, so `src/pregap.c` does not exist at `f8ebf48`. **Not ours to contribute**: the algorithm is UltraFuzzy's and we carry it with three review fixes (`docs/pregap-carry.md`). The reason it matters here is that `pregap.c` is the file a sync would collide on, and the carry note's *"still open as of 2026-07-31"* is the kind of dated absence that needs a check rather than a re-read | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && ! git ls-tree --name-only $r src/ \| grep -q pregap` |
| **`-Y` / `--verify-log` is already upstream** — not ours to contribute | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_main.c) && printf '%s' "$u" \| grep -q verify_log` |
| **Upstream's filter string is a ternary cascade**, so `-H` wins outright and de-emphasis is never in the graph — `-W` and `-E` both inert under `-H`. Ours is identical | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_encode.c) && printf '%s' "$u" \| grep -q 'hdcd ? "hdcd" :'` |
| **Upstream prints `(deemphasis applied)` from the SETTINGS**, not from what happened, so the log claims it under `-H` when it did not occur. Ours is identical | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_log.c) && printf '%s' "$u" \| grep -q 'settings.deemphasis \|\| ctx->settings.force_deemphasis'` |
| **Upstream parses `-a`/`-t` with `av_dict_parse_string`**, whose tokeniser treats `'` as a quote — a bare apostrophe swallows every later field. Ours is identical, and it is the defect most likely to corrupt a real rip | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_main.c) && printf '%s' "$u" \| grep -q 'av_dict_parse_string'` |
| **Upstream truncates a name at invalid UTF-8 and can thereby make a multi-component `-D` absolute** — same `av_utf8_decode` failure in `naming.c`, same line, verified against `master` rather than inferred from ours | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && git show $r:src/naming.c \| grep -q 'Error parsing string' && git show $r:src/naming.c \| grep -q 'av_utf8_decode'` |
| **Upstream hands `-r` to libcdio-paranoia unrounded** (`cyanrip_main.c:428-429` at `f8ebf48`), and the library checks the per-frame limit only at multiples of 5. So upstream `-r 3`, any value that is not a multiple of 5, and `-r 0` **do not return on an unreadable sector** at the default paranoia level. Measured on ours before the fix, with the same call and one injected bad sector (`tests/badsector.c`): `-r 3` did not finish in 90 s, and `-r 10` finished in about a second. Upstream's default, 10, is safe. Ours rounds up (`crip_frame_retry_limit()`) | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_main.c) && printf '%s' "$u" \| grep -q cdio_paranoia_read_limited && ! printf '%s' "$u" \| grep -q crip_frame_retry_limit` |
| **Upstream tags `media` from the `-H` SETTING**, `decode_hdcd ? "HDCD" : "CD"` (`cyanrip_main.c:1609-1610` at `f8ebf48`), before a sample is read. So every `-H` rip is tagged HDCD whatever the disc is. The rig's P3 section shows it on every filed transcript, `HDCD detected: no` above `media: HDCD`. Found in round 26's real test and fixed here (`media_tag`) | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && git show $r:src/cyanrip_main.c \| grep -q 'decode_hdcd ? "HDCD" : "CD"'` |
| **Upstream's AccurateRip tally counts a track whose read was interrupted.** `cyanrip_log_finish_report()` tests only `t->ar_db_status`, and a SIGINT `break`s to that report (`cyanrip_main.c:2070-2111` at `f8ebf48`). An interrupted read past sector 450 leaves a matching one-sector `Accurip 450`, so the partial tally counts a track that was not ripped. Read from the source, NOT run upstream. Found on ours in round 26's real test and fixed here (`test_disc_tally_skips_an_interrupted_track`) | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && u=$(git show $r:src/cyanrip_log.c) && printf '%s' "$u" \| awk '/void cyanrip_log_finish_report/,/Tracks ripped accurately/' \| grep -q 'if (t->ar_db_status == CYANRIP_ACCUDB_FOUND) {'` |
| **Upstream's `crip_find_ar()` compares a 450 checksum against the WHOLE-TRACK one on a miss.** `if (is_450 && e->checksum_450 == checksum) … else if (e->checksum == checksum)` (`accurip.c:239-242` at `f8ebf48`), so a 450 lookup that misses an entry's frame checksum falls through to its whole-track checksum. One chance in 2^32 per entry. It reaches the log's `Accurip 450` line and the offset search, which scans thousands of offsets per track. Found by Platterpus, round 27 lap 2 B1a, reading our source. Their *"unchanged since `b8e5e79`, 2020"* is not checked here: our `master` is shallow at `c4740bc`. Fixed here (`test_accurip_450_never_compares_the_whole_track_checksum`) | `r=$(git rev-parse -q --verify master \|\| git rev-parse -q --verify origin/master) && git show $r:src/accurip.c \| grep -A2 'is_450 && e->checksum_450 == checksum' \| grep -q 'else if (e->checksum == checksum)'` |

## Platterpus's side — some relayed, some read from their tree

**The tag on each row is the difference, and it matters.** `— theirs:` means a
lap told us and a lap can be wrong. `— peer-source:` means it was read at a
pinned commit in their public repository, with the path and line, which is what
`CLAUDE.md` has required since 2026-09-13 — nothing was relayed and no lap is in
the chain. This heading used to read *"held because they told us, not because we
checked"*, which stopped being true of every row the day their source became
readable from here.

| fact | check |
|---|---|
| **A HASH OF A HELD LAP IS NOT A FACT ABOUT THAT LAP, AND OUR OWN LAP 5 IS THE WORKED EXAMPLE.** It recorded their held round-21 lap 4 as blob `4c672bcf…` / 31,732 B / sha256 `989427bd…` at `27a174dc` and instructed *"file it byte-exact against that sha256"*. **All three still reproduce at that commit** — a SHA-pinned read is durable because a commit is immutable. The DOCUMENT had moved twice by the time they flagged it: **47,478 bytes** at `0f1b54a4`, half again as large, and still `READY-TO-READ: no`, so `--announce` moves it again. A held lap declares itself mutable in its own header; quoting its digest as an identity is citing a branch tip one level in. **They found it in our text before we did.** Split here into `HANDSHAKE-INBOUND-HELD` (sent) and `HANDSHAKE-INBOUND-OBSERVED` (held) | — peer-source: measured against `platterpus@27a174dc` and `platterpus@0f1b54a4`, `docs/handshake/outbound/round-21-lap-04.md` |
| **THE ROUND-DIGEST AGREEMENT IS STRONG EVIDENCE BECAUSE THE SHARED ANCESTOR IS ABSENT BY CONSTRUCTION** — the inverse of the two-related-witnesses rule, and Platterpus stated it better than we had. Two implementations agreeing is normally weak: the commonest cause is a common ancestor, which is how a shared fixture and a shared clock each fooled this project while every test passed. `scripts/round_digest.py` was built from our WRITTEN SPEC and has never read our code, so seven consecutive rounds of identical digests measure the convention rather than a copy. **Before crediting an agreement, ask what the two parties share** | `python3 tools/round-digest.py 21` — `4c70113a594df502 over 3 lap(s)`, the value their lap 4 declares |
| **A `verified/` RECORD IS NOT A LAP, AND THEIRS IS NUMBERED AS ONE ANYWAY.** `platterpus@7049d66:docs/handshake/verified/round-21-lap-06.md` declares `HANDSHAKE-LAP: 6` at column 0 while its own `READY-TO-READ` says *"a verification file, not a lap: it is not sent and adds nothing to your inbox"*. It is numbered so **their** gate can read a closing verdict off their own side — a round closes on the gate of whichever side sent the last lap — with precedent at `verified/round-20-lap-04.md`. **It is not sent, not in our `inbound/`, in no digest, and must NOT be filed here**: filing it would change our digest. Their own line is the sharpest: *"If this had been a sendable lap the number would have collided with yours, which is exactly K1."* **The caveat we keep:** under `PROTOCOL.md` §5a a column-0 `HANDSHAKE-LAP:` is what a conforming enumerator counts, whatever the prose beside it says — the mirror of why our `STATUS.md` carries `STATUS-` prefixes | — peer-source: `platterpus@7049d66:docs/handshake/verified/round-21-lap-06.md:3,9,10` |
| **THEIR PARSER RECOGNISES `Retry limit:` AND EXTRACTS NOTHING FROM IT, BY DESIGN.** It is an entry in `_IGNORED_DISC_LINES` — the allowlist of disc rows the parser matches so its completeness sweep does not trip, and from which no field is derived. Their own comment: *"We extract nothing from it either way, so the rename is invisible to the PARSE."* Both labels are in the one pattern, so nothing regressed at the rename. **This is why round 21 §0.1 item 2 cannot be satisfied as worded** — we wrote a close condition asserting a mechanism in their code without reading it first, and the comment was in their tree when the lap was written | — peer-source: `platterpus@4bedb45:src/platterpus/parsers/cyanrip_log.py:1876-1879` |
| **`Track N ripped and encoded successfully!` IS THEIR BLOCK DELIMITER, NOT A FIELD THEY READ — so round 22's rename costs them every per-track fact, not one value.** `_TRACK_START`, commented *"A track block opens with its outcome line"*; its alternation matches **both** arms we renamed, plus `is data:`, which we did not touch. Line **2452** is the **only** `_TrackAcc(` construction site and sits inside that match, so no match means **no track is parsed**; `rip_completed_tracks` is set separately at line **1458** from the disc-level footer. **The same parse reports 14 of 14 tracks and `No errors occurred` over a record carrying zero tracks.** They reported it in round 22 and we verified it by reading their source at the SHA they cited, not on their word. **They did not veto**: the ask is their round-20 ordering — their both-wordings release first, `+platterpus.14` second — which makes `.14` the first release of this fork with a consumer-side prerequisite. **The order was honoured: Platterpus 0.6.53 (`52b44282`) accepts both wordings and no earlier tag does, and `.14` shipped after it, at `3e01bb3` on 2026-09-22.** `Encoder errors:` they accept as a clean P2, all three arms tripping their completeness sweep, with tracks still parsing | — peer-source: `platterpus@417d61b:src/platterpus/parsers/cyanrip_log.py:212-215` |
| **`Ripping errors: 0` BECOMES `health_status = "No errors occurred"`**, in `_take_rip_errors`, *"same phrasing as whipper's healthy verdict so downstream string checks treat both backends alike"*. That string is what their EAC-compatible export writes, which is what round 21 §0.1 item 3 asks for and what the 2026-09-17 `3952c03` export carries at line 252. **A non-zero count renders as `f"{count} ripping errors"`**, so the field discriminates | — peer-source: `platterpus@4bedb45:src/platterpus/parsers/cyanrip_log.py:1617-1624` |
| `~/.local/bin/cyanrip` on the rig is a **host-exported Distrobox wrapper**; the real ripper runs in a container. **ARCHITECTURE YES, CAUSE NO** — their lap 16 §D1(a) withdraws it as the explanation for the empty capture, because a later run's capture was 111 bytes and a theory predicting *always empty* does not predict *sometimes empty* | — theirs: their lap 12 §E2, corrected by their lap 16 §D1(a) |
| Their `cyanrip` script verb is bounded: 300 s, then a kill, then 20 s, then an unreapable-child record with a **null** exit code | — theirs: their lap 12 §A |
| `0.6.26` was **not published** until 2026-08-25; the operator was on `0.6.25 (5f374aa)` before that | — theirs: their lap 13 §A2 |
| Their gate had the same `HANDSHAKE-TEST-PIN: none.` misreading as ours, fixed the same way | — theirs: their lap 12 §D |
