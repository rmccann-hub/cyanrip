INFO  album/discovery  newest rip found: /var/home/rmccann/platterpus-rig/platterpustestsession20260926t000413z/rips/The Police/derived mp3 20260926t000413 platterpus-fork-g221a1df
INFO  platterpus/version  0.6.60 (build 88c09dd)
INFO  ripper/version  cyanrip 0.9.4-rc2+platterpus.16 (platterpus-fork-g221a1df)  [ripper-version.txt]
INFO  ripper/handshake  unapproved — ripper build platterpus-fork-g221a1df is NOT the build this Platterpus was verified against (platterpus-fork-gdf91ae7, approved by handshake round 26). The rip is still bit-perfect if its own checks passed — but it was not produced by a jointly-verified ripper. That build is the pin an OPEN handshake round proposes (221a1df); it has not been approved by either project yet.
INFO  ripper/pin  this Platterpus pins df91ae7; the installed banner is above. A test pin is expected to differ during an open round.
OK    argv/integrity  all 16 flag token(s) we composed arrived intact (--consumer -F -G -N -T -Z -a -c -d -j -l -o -r -s -t) in the binary's own record of the 30 args we sent. Flag tokens are compared, not values: a value may be re-quoted in their rendering, a flag may not change  [argv-probe-output.txt]
INFO  handshake/note  closed: Handshake:      round 26 lap 6 closed, verdict GO -- released build
INFO  parser/paranoia  READ (the re-read witness): per-track counters sum to 2355; the disc block totals 2355. Scope: line present on 0 of 2 track(s) — secure re-read genuinely exercised: no. Also bound-checked, not summed in: OVERLAP, VERIFY (they do not scale with the passes)
INFO  parser/interrupted  no 'Interrupted at:' line in this log (rip_completed=True) — expected for a rip that ran to the end; only a cancelled or killed rip produces one
OK    parser/log  parsed 2 track(s) from derived mp3 20260926t000413 platterpus-fork-g221a1df.log
INFO  parser/cache-probe  no Cache probe: line in this log, and there never will be one — `-x` is not in the rip argv builder at all, so no Platterpus rip probes the cache. The probe is a separate `cyanrip -N -x -I` invocation (round 14 T3), whose exact argv, exit code and complete output are recorded in the SCRIPT REPORT and transcript, not here. Look there, not for an absence in this manifest.
SKIP  device  no --device given
