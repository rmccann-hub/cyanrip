# Release plan — `+platterpus.17`, round 27's fixes and the early-log banner

> **THIS PLAN WAS EXECUTED. Marked 2026-09-26, the same day.**
> `0.9.4-rc2+platterpus.17` shipped at **`e0471f4`**, ledger row **27**,
> `release_seq` 27, **stable**, authorised by **round 27**.
>
> **§4's sequence was followed exactly:** the plan (`67cee09`), the bump
> (`74872db`, red by construction), the regeneration (`382e68f`, generated by
> the bump), the candidate (`e0471f4`, the release), and the publish commit that
> carries this banner. The candidate was proved green before publication in a
> fresh worktree from a removed log, and by a `git archive` tarball built with
> `-Ddeclare_released=true`, whose rip of a disc image reports `Handshake: round
> 27 lap 6 closed, verdict GO -- released build`. The publish commit's message
> gives both results.
>
> **The text below is kept verbatim as the record of what was intended**, and
> its present-tense claims are now false.

*Written 2026-09-26, the day round 27 closed. **A plan, not a release.**
`meson.build` says `0.9.4-rc2+platterpus.16`, the ledger's last row is seq 26,
and `tools/release-gate.py --release-gate` exits 0: round 27 closed `GO`/`GO` at
our lap 6.*

**Nothing here is a decision still to make.** Round 27 settled it. Our lap 4
named `.17` as the candidate, our lap 6 corrected its contents to three `src/`
commits and said we would cut it once that lap was released (S19), and the
operator asked on 2026-09-26 to *"make new release and lap 1 when ready"*. v6 R8
is the rule: a round's close authorises a release of both applications,
provider first.

## 1. The condition

- **The authorising round: 27**, closed `GO`/`GO` on 2026-09-26 at six laps. Our
  lap 6 transcribes Platterpus's lap 5 `GO` (sha256 `33ab7dac…`).
- **No consumer-side prerequisite.** `ec0fe47` removes the words `track is
  partially accurately ripped`, and Platterpus's lap 5 §A and §C say nothing of
  theirs depends on them: their parser reads only `confidence N` from the
  parenthetical (`platterpus@88c09dd:src/platterpus/parsers/cyanrip_log.py:436-442`),
  and their two help texts that quoted the old tail were changed, at their
  `fafa565` (their lap 5 §A).
  `ee0221c` removes no string.

## 2. What `.17` contains, against `.16` at `221a1df`

| change | commit | who can notice |
|---|---|---|
| **a 450 lookup compares only 450 checksums**; a miss no longer falls through to the entry's whole-track checksum | `10f36fe` | nobody, in any rip so far: the false match needed one chance in 2^32 per entry. It reaches the `-f` offset search too |
| **the `Accurip 450` match says what it covers**: `(matches Accurip DB, confidence N, one frame only; whole-track checksums not found)` | `ec0fe47` | a reader of any track that matched on frame 450 alone. A P2 line, announced in round 27 lap 4 |
| **a log opened by a run that then fails early starts with the banner**, `Invoked as:`, `Handshake:` and `Consumer:`, and replays the early output | `ee0221c` | a reader of an early failure's log. Platterpus's dispatcher now takes such a log for cyanrip's (round 27 lap 6 S15) |
| each change's own test, in its commit; the cross-rip reader (`346bedb`), the lap statement checker (`f34a96c`), and the suite speedups (`f482a91` and after) | as named | our test suite |
| the round 27 bundles (`29cae9e`, `2e9884d`), laps and docs | as named | nobody running the binary |

`src/` changed at those three commits only. No other line's text changes, the
CLI is unchanged, and `-j` stays `cyanrip-diagnostics/6`.

## 3. The channel: stable, and the window it opens

**Stable, both channels resolving to it**, as `.15` and `.16` were. Platterpus
ships 0.6.61 after round 28 lap 1 names `.17`, carrying `FORK_PIN` `221a1df` and
`PIN_UNDER_REVIEW` `.17` together (their round 27 lap 5 §D). So their app offers
`.17` marked `unapproved` until round 28 reviews it. That is v6 R8 point 2's
mark, and it is not withheld: their offer states it, and a person decides.

## 4. The sequence

1. **The gate**: `--release-gate` exits 0.
2. **Bump** `meson.build` to `0.9.4-rc2+platterpus.17`. Red by construction.
3. **Regenerate, never hand-edit**: the provider contract, the golden
   reference and the interrupted sample, in their own commit, labelled
   *"generated by X, committed at Y"*. Never `--amend`.
4. **Name the candidate** at the first commit where the version and every
   derived artifact agree. Prove it green on its own: the full suite in a fresh
   worktree from a removed log, and a `git archive` tarball built with
   `-Ddeclare_released=true` reporting `released build`.
5. **Publish**, in one commit: one ledger row (seq 27, round 27), the manifest
   regenerated with `--check` exit 0, the changelog heading, the handshake
   README's pin blocks, `STATUS.md`'s release rows, `CLAUDE.md`'s release
   paragraph, and this file's banner.
6. **Then round 28 lap 1**, naming the release commit as its pin.

No tag: tag push is `HTTP 403` here, and the commit SHA is the identifier.

## 5. What this release does NOT verify

- **None of the three changes has run on a drive.** Round 27's Full run was on
  `.16`, and it printed the old 450 wording on its two wrong reads. `.17` is
  round 28's to test, with Platterpus 0.6.61.
- **A wrong read still logs `Ripping errors: 0`.** `Ripping errors:` counts
  operational failures, not read quality, and that is documented, not changed.
  It is one of the two lines of ours for which Platterpus's ledger graded round
  27's Full run `partial`.
- **`Encoder errors: none; 1 track encoded` over an interrupted rip is
  unchanged**, the other one (`docs/KNOWN-ISSUES.md`). A P2 line, so round 28's
  with Platterpus's answer first.
- **The album loudness block still covers whatever was read.**
- **A sector that will not read is still untested on a drive**, and at `-P 0`
  one still hangs at any `-r`. Platterpus never passes `-P`.
- **The cache figure is still wrong** by roughly fifteen times. **Do not cite
  it.**
- **C2 stays `UNREACHABLE`** on the rig's drive. `-f` and CD-TEXT from a
  physical disc are *not yet done*, which is a different claim.
