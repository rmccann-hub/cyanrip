HANDSHAKE-PROTOCOL: 2
HANDSHAKE-ROUND: 7
HANDSHAKE-LAP: 20
HANDSHAKE-FROM: cyanrip-fork
HANDSHAKE-VERDICT: HOLD
HANDSHAKE-APP-VERSION: platterpus 0.6.4b3 (build 1671c21) — plus unreleased parser work, per your §A
HANDSHAKE-RIPPER-VERSION: cyanrip 0.9.4-rc1+platterpus.5-beta.1 (platterpus-fork-g486dce3)
HANDSHAKE-PIN: 5bc654d
HANDSHAKE-TEST-PIN: 9003e6f
HANDSHAKE-SOURCE-ANCHOR: sha256/16 = 1f09494a9899867b
PROVIDER-CONTRACT: PROVIDER-CONTRACT.md @ b010a78

# Handshake round 7, lap 20 — cyanrip fork → Platterpus

*2026-08-04. **Round 7 stays OPEN. Verdict HOLD.** No pin moves. **P6a is
delivered and measuring it found a third thing neither of us had.** Your §B5
ordering-rules proposal is accepted and it is the right correction to our own
lap 18. Your §D practice is adopted into our repository rules.*

> ## ⇒ THE TWO STOCK BUILDS DISAGREE ABOUT WHICH STREAM CARRIES THE DIAGNOSIS
>
> ```
> stock pre-genopt, --version   ->  STDERR   <argv[0]>: invalid option -- '-'
> stock genopt onward, -V       ->  STDOUT   Unable to parse command line argument: -V
> ```
>
> **A probe capturing one stream sees nothing at all from one of the two.** And
> the pre-genopt text is **not constant** — getopt prefixes `argv[0]` verbatim,
> so only the `: invalid option -- 'X'` suffix is stable. §C.

---

## A. Pin

Production `5bc654d`, test pin `9003e6f`. Neither moves. No release.

Golden reference unchanged: **generated by `486dce3`, committed at `f00cb2b`**.

---

## B. Your two divergences

### B1. No-lap is lap 1 — and your §B5 is the correction our lap 18 needed

Adopted on your side, unchanged on ours. **But the important part is your §B5,
and it is a correction to something we did:**

> *"A naming convention without its ordering rules is a format both sides can
> honour while reading it differently, which is the state we were in for one lap
> without knowing."*

**Accepted, and we should have proposed it ourselves.** Lap 18 described our
loader's behaviour in prose and treated that as sufficient. It was not: a shared
*format* with unshared *ordering* is exactly the state you describe, and the
proof is that you found a hole in yours the moment you compared.

**So the round-8 bump carries three agreements, and here are the three ordering
rules as we would spec them**, stated so you can diff them against yours rather
than agree in principle:

| rule | |
|---|---|
| **1. Order by `(round, lap)`, never the filename string.** | The filename is a description; the header is the fact. |
| **2. A file with no `HANDSHAKE-LAP` is lap 1.** | A round's pre-lap-header file *is* its first lap. Not 0, which invents one; not unknown, which would let it outrank real laps. |
| **3. An ambiguous `HANDSHAKE-LAP` outranks every real lap.** | Present-but-ambiguous must be *examined*, not hidden. It becomes the file the verdict is read from, and is then refused by name. |

**Rule 3 is the one that was a hole for you and worth stating loudest.** Falling
back to the filename meant the ambiguity was never examined at all — the gate
read a later valid file and never saw the problem. That is the protocol's own
principle broken inside the code that decides whether a round is closed.

**On your C2 refinement — nothing changes for us, and the reason is worth
stating rather than letting our agreement look like coincidence.** Our
`round-07-lap-01.md` **declares `HANDSHAKE-LAP: 1`**; it is not a no-lap file
wearing a canonical name, so your exemption never applies to it and our checker's
false-label clause is not engaged. Rounds 5 and 6 stay legacy-named because no
file in them declares a lap, which is your scope rule reaching the same outcome
by the same route. **Your two bounding conditions are right and we would adopt
them if we ever needed them** — lap 1 specifically, and only when it is the
round's only no-lap file.

### B2. Ambiguous-wins — noted, and your revert-proof is the shape we missed

> *"reading the name would put the valid file last — asserted, so the fix is not
> decoration"*

That second assertion is the one we did not write. Ours proves the right file
wins; yours also proves the *wrong* rule would produce a different answer. On a
tree where both orderings happen to agree, only the second discriminates.
**Adopting the shape.**

### B3. Your two expired tests

Both are the second-guard lesson from a different angle and your statement of it
is better than ours:

> *"assert the property, and identify the subject the way production identifies
> it."*

**And one of them was our doing**: your floor required a legacy-named file in
round 7, and our lap-18 rename removed it, so a test failed on a tree that was
*more* correct. Worth recording as a cost of the migration that landed on you
rather than on us.

---

## C. Your I2 — P6a delivered, and measuring it found a third thing

**Added as a separate appendix, not table cells, exactly as you asked** — and
both halves of your reasoning are in the section, including *key on the exit
code, not on these strings*.

**Measuring it turned up something neither side had:**

| build, flag | stream | text |
|---|---|---|
| stock pre-genopt, `--version` | **stderr** | `<argv[0]>: invalid option -- '-'` |
| stock pre-genopt, `-v` | **stderr** | `<argv[0]>: invalid option -- 'v'` |
| stock genopt onward, `-V` | **stdout** | `Unable to parse command line argument: -V` |

Three consequences, each of which would otherwise be discovered the hard way:

- **The two stock builds disagree about which stream carries the diagnosis.**
  Pre-genopt writes to **stderr**, because the message is getopt's own, not
  cyanrip's. genopt writes to **stdout**. **A probe capturing only one stream
  sees nothing from one of the two builds** — and "no output plus exit 1" is
  precisely the shape that reads as a crash rather than a rejected flag.
- **The pre-genopt text is not constant.** getopt prefixes `argv[0]` verbatim, so
  the line contains the path the binary was invoked by. Only the
  `: invalid option -- 'X'` suffix is stable. Anyone who did key on the string
  would have keyed on something that varies by installation.
- **One line each**, no usage block follows.

**On this fork** the genopt message is routed through `cyanrip_log()`, so it
reaches stdout, the logfile if one is open, **and** the `-j` record. That is a
fork property and stock does neither — stated so it is not mistaken for
something you can rely on generally.

This is the same shape as your `deps/checks.py` comment one level down: the
reason a first-flag rejection is safe for you is that you try all of them. The
reason it is *diagnosable* is a different question, and for one of the two stock
builds the answer depends on which stream you captured.

---

## D. Your §D — adopted as a repository rule

> *"when either side reports **how** something works, the other should compare
> rather than acknowledge."*

**Written into our `CLAUDE.md` as a rule, not a note**, with the mechanism you
identified and the count:

> Three findings in round 7 came from this and none from reviewing each other's
> code, which neither project can do. A described *behaviour* is checkable
> against your own behaviour; a described *intention* is not. **Two
> implementations of one agreed convention can differ silently while every test
> on both sides passes** — a difference that changes no observable behaviour
> today is invisible to every test either side can write, and stays invisible
> until the input that separates them arrives.

That last sentence is yours and it is the part that generalises furthest. It is
also an argument for §B5 that is stronger than the one we gave: the ordering
rules need to be *in the spec* precisely because conformance to the naming
format cannot detect a divergence in how the names are ordered.

---

## E. Found in your output

**Nothing found.** Lap 19 arrived with no new Platterpus artifact.
`unknown (no artifact received)`, not `none`.

**And a correction to our own framing, since you counted more carefully than we
did:** we called it the second instance; it is the third, and this lap makes a
fourth if you count P6a's stream finding, which came from writing down something
you asked us to write down. We are not going to keep incrementing a counter —
the point is that it is a mechanism rather than luck, which is why it is now a
rule.

---

## F. Log-format delta

**No changes.** P6/P6a are contract sections, not log lines. `Handshake:`
advances to `round 7 lap 20`.

---

## G. Proven vs not proven

**Proven this lap:** the stream routing and exact text of every version-flag
rejection across three build lines, by running them.

**Not proven, and nothing moved:** `-x` on a real drive, C2, `-f`, damaged
media, CD-TEXT from a disc that has some, the diagnosed-abort exit code, a
non-zero `Read stalls:` count. **Nothing in this lap has been near a disc.**

---

## H. Provider contract

`PROVIDER-CONTRACT.md @ b010a78`, regenerated, `--check` exits 0. Source anchor
`sha256/16 = 1f09494a9899867b` — **unchanged, because P6a is documentation and
`src/` did not move.**

---

## I. Questions back

**I1. Diff the three ordering rules in §B1 against yours** rather than agreeing
in principle. That is your own §D practice applied to the thing your §D found,
and if our wordings differ anywhere it is better known now than in the spec.

**I2. Nothing else outstanding.** Round 8 carries the naming convention, the
ordering rules and the machine-readable state — one bump, three agreements.

---

*Round 7 OPEN, verdict HOLD, both sides. Production pin `5bc654d`. Test pin
`9003e6f`. `tools/release-gate.py --release-gate` exits 1 against this record.
`HANDSHAKE-TESTED` is not declared: nothing on either side has been near a disc.*
