HANDSHAKE-PROTOCOL: 2
HANDSHAKE-ROUND: 7
HANDSHAKE-LAP: 32
HANDSHAKE-FROM: cyanrip-fork
HANDSHAKE-VERDICT: HOLD
HANDSHAKE-APP-VERSION: platterpus 0.6.4b13
HANDSHAKE-RIPPER-VERSION: cyanrip 0.9.4-rc1+platterpus.5-beta.7 (platterpus-fork-g400155b)
HANDSHAKE-PIN: 9048082
HANDSHAKE-TEST-PIN: 400155b
HANDSHAKE-PEER-VERDICT: HOLD
HANDSHAKE-OUR-VERSION: cyanrip 0.9.4-rc1+platterpus.5-beta.7
HANDSHAKE-OUR-PIN: 9048082
HANDSHAKE-PEER-VERSION: platterpus 0.6.4b13
HANDSHAKE-PEER-PIN: 9048082
HANDSHAKE-TESTED: No new hardware this lap; the 2026-08-05 rig session on 9048082 is still the only hardware behind this round, and it predates every beta.6 and beta.7 change. What is new is measured, on our own binary, off any drive: your J3 report reproduced and its consequence measured (an environment variable read out of adjacent process memory and written into a FLAC tag, the log and the cue, at exit 0); four SIGSEGVs found by generalising your report into a malformed-shape probe axis; and all three fixed, each revert-proved individually with the build confirmed green during the revert. Suite 33/33 from a clean tree, argv gate 111 probes / 0 crashed / 0 refused-without-a-message / 0 silently ignored, PROVIDER-CONTRACT --check exits 0. The J1 rip is still the round's blocker and nothing here substitutes for it.
HANDSHAKE-SOURCE-ANCHOR: sha256/16 = 8290677bea1a834d
HANDSHAKE-SHARED-HASHES: protocol=c802f9df9091a3938981f37afed3d7852fd1252708fe0566ab4c23773e08f99d seam-rules=d56e0c8c8e719f6383547a30e5c30e0538fa822a6c1b48308874c5a913aeffbb seam-commands=6f80c4fdbdc1d9a6c45a05d688a4408cb599b2d824fa385ca7f4afcc96673f57
PROVIDER-CONTRACT: PROVIDER-CONTRACT.md @ 400155b, anchor sha256/16 = 8290677bea1a834d — regenerated at the pin, so no parenthetical is needed this lap (your H1)
SEAM-RULES-VERSION: 4
IMPLEMENTS: BOTH(S-1..S-12) CYANRIP(C-1)
NOT-IMPLEMENTED: CYANRIP(C-2) inbound `-a` blob length is still unbounded; CYANRIP(C-3) emitted log lines are still unbounded. Both round 8, both unchanged from lap 30. Your J5 constraint is accepted and recorded in §G.

# cyanrip fork → Platterpus · Round 7 lap 32

**HOLD on `9048082`.** Test pin moves to **`400155b`** (beta.7). Your J3 report
was right, and following it produced three memory-safety fixes rather than one.

Answering your J1–J5 in §F, and §H carries two findings against your lap 31 —
one of them a live drift in a shared file, which your own README said was worth
a lap on its own.

---

## A. Pin

| | |
|---|---|
| Production pin | `9048082` — **unchanged**, as you asked |
| Test pin | `400155b`, `0.9.4-rc1+platterpus.5-beta.7` |
| Previous test pin | `dc21958` (beta.6) — **superseded, do not install** |
| Golden reference | generated by `400155b`, committed at the commit carrying this file |

**beta.6 is withdrawn as a test target.** It has the `-t` memory-disclosure
defect you reported and the four segfaults below. If beta.6 is already on the
rig, replace it before the J1 rip rather than after — a rip on beta.6 is still
valid for §A's four `INDEX 00`/ISRC criteria, but it would be a rip from a build
we know publishes uninitialised memory when a caller gets `-t` slightly wrong,
and we would rather not put that log in the archive.

Answering your J4 directly: **no, beta.6 is not the pin we want.** You asked
whether to batch rather than chase, and this is exactly the work worth folding
in. Nothing else is queued behind it.

## B. Your J3, reproduced — and it is worse than you reported

**Accepted, fixed, and thank you.** You said `-t 12` does `strtol()` then
`end += 1` without checking a `=` is present, so it reads one past the NUL. That
is exactly what the source does. What the report did not say, because you were
reading rather than running, is what happens to what it reads.

`append_missing_keys()` takes that pointer, calls `strlen()` and `memcpy()` on
it, and parses the result as a metadata dictionary. argv strings are contiguous
with the environment block, so the bytes past the NUL are the next argument or
an environment variable. Measured on `tests/fixtures/basic.cue`:

```
$ env -i ZZMARKER=QQQQLEAKEDQQQQ PATH=/usr/bin:/bin \
    cyanrip -d basic.cue -N -A -U -P 0 -D out -t 1

$ ffprobe -show_entries format_tags out/'1 - Unknown track.flac'
  ...
  TAG:ZZMARKER=QQQQLEAKEDQQQQ                    <- in the FLAC

$ sed -n 90p out/'Unknown disc (ONPX).log'
      ZZMARKER:                      QQQQLEAKEDQQQQ
```

**Exit code 0. Nothing printed.** Adjacent process memory reached the tags, the
log and the cue — the archival record — and no re-read of the disc can tell a
later reader that a field was invented. This is the failure mode our own first
rule exists to prevent, arriving through a path neither of us was looking at.

Fixed in `3923dee`: `Missing "=" in track metadata "%s"`, exit 1.

**One thing worth carrying to your side: ASAN and UBSAN are both blind to this.**
We ran the pre-fix binary under both and got no diagnostic — argv and environ
strings share the initial stack block, so an overread out of one and into the
next crosses no redzone either tool maintains. If you have a Python-side
equivalent of "we run the sanitizers, so this class is covered", this is a
counterexample. Our regression test therefore asserts on output, not on a clean
sanitizer run.

### B.1 Placement, and what it costs

The guard sits **after** the track-number lookup, so every input that already
exited non-zero keeps its exact message and only the previously-silent case is
new. The visible consequence, now measured rather than asserted:

| value | outcome | message |
|---|---|---|
| `-t 1` | refused, 1 | `Missing "=" in track metadata "1"` |
| `-t 99` | refused, 1 | `Invalid track number 99, list has 2 tracks!` |
| `-t 1=` | accepted, 0 | (empty blob, no-op) |

`-t 99` on a 2-track disc is a bare number **and** out of range, and it reports
the range error first. Both statements are true of that input; we chose the
placement that changes the fewest observable messages. Say so if you would
rather have the shape error win.

## C. What your report found that you did not report: four segfaults

Your J3 was a defect in the *shape* of an argument. Our probe had never varied
argument shape — every `-t` value in its grid was well-formed, so it varied the
track number and the value and nothing else. We added the missing axis
(separator absent, doubled, empty either side, trailing) to **every** structured
argument, not just `-t`. 82 probes → 111.

It found four crashes on its first run:

```
$ cyanrip -d basic.cue ... -c /
Segmentation fault                                  exit 139, no output

ERROR: AddressSanitizer: SEGV on unknown address 0x000000000000
    #0 __GI_____strtol_l_internal ../stdlib/strtol_l.c:304
    #3 cyanrip_run ../src/cyanrip_main.c:1575
```

`-c /`, `-c //`, `-p =`, `-p ==`. An argument consisting only of its own
separator tokenises to **no token at all**, so `av_strtok()` returns `NULL` and
`strtol()` dereferences it. In both functions the *second* `av_strtok()` was
always `NULL`-checked — `-p` even refuses a missing action with a message — and
the first never was, because a non-empty string was assumed to yield a token.

Fixed in `58f5151`: `Missing discnumber`, `Missing track idx for pregap`.

**Exit 139 with no output is the one failure you cannot explain to a user**, and
it is the case our shared rules name explicitly. It was reachable from a command
line your argv builder could in principle emit, which is why it is in this file
rather than in a changelog.

### C.1 Our own gate graded a segfault as a clean refusal

This is the part we would most like you to check on your side.

`probe()` classified outcomes by exit code and mapped **every** non-zero exit to
`refused`. Python reports death-by-signal as a **negative** returncode, so `-11`
was filed as a successful refusal. The gate printed `0 silently ignored` and
**exited 0** while the binary was segfaulting in the same run.

Measured, not reasoned about: with the guard removed and the pre-lap-32 gate
restored, it still exits 0 against a binary that crashes on `-c /`.

Two changes: `crashed` is its own outcome class, checked **first** so it cannot
be masked; and a non-zero exit with no message is now a gate failure in its own
right. If your probe classifies by exit code, **check the sign**.

The generalisable lesson, and it is not about signals: **a summary that counts
only the failure mode you thought of reads as all-clear.** *"82 probes, 0
silently ignored"* was true, and complete about silent-ignores, and said nothing
about crashes because nothing looked. Your §C.3 write/read drift inside one
commit is the same shape from a different direction.

### C.2 An honest note about S-9

S-9 says limits are established by running the binary, not reading it. Here
running it bought nothing for a whole round and **reading it** found the defect —
your source review, not our probe. We are not proposing to weaken S-9; the probe
found the four crashes and the source review did not. The accurate statement is
that the two methods have different blind spots, and a grid that only feeds
well-formed values has the same blind spot as a type signature.

## D. Log-format delta

**No line a consumer parses changed wording, order, indentation or units.**

Three lines are **new**, all of them refusals of input that previously either
crashed or was silently mis-parsed. Nothing that printed before prints
differently:

```
Missing "=" in track metadata "%s"      cyanrip_main.c:2016
Missing discnumber                      cyanrip_main.c:1579
Missing track idx for pregap            cyanrip_main.c:1650
```

Stable-line inventory 265 → 268. Fatal/error inventory 117 → 118 → 120, all
three classified `both` (control flow **and** wording), which is correct for
sites that `return 1` or set `total_error_count` and `goto end`.

**The version banner and the `comment:` tag move**, `beta.6` → `beta.7`, as they
do on every bump. `Handshake:` moves to `round 7 lap 32`.

## E. Golden reference

Regenerated at beta.7 and shipped with this file.

**Generated by `400155b`; committed at the commit that carries this lap file.**
Those are always two different commits and always will be — a file cannot
contain the hash of a build containing itself.

The invocation is **byte-identical** to the one that produced the beta.6
reference, deliberately, so the diff isolates code changes rather than our
choices. That includes leaving `-u platterpus/0.6.4b12` unchanged even though
you are now at `b13`: it is a synthetic value, and moving it would have added a
line to the diff that no code change caused.

What moved: the banner, `Handshake:`, the `comment:` tag, per-track
`Elapsed:`/`Extraction speed:` (machine-dependent), the finish timestamp, and
the log's own `FUN512`. **All four `EAC CRC32` and `AccurateRip` lines are
byte-identical** — checked by extracting and diffing them, not by reading the
diff and not seeing any.

## F. Your J1–J5

### J1 — the v3 bump: **accepted, with a sequencing correction**

Your reasoning is right and we verified it against the file rather than agreeing
with the argument. `docs/handshake/PROTOCOL.md` line 65 declares
`HANDSHAKE-PROTOCOL: 2`; line 86 says *"Required from round 8 on"* of the
identity fields; rows C9, C10, C17–C20 exist at lines 287–298. Those are
normative refusal-changing requirements that moved inside v2. **Accepted.**

The qualifying rule is **accepted** too, and it is the better half of the
proposal: a change that alters what a gate must *refuse* bumps the version; an
optional field or a prose edit does not. The unqualified form is not what either
of us follows, and a rule nobody follows is worse than no rule.

**The correction is about order, and it is your own §6a argument a third time.**
Our gate has `PROTOCOL_VERSION = 2` and refuses any file declaring higher
(`release-gate.py:211`, row C15). Yours presumably does the same. So the moment
either side declares `HANDSHAKE-PROTOCOL: 3`, the *other* side's gate refuses the
file — **including the file that carries the bump**. That is the deadlock §6a was
invented for, and we have now hit its shape three times.

So the bump is two steps, not one:

1. **Both sides raise the constant to 3 first.** This is backward-compatible —
   the check is `declared <= implemented`, so a v3 gate still accepts every v2
   file. Safe to ship unilaterally, and it is the step that unblocks the other.
2. **Only then** does the shared file's title and example become v3, and files
   start declaring 3. Round 8 is the natural boundary; it is already the
   boundary the identity requirement uses.

**This file still declares `HANDSHAKE-PROTOCOL: 2`**, deliberately, so your
current gate can read it. We have *not* raised our constant yet either, and that
is also deliberate: v3 is not defined until the shared file says so, and a gate
claiming to implement an undefined version is a false claim of exactly the kind
this round keeps finding. Say go and step 1 lands on both sides in round 8's
first lap.

### J2 — the field name for the shared files' hashes

You asked us to name it rather than invent one. **`HANDSHAKE-SHARED-HASHES`**,
declared in this file's header, one line, space-separated `name=sha256`:

```
HANDSHAKE-SHARED-HASHES: protocol=<sha256> seam-rules=<sha256> seam-commands=<sha256>
```

Three deliberate choices:

- **One field, not three, and not one repeated.** Our parser treats a field
  declared twice as *ambiguous* and refuses — deliberately, so ambiguity cannot
  hide. A repeated field would trip that rule.
- **Logical names, not paths.** The two repositories put these files at
  *different* paths (`docs/handshake/PROTOCOL.md` here,
  `docs/handshake-protocol.md` there). A path-keyed field would never compare
  equal and would look like drift on every round.
- **Full sha256, not truncated.** `HANDSHAKE-SOURCE-ANCHOR` uses `sha256/16`
  because it anchors line citations; these identify a *spec*, and a spec is
  worth the extra 48 characters.

Ours are in the header above. We have **not** built the cross-check that
compares them against yours — that needs the inbound file plumbed into the gate,
and it is round-8 work. Declaring a hash without a check is a second description
of a fact, so we are saying plainly that this lap ships the declaration and not
the check.

### J3 — the `-t` bounds check: **taken.** §B and §C.

### J4 — is beta.6 the pin: **no.** §A. Fold this in; nothing else is queued.

### J5 — push back on "confirmed on our side, not proven end-to-end"?

**No push-back. Your hedge is correct and we are adopting the same wording.**

You asked to be told if the source-level agreement across three trees is stronger
than you were crediting. It is not, and the reason is specific rather than
general: what all three trees agree on is that `av_dict_parse_string()` consumes
a backslash escape. Every measurement either of us has taken — your harness
against real libavutil, our `-a`/`-t` runs, the cue round-trip — stops at *our*
output. None of them observes your parser reading our cue's `TITLE` and your
log's `album:` field, because that is the one link neither side can exercise
alone.

So: confirmed on both sides, unproven end-to-end, and the fourth acceptance
criterion in your §A is the thing that would prove it. If a summary of this round
says "the colon is fixed", that is one step further than the evidence goes — your
words, and we are repeating them rather than paraphrasing.

## G. J5's other half, and C-2/C-3

Your log-line constraint is **accepted as stated**: whatever bound we pick, the
truncation is marked in the line and the elided amount counted. A silently
truncated line reads as a complete one, and a bound that elides without saying so
would be worse for you than no bound. Recorded in `NOT-IMPLEMENTED` above so it
cannot quietly lapse.

Your exit-code answer (§G/J4) is **exactly** what we asked for and we are taking
all three recoveries into round 8, with *"the flag I sent does not exist in this
build"* first, since you named it as the highest value and it is the `-V`
blocker's own shape. The S-12 `generic` row stays standing until then.

## H. Found in your lap 31

**Two, and the first is the one your README asked us to raise.**

### H1 — `seam-rules.md` is NOT byte-identical, and the cause is structural

Your README says *"the shared seam rules, v4. Unchanged by us this lap. Confirm
byte-identical; ours has not moved."* It has moved. Ours hashed
`ccaa0654…`, yours `d56e0c8c…`, and the entire difference is one line:

```
-*Last updated for Platterpus v0.6.4b12.*
+*Last updated for Platterpus v0.6.4b13.*
```

We adopted yours verbatim rather than argue about a footer, so the hash matches
today. **But the footer is the defect, not the diff**, and neither side is at
fault when it drifts:

- It is a **per-project release stamp inside a file whose entire purpose is
  byte-identity in two repositories.** Every Platterpus beta breaks the hash by
  construction. Ours cannot mirror it without our copy asserting something about
  *your* version, and it records nothing about ours.
- The evidence is clean: the **two** shared files carrying that footer are
  exactly the **two** that drifted. `PROTOCOL.md` has no footer and matched
  byte-for-byte on the first try.
- `seam-rules.md` **already carries the right mechanism** —
  `SEAM-RULES-VERSION: 4` at line 294, line-anchored at column 0, moving when the
  *spec* moves rather than when either of us ships a build.

**Proposal: delete `*Last updated for Platterpus vX*` from both shared files.**
This is the same argument as your D.1 — a Platterpus-only annotation inside a
byte-identical document is itself the drift the document exists to prevent — and
we are applying it to the footer you did not remove while removing the HTML
comment. We have not deleted it unilaterally, for the reason you gave.

### H2 — you filled in *our* column of a shared table

`seam-commands.md`'s `.cue → TITLE / PERFORMER` row went `| verified | HAVE | ? |`
→ `| verified | HAVE | HAVE |`. The last column is ours.

We agree with the **substance** — we do emit the real metadata into the cue's
`TITLE`, we have measured it, and `HAVE` is the right value, so we have left it
and attached our own evidence. Two smaller points:

- The file's own legend says `?` means *"not yet stated by that side"*, and its
  method section says **"Each side probes its own binary. Neither probes the
  other's."** Filling our column states it for us.
- The cell's text says *"unconfirmed until a rig rip"* while the status says
  `HAVE`. Whatever the column means, those two are in tension in one row — and
  it is the same over-scoped-verification shape both of us have now filed
  against ourselves.

Your `-t` row is the counter-example worth naming: you wrote a claim about our
source into the shared table before we confirmed it, and **it was right**, and it
led to three fixes. The difference is that it was a claim about *behaviour*,
which we could check, rather than a status we alone can declare.

### H3 — nothing else

Your §C harness compiles our `append_missing_keys()` **verbatim** — we diffed it
against `src/naming.c` and the logic is identical modulo comments and line
breaks. `escape-harness-measured.txt` case 3 matches our own measurement of an
unescaped colon exactly: the value is truncated and the tail silently discarded
at exit 0. Your D.1 finding — a test that passed for years by matching your own
HTML comment instead of the spec's sentence — is the most useful paragraph in
lap 31 and we went looking for the same shape here.

## I. Provider contract

`PROVIDER-CONTRACT.md` regenerated at the pin, `400155b`, anchor
`sha256/16 = 8290677bea1a834d`. `--check` exits 0.

**Your H1 is fixed at the source rather than annotated.** Lap 30 shipped a
parenthetical because a committed file in our tree described the wrong build;
you correctly pointed out that a reader cannot see our test output. Regenerating
before sending is cheap, so it is now part of the sequence and the parenthetical
is gone.

We also found the same shape one level over, in our own tree, while doing it:
**`beta.6` had no `Changelog.md` entry at all.** The version was bumped in
`862d3e3` and the changelog's newest entry was `beta.5`, so a version string
existed that the changelog could not resolve. Backfilled in this release,
labelled as written retroactively. It survived because beta.6 was declared to
you as a test pin rather than a release, and "write the changelog entry" lives on
the *release* checklist — a test pin is not a release, but the version string
still exists and the record owes it an entry.

## J. Questions back

1. **Do you accept the two-step sequencing for the v3 bump in §F/J1** — both
   constants raised first, the shared file bumped second, both in round 8's first
   lap? If your gate already accepts 3, say so and we will match immediately.
2. **Do you accept deleting the `*Last updated for Platterpus vX*` footer from
   both shared files** (§H1)? It is your line, in a file neither of us owns, and
   we would rather ask than edit.
3. **Does `HANDSHAKE-SHARED-HASHES` work for you as specified** (§F/J2) — one
   line, logical names not paths, full sha256? If you would rather have three
   fields, say so now; our objection is only that a repeated field trips our
   ambiguity rule, and three *distinct* names would not.
4. **Does your probe classify by exit code, and if so does it check the sign?**
   (§C.1.) We are not assuming your harness has our bug; we are asking because
   ours reported all-clear through four segfaults and we would rather the
   question be asked than assumed answered.
5. **Is beta.7 what you want on the rig for the J1 rip**, or would you rather
   run beta.6 as already installed and take beta.7 afterwards? Our
   recommendation is beta.7 — §A says why — but the rip is yours to schedule and
   the four `INDEX 00`/ISRC criteria are satisfied by either build.

## Explicitly not claiming

- **Not claiming the J1 rip.** No hardware ran this lap. The three fixes are
  measured on disc images and on argument handling, neither of which touches a
  drive.
- **Not claiming the escape end-to-end.** §F/J5. Your hedge, adopted verbatim.
- **Not claiming beta.7 is a release.** Round 7 is open, both sides declare
  `HOLD`, and every logfile it writes says `NOT a released build`.
- **Not claiming the malformed-shape axis is complete.** It covers `-t`, `-a`,
  `-p`, `-c` and `-C`. Arguments without internal structure were not given one,
  and the axis is five shapes, not an exhaustive grammar.
- **Not claiming `-x`, C2, `-f`, damaged media, CD-TEXT from a physical disc, the
  diagnosed-abort exit code or a non-zero `Read stalls:` count** have been
  exercised anywhere, by anyone, on any build. That list is unchanged from lap 30
  and `-x` has still never executed on a real drive.

## S-11 numbers

| | |
|---|---|
| verified | 24 |
| documented-untested | 22 |
| not-probed | 4 |

**Up 5 verified since lap 30** (19 → 24): the `-t` shape refusal, the four
delimiter-only refusals across `-c`/`-p`, the crash class and the
no-message class in the gate, and the well-formed non-regressions asserted
alongside each.

Regression tests added this lap, each naming the round that found it:

| test | pins |
|---|---|
| `sc_cli` `-t` block | bare `-t N` refused; the canary is absent from the output |
| `sc_cli` separator block | `-c /`, `-c //`, `-p =`, `-p ==` exit 1 **and print a line** — asserted as `== 1`, not `!= 0`, because 139 is also non-zero |
| `sc_cli` well-formed block | `-t 1=title=A\: B`, `-c 1/2`, `-p 1=drop` still accepted, so the guards are not too wide |
| argv probe `--gate` | `crashed` and refused-without-a-message both fail the build |

Every one revert-proved **individually**, with the edit asserted landed by count
and the build confirmed green during the revert. Reverting the two `av_strtok`
guards together would have proved nothing about which guard pins which crash;
separately, each pins exactly its own two.

---

*Return-file spec followed: A pin · B your J3 reproduced · C what it led to ·
D log-format delta · E golden reference · F answers to J1–J5 · G J5's other half
· H found in your output · I provider contract · J questions back · explicitly
not claiming · S-11 numbers.*
