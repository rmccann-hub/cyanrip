INFO  platterpus/version  0.6.25 (build 5f374aa)
INFO  ripper/version  cyanrip 0.9.4-rc2+platterpus.10 (platterpus-fork-gd9c058c)  [ripper-version.txt]
INFO  ripper/handshake  unapproved — ripper build platterpus-fork-gd9c058c is NOT the build this Platterpus was verified against (platterpus-fork-gddf7ac3, approved by handshake round 8). The rip is still bit-perfect if its own checks passed — but it was not produced by a jointly-verified ripper. That build is the pin an OPEN handshake round proposes (d9c058c); it has not been approved by either project yet.
INFO  ripper/pin  this Platterpus pins ddf7ac3; the installed banner is above. A test pin is expected to differ during an open round.
OK    argv/integrity  all 15 flag token(s) we composed arrived intact (-F -G -N -T -Z -a -c -d -j -l -o -r -s -t) in the binary's own record of the 26 args we sent. Flag tokens are compared, not values: a value may be re-quoted in their rendering, a flag may not change  [argv-probe-output.txt]
INFO  handshake/note  OPEN: Handshake:      round 14 lap 3 OPEN, verdict HOLD -- NOT a released build
INFO  parser/paranoia  per-track counters sum to 2567; the disc block totals 2567. Scope: line present on 0 of 2 track(s) — secure re-read genuinely exercised: no
INFO  parser/interrupted  no 'Interrupted at:' line in this log (rip_completed=True) — expected for a rip that ran to the end; only a cancelled or killed rip produces one
OK    parser/log  parsed 2 track(s) from derived wav platterpus-fork-gd9c058c.log
INFO  parser/cache-probe  no Cache probe: line in this log, and there never will be one — `-x` is not in the rip argv builder at all, so no Platterpus rip probes the cache. The probe is a separate `cyanrip -N -x -I` invocation (round 14 T3), whose exact argv, exit code and complete output are recorded in the SCRIPT REPORT and transcript, not here. Look there, not for an absence in this manifest.
SKIP  device  no --device given
