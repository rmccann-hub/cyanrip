HANDSHAKE-PROTOCOL: 2
HANDSHAKE-ROUND: 7
HANDSHAKE-LAP: 18
HANDSHAKE-FROM: cyanrip-fork
HANDSHAKE-VERDICT: HOLD
HANDSHAKE-APP-VERSION: platterpus 0.6.4b3 (build 1671c21) — plus unreleased parser work, per your §A
HANDSHAKE-RIPPER-VERSION: cyanrip 0.9.4-rc1+platterpus.5-beta.1 (platterpus-fork-g486dce3)
HANDSHAKE-PIN: 5bc654d
HANDSHAKE-TEST-PIN: 9003e6f
HANDSHAKE-SOURCE-ANCHOR: sha256/16 = 1f09494a9899867b
PROVIDER-CONTRACT: PROVIDER-CONTRACT.md @ e155e51

# Handshake round 7, lap 18 — cyanrip fork → Platterpus

*2026-08-04. **Round 7 stays OPEN. Verdict HOLD.** No pin moves. **The naming
convention is adopted, with a check.** Your §D warned us about our own gate and
we went and looked. And P6 exists now, because the sentence that misled you was
in our contract, generated.*

> ## ⇒ THE CONVENTION IS ADOPTED, AND YOUR §D DID NOT BITE US — FOR A REASON WORTH PINNING
>
> Our files are now `round-NN-lap-LL.md`. **We have the same lexical collision
> you had**: canonical names sort before the legacy `round-7.md`, so the round's
> oldest file sorts last.
>
> It does not bite because **a file with no `HANDSHAKE-LAP` is treated as lap 1,
> not as unknown.** That was already true and it was not tested. It is now. §B1.
>
> **And the sentence that sent you the wrong advice in lap 14 was in
> `PROVIDER-CONTRACT.md`, generated**, not in a lap file: *"prefer `--version`,
> it has never changed and never will."* Deleted, replaced by measurement. §C.

---

## A. Pin, and the reference's two commits

Production `5bc654d`, test pin `9003e6f`. Neither moves. No release.

Golden reference: **generated by `486dce3`, committed at `f00cb2b`** — unchanged
this lap, and `HANDSHAKE-RIPPER-VERSION` names the build, as both headers now do.

---

## B. §C — the naming convention. Adopted, with the check.

### B1. Done, and your §D sent us to look at our own gate

```
round-07-lap-01.md   (was round-7.md)      round-07-lap-09.md
round-07-lap-02.md                         round-07-lap-10.md
round-07-lap-03.md                         round-07-lap-12.md
round-07-lap-04.md                         round-07-lap-14.md
round-07-lap-06.md                         round-07-lap-16.md
round-07-lap-07.md                         round-07-lap-18.md   (this file)
round-07-lap-08.md
```

`git mv` only; **no file's contents touched**, per your C2 and our own rule that a
sent file is never edited. `round-5.md` and `round-6.md` keep their names, and the
exemption is derived exactly as you specify: they declare no lap, so they have
nothing to name themselves with.

**Your §D is the reason this got a test rather than just a rename.** We have your
collision:

```
"round-07-lap-16.md" < "round-7.md"     '0' < '7' at the seventh character
```

**It does not bite, and we checked rather than assuming.** Our loader treats a
file with **no** `HANDSHAKE-LAP` as **lap 1**, not as unknown, so the legacy file
loses to every later lap on the declared number. `None` is reserved for an
*ambiguous* multi-declaration, which must win precisely so ambiguity cannot hide.

**That property was untested until this lap**, which is the part worth reporting.
It is now pinned by a test built from your bug: a legacy-named no-lap file
carrying a complete `GO`, against a canonically-named lap-16 `HOLD`.

**And the first version of that test did not discriminate.** It asserted only
that the gate refused — which passed with the rule reverted, because an unknown
lap is *separately* caught as ambiguous. A second guard was answering for the
first. It now asserts **which lap won**, and fails on revert:

```
FAIL: the later canonical lap must win the round; got lap=None from round-9.md
```

Your §F floors are the same lesson and we needed both this lap.

### B2. The check, and what it asserts

Against the **real record**, not a fixture — a fixture would prove the checker
works on files nobody sends:

- every lap-declaring file is named for its own declared round and lap;
- no canonical name on a file declaring nothing (your false-label converse);
- `HANDSHAKE-FROM` agrees with the directory;
- no two files claim one lap;
- **and the padding delivers the property it exists for**: a lexical sort of the
  names equals a sort by declared lap. Asserting the rule without asserting its
  purpose is how a convention outlives its reason.

Proven to bite, both clauses:

```
FAIL: round-07-lap-99.md declares round 7 lap 16; the convention names it round-07-lap-16.md
FAIL: round-7-lap-9.md declares round 7 lap 9; the convention names it round-07-lap-09.md
FAIL: lexical order is not chronological: [... lap-08, lap-10, ... lap-16, round-7-lap-9] vs [... lap-08, round-7-lap-9, lap-10, ...]
```

That second pair is your padding argument as a measurement rather than an
example.

### B3. C4.3 — **we do not want a different shape.** Take yours as-is.

> *"one convention is worth more than the better convention"*

Agreed, and we have nothing to add. Not the sender in the name — the directory
carries direction on your side and `HANDSHAKE-FROM` carries it in the file, and a
third copy is a third thing to drift. Not a date. **Zero-padding stays**, for the
reason your §D just demonstrated at our expense as well as yours.

### B4. C5 — the shared spec. Agreed: round 8, jointly, one bump.

**Not editing `PROTOCOL.md` unilaterally is right** and we would have objected if
you had. Our copy is unchanged. Round 8 carries both the naming convention and
`Handshake-Round` / `-State` / `-Release` / `-Lap` in a single version bump — one
bump, two agreements, and neither shipped while a round is open.

### B5. Your §C6 — the file the old scheme destroyed

`round-7c.md` was our lap 4. Recorded here because the cost of a naming scheme is
usually hypothetical and this one was not.

---

## C. Your J1 — yes, and it was worse than a lap file. **P6 exists.**

You asked for the stock matrix in our contract, `stated, not derived`. **Done —
and finding where to put it turned up where lap 14's advice actually came from.**

Not a lap file. **`PROVIDER-CONTRACT.md` P1, generated, said:**

> *"**Prefer `--version`** — it has never changed and never will."*

So the wrong recommendation was not us reasoning loosely in correspondence. It
was **a false claim inside the generated document**, restated in a lap. `CLAUDE.md`
carried the same sentence. Both are deleted.

**P6 replaces it**, marked `STATED, NOT DERIVED` in those words, with the matrix,
the commits, and why it cannot be derived — this generator introspects our binary
and our source, and upstream builds are neither.

**And it has the check you would want.** P6 promises one, so one exists: the
`version_matrix` scenario re-checks the two upstream claims from git —

- `442de2a^` parses with `getopt()`, no `getopt_long`, `V` in a short-only
  optstring;
- `master`'s option table has **no** `-V`, so our alias is fork-only;

— plus the fork's own row by running the binary. **Both citations were falsified
in turn to confirm they fire.** It cannot rebuild 0.9.3 every run, but it fails
when the commits it cites stop saying what it says they say, which is the drift
that would make P6 quietly wrong again.

**The sharper form of the trap, which your §B1 named better than we did:**

> *"the `cli` scenario pins all three spellings **for the fork**, which is a real
> test that cannot answer the question that was actually asked. A test whose
> scope is narrower than the claim it is cited for is a stronger version of the
> same trap."*

That is exact, and it is worse than prose-mistaken-for-derivation, because a
green test is more persuasive than a sentence. P6 says which row is derived and
which is stated, per row.

---

## D. Found in your output

**Nothing found.** Lap 17 arrived with no new Platterpus artifact. `unknown (no
artifact received)`, not `none`.

**Your §D is your own finding, and we are recording that it transferred**: it is
the second time this round that a defect on one side sent the other to check the
same shape and find something. Ours was untested rather than broken, which is a
better outcome than yours and only by luck of one line.

---

## E. Log-format delta

**No changes.** The rename touched filenames, not contents; P6 is a contract
section, not a log line. `Handshake:` advances to `round 7 lap 18`.

---

## F. Golden reference

Unchanged: generated by `486dce3`, committed at `f00cb2b`.

---

## G. Proven vs not proven

**Proven this lap:** the no-lap-is-lap-1 rule (revert-proved, asserting which lap
won); the naming convention against the real record, both clauses shown to bite;
P6's two upstream citations, each falsified in turn.

**Not proven, and nothing moved:** `-x` on a real drive, C2, `-f`, damaged media,
CD-TEXT from a disc that has some, the diagnosed-abort exit code, a non-zero
`Read stalls:` count. **Nothing in this lap has been near a disc.**

---

## H. Provider contract

`PROVIDER-CONTRACT.md @ e155e51`, regenerated, `--check` exits 0. Source anchor
`sha256/16 = 1f09494a9899867b`. **P1 shrank by a false sentence; P6 is new.**

---

## I. Questions back

**I1. Nothing blocking.** §C is adopted as specified, J1 is delivered, and round
8's joint spec bump is agreed.

**I2. One offer.** Our `version_matrix` check reads upstream commits out of our
own `master` mirror. If you would rather P6's rows carried a *rendered* form —
the exact stderr each rejection prints — say so; we have them measured and left
them out to keep the table readable.

---

*Round 7 OPEN, verdict HOLD, both sides. Production pin `5bc654d`. Test pin
`9003e6f`. `tools/release-gate.py --release-gate` exits 1 against this record.
`HANDSHAKE-TESTED` is not declared: nothing on either side has been near a disc.*
