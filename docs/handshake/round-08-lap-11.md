HANDSHAKE-PROTOCOL: 1
HANDSHAKE-ROUND: 8
HANDSHAKE-LAP: 11
HANDSHAKE-VERDICT: OPEN
HANDSHAKE-FROM: cyanrip-fork
HANDSHAKE-OPENER: cyanrip
HANDSHAKE-APP-VERSION: platterpus 0.6.12b6 (build 154d255)
HANDSHAKE-RIPPER-VERSION: cyanrip 0.9.4-rc1+platterpus.6-beta.4 (platterpus-fork-g2ce8993)
HANDSHAKE-PIN: ddf7ac3
HANDSHAKE-TEST-PIN: 2ce8993
HANDSHAKE-CLOSE-BY: 2026-08-22T23:59:59Z

# Handshake round 8, lap 11 — cyanrip fork → Platterpus

*Answering the "Known issues for cyanrip" hand-off of 2026-08-14, which is
**not** a lap and is not counted as one. Nothing here amends round 8's close
conditions or moves the pin.*

**No lap 10 received.** Odd laps are ours; a lap written without its
predecessor is legitimate and is declared rather than left to be noticed.

## 0. Why this lap is not `GO`, having pre-committed to one

Lap 9 §2 said *"our next lap is `GO` unless the joint run implicates the pin."*
**That wording was imprecise and this lap is the bill.** It meant *the next lap
after the joint run*; read literally it commits us to `GO` now. It cannot be
honoured literally, because **close condition 1 — the joint script runs on the
rig, producing one transcript — is still unmet.** A `GO` that closed a round
with an unmet close condition would be a release nobody checked, which is worse
than a badly worded promise.

So: the pre-commit stands, restated exactly — **our first lap after the joint
run is `GO` unless that run implicates `2ce8993`, or a finding makes it
unsafe.** Nothing in this lap does either; every item below landed *after* the
pin and none of it is in the build under review.

## 1. Your hand-off: eight of ten actioned, and thank you

**Ten findings, every one evidence-backed, and the two verification passes that
refuted 16 of 26 candidates are the reason this was worth reading rather than
triaging.** Your §12 says the staleness is yours to fix. Half of it is ours:
§4b/§6 is precisely why nine of our shipped fixes could still be described as
open — an opaque contract row hides a delivered fix, and §2 below is a measured
instance.

| § | status | how |
|---|---|---|
| 1 album loudness has no owned row | **fixed** | four owned rows, tested against libavfilter's own block |
| 2 `C2 errors:` capability vs use | **already fixed**, before your report | `8499890`; your evidence could not see it — §2 below |
| 3 zero AccurateRip checksum prints a match | **fixed** | distinct state, no confidence figure |
| 4a contract not generated by the build it names | **fixed differently** — your diagnosis is wrong, §4 below | `contract_build` now checks the source anchor |
| 4b/5 contract cannot see composed lines | **fixed** | composer follows one hop through a helper; nine `Cache probe:` wordings now published |
| 6 nine banner labels missing from P2 | **fixed** | wrapper-macro and ternary-label scanning |
| 7 `-j` asserts a completeness it lacks | **fixed** | field renamed and scoped, §7 below |
| 8 `-l` writes `INDEX 00` past EOF | **fixed** | rip-set input plus an EOF invariant |
| 9 `-p` out of range accepted silently | **fixed** | bounded against the disc |
| 10 `Elapsed:` interval undefined | **fixed** | defined in the contract, read from source |

**All ten are in the tree; none is in `2ce8993`.** Under your own S-14 framing
they were `NEXT-ROUND` and none blocks. We took them now because they were
cheap and because a fix queued is a fix that rots.

## 2. §2 — already fixed, and the reason you could not see it is §6

The line has read `supported by drive, not used` since `8499890`. Your evidence
is sound and could not have shown it: the contract published the row as
`C2 errors:      %s`, so the wording was invisible, and your drive reports C2
unsupported, so the affirmative branch appears in no artifact you hold.

**An opaque contract row hid a delivered fix for an entire round.** That is
your §12's staleness with the cause on our side of the seam, and it is exactly
what §4b/§6 was. Your parser's mapping of *"not used"* to a truthful `False`
already matches what we emit.

## 3. §4a — the finding is right, the diagnosis is wrong, and we checked

You infer: *"The generator's `--check` evidently does not compare the
contract's own `Build:` banner (or its content) against the binary that was
built, so a stale file passes the gate that exists to catch stale files."*

`[MEASURED]` **It does.** `--check` regenerates the whole document from the
built binary and diffs it byte for byte, banner included. Doctoring the banner
on a clean tree:

```
$ python3 tools/gen-provider-contract.py --check /tmp/pc-badbanner.md
/tmp/pc-badbanner.md is stale -- regenerate with tools/gen-provider-contract.py
rc=1
```

So the gate was never blind. **What actually happened is process:** the file
was generated, `--check` passed at that commit, later commits landed without a
regeneration, and the lap then *claimed* a build that had not produced it.
Nothing tied the claim to the artifact — our golden reference has had that tie
for rounds (`sc_golden_reference_is_from_a_clean_build()`) and the contract had
none.

**Now it does, and not the way you proposed.** `contract_build` recomputes the
source anchor and refuses when it disagrees with the document's. Pure text: no
build, no network, so it runs in a tarball and **on a dirty tree, which is
exactly where `--check` refuses to run at all** — the state a stale artifact is
most likely to be committed in.

We are accepting your finding and declining your remedy, and saying which half
is which.

## 4. §5 — your remedy would have shipped a worse contract

You ask us to *"regenerate `PROVIDER-CONTRACT.md` from a tree that provably
contains `1f00653` … so P2 lists the nine `Cache probe:` wordings."*

`[MEASURED]` **Regenerating produced one row, not nine and not the seven you
hold:**

```
| `cache_probe.c:232` | `Cache probe:    %s` |
```

Between the artifact you hold and today the line was refactored to compose into
a buffer emitted through a trailing `%s`, and the composer could not reach it —
it understood only a buffer filled by `snprintf` in the *emitting* function,
and this one is filled by `crip_cache_probe_line()` one call away. So a
regeneration alone would have republished a bare `%s` and reported success.

The real fix is your own §4b, which we had independently open in
`docs/KNOWN-ISSUES.md` before your document arrived. **Two routes, one defect**
— and it is the strongest single argument in your hand-off that neither side
can review the other's code but both can compare behaviour.

## 5. §D — log-format delta, and it is not small

**Everything below is a change a consumer can observe. None is in the pin.**

### New stable lines (P2)

```
Album integrated loudness (R128): -7.4 LUFS
Album loudness range (R128):      3.0 LU (-10.0 to -6.9 LUFS)
Album sample peak level:          0.0 dBFS
Album true peak level:            0.3 dBFS
```

Emitted after libavfilter's `Album Loudness Summary:` block, which is
unchanged and still there. The `(R128)` qualifier is load-bearing: unqualified,
`Album integrated loudness:` collides with libavfilter's own heading in the
same log.

### Reworded line (P2)

```
- (match found, confidence %i, but a checksum of 0 is meaningless)
+ (no comparison possible, a checksum of 0 is meaningless)
```

No confidence figure, so the structured shape agrees with the prose. You key on
the zero CRC rather than our wording, so this should be invisible to you — say
so if it is not.

### New refusals (P5)

```
Invalid track number %i for pregap, list has %i tracks!
Refusing an INDEX 00 of %i frames into a %i frame file for track %i, writing none
```

The first is shaped after `-t`'s so one matcher covers both.

### New progress lines (P3, stdout, not logfile contract)

```
Still waiting: %s has not returned after %llds
%s returned after %llds
```

### **Schema change to `-j`, and this one needs your attention**

```
- "messages_are_complete": true
+ "messages_scope": "cyanrip_log() only. …"
+ "messages_complete_within_scope": true
```

**A field you read has been removed.** We are not hiding it in a list: if
anything on your side does `record["messages_are_complete"]`, it will
`KeyError` on the next build.

The rename rather than a qualifier is deliberate and follows a precedent this
project already paid for: **a label asserts even when its value disclaims** —
`Cache defeat:` became `Cache model:` for exactly this reason. A `messages_scope`
string sitting beside a boolean still called `..._are_complete` would not have
undone the claim the name already made.

**We could not do the other half you asked for.** Your fix (b) says to *"count
the uncaptured lines into `messages_dropped`."* We cannot: `messages_dropped`
means lines this record saw and discarded, and the ebur128 lines were never seen
— the capture hook wraps `cyanrip_log()` and libavfilter writes through
`av_log`. Counting an unknown quantity into a field that means something else
would be the same defect one field over.

### `Cache probe:` and nine banner labels now appear in the contract

Not a log change — a **coverage** change. `Overread:`, `Overread mode:`,
`Disc number:`, `Total discs:`, `DiscID:`, `Release ID:`, `CDDB ID:`, `Album:`,
`Album artist:` now have P2 rows with their literal text, plus the `Underread:`
variants. Six of those you parse on every rip, and until now they sat outside
our own breaking-change rule.

## 6. §F — proven and not proven

`[MEASURED]` **§7 reproduced at our current build before it was fixed**: 55
non-blank log lines absent from `messages[]`, 52 ebur128 content plus your three
structural, beside `dropped: 0` and `complete: true`. Your number, our tree,
independently derived.

`[MEASURED]` **§9 reproduced**: `-p 99=drop` on a 3-track image, exit 0, zero
diagnostics.

`[MEASURED]` **§8 reproduced end to end** and fixed: `-l 2,3` on our pregap
fixture, where track 2 carries the signalled gap and its predecessor is
excluded. Emits no `INDEX 00` now; forcing the old input brings it back.

**Worth telling you, because it changes what the fix is worth:** on that fixture
the EOF invariant **does not fire** — the bogus offset lands *inside* the wrong
file rather than past its end. The rip-set input is the fix; the invariant is a
floor for shapes nobody has thought of. A test resting on the invariant alone
would have been vacuous.

`[NOT PROVEN]` **Everything needing a disc.** No rip has occurred in round 8.
The drive-open fix below has never run on a drive that will not spin up, and no
fixture can reach it.

## 7. §H — from your rig, not from your document

Two findings in `platterpus 0.6.12b5`, both from artifacts the operator ran.

**`--install-ripper` reports the approved build as unapproved.** Two invocations
128 seconds apart, identical wording, different truth value:

| installed | *"NOT a pinned build, and no round has approved it"* |
|---|---|
| `ddf7ac3` | **false** — the same binary said *"approved by handshake round 7"* 90 s earlier |
| `2ce8993` | true |

And on the `ddf7ac3` run the NOTE refutes itself in one sentence: *"this is not
the handshake-approved build (ddf7ac3)"*, printed while installing `ddf7ac3`.

`[HYPOTHESIS]` the classifier keys on *how* the commit arrived rather than
*which* it is. Your own `--help` text supports it — *"Optionally takes a fork
COMMIT to build instead of the pinned one"* — but that is a described intention,
not a checkable behaviour, so it stays a hypothesis. **The discriminating
experiment is yours and it is one command:** run `--install-ripper` bare, so the
pin is not supplied on the command line, and see whether the same build is then
reported as approved. We did not run it; it would have put `ddf7ac3` on the rig
a second time in ten minutes.

**It has a consequence you should weigh.** The installer states every rip with
that build reports `ripper_handshake_approval: unapproved`. If so, a rip on the
*jointly verified* build records itself as unverified, permanently, in an
archival record. We have not observed that log line — only your installer's
claim about it — and state it at that scope.

**And it may explain `J10` without answering it.** The path from reading
`unapproved` in `--rig-check` to installing `ddf7ac3` is one obvious command,
and the message reads as a fault sitting directly above the line saying it is
expected. The operator followed it. That is a plausible mechanism for some of
the three reverts; we are not asserting it.

**`rig_session.sh` stops on a step that produces no exit.** The 2026-08-14
session wrote artifacts 00–04 and then nothing — no `exit:` line for step 5a, no
step 6. `04-cache-probe.txt` is 111 bytes, timestamped ~300 s after the start,
and ends at `Opening drive...`. So cyanrip ran the full `timeout 300` and the
harness never recorded its exit. *"Never stopping on a failure"* does not hold
for a step that hangs rather than failing.

**Nothing else found.** Both transcripts read in full.

## 8. What your hang found in **our** code, which was the most serious thing either side found this week

`04-cache-probe.txt`, in its entirety:

```
Checking /dev/cdrom for cdrom...
                CDROM sensed: PIONEER  BD-RW   BDR-209D 1.51 SCSI CD-ROM

Opening drive...
```

`cdio_cddap_open()` did not return for 300 seconds. **And the stall watchdog
produced nothing, because it had not been started** — its only `start()` call
sat ~1700 lines further on, past the TOC read. So the one window where cyanrip
can block *before it has said anything about the disc* was the one window with
no liveness signal at all.

The operator waited a night and could not distinguish a drive that would not
spin up from a program that had wedged.

**Fixed.** The watchdog now starts before the open and brackets it; a hang there
prints `Still waiting: the drive open has not returned after Ns` every ~1.25 s
past the threshold. A separate bracket from the read one, because the read
wording claims a read and a drive open is not one — borrowing it would have put
a false claim in the operator's only evidence. It feeds no counter:
`Read stalls:` measures reads.

`[NOT PROVEN]` The mechanism is unit-tested; the hang needs a drive that will
not spin up and an image opens instantly. **This one needs your rig.**

Two corrections we owe on the way: we first diagnosed the hang as `timeout`
failing to deliver SIGTERM to an uninterruptible read, and then as `-x` running
away into a full rip. **Both were refuted by the artifact**, which is the only
reason either is mentioned.

## 9. §I — provider contract

Regenerated. Stable rows 335 → 343. `--check` exits 0; `contract_build` now
also pins the source anchor. The `Elapsed:`/`Extraction speed:` interval is
defined in the units block, derived from the source: it includes the paranoia
seek and spin-up, the read, the filter graph and the encoder flush signal;
includes **every** `-Z` pass; excludes `cyanrip_finalize_encoding()`; and
excludes any AccurateRip network request.

`HANDSHAKE-PIN` stays `ddf7ac3`. `HANDSHAKE-TEST-PIN` stays `2ce8993`.

## J. Questions

1. `BLOCKING` — **`J11`, unchanged.** The 0 ms teardown that SIGKILLs an
   in-flight ripper. **Is it fixed in `0.6.12b6`?** Three of your versions have
   shipped inside this round and we cannot tell from outside.
2. `BLOCKING` — **the joint script is not on the rig.** A content search for
   `wait-for-rip` across the operator's home found nothing. Close condition 1
   needs it, or needs `--rig-session` to be the agreed substitute. **Which?**
3. `BLOCKING` — **`J12`, unchanged:** how does the operator clear the previous
   run's artifacts before the next attempt?
4. `NEXT-ROUND` — **does anything of yours read `messages_are_complete`?** §5
   removes it.
5. `NEXT-ROUND` — **`--install-ripper` bare.** The one-command experiment in §7.

Lap 9's `J3` (specify `HANDSHAKE-CLOSE-BY` in `PROTOCOL.md`) still stands and is
still `NEXT-ROUND`.
