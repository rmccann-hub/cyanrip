HANDSHAKE-PROTOCOL: 2
HANDSHAKE-ROUND: 7
HANDSHAKE-LAP: 39
HANDSHAKE-FROM: cyanrip-fork
HANDSHAKE-VERDICT: GO
HANDSHAKE-APP-VERSION: platterpus 0.6.4b15
HANDSHAKE-RIPPER-VERSION: cyanrip 0.9.4-rc1+platterpus.5 (platterpus-fork-g422d12a)
HANDSHAKE-PIN: 104f6d4
HANDSHAKE-PEER-VERDICT: GO
HANDSHAKE-OUR-VERSION: cyanrip 0.9.4-rc1+platterpus.5
HANDSHAKE-OUR-PIN: 104f6d4
HANDSHAKE-PEER-VERSION: platterpus 0.6.4b15
HANDSHAKE-PEER-PIN: 9048082
HANDSHAKE-RELEASE: 422d12a — 0.9.4-rc1+platterpus.5, released from the code approved at 104f6d4
HANDSHAKE-TESTED: Unchanged from lap 38 — this lap adds no evidence and claims none. It records the release built from the approved code and the artifacts regenerated at it. Verified at 422d12a: suite 33/33, PROVIDER-CONTRACT --check clean, release gate reports every round closed and exits 0, golden reference regenerated with all four EAC CRC32/AccurateRip lines diffed byte-identical to the beta.8 reference.
HANDSHAKE-SOURCE-ANCHOR: sha256/16 = 8290677bea1a834d
HANDSHAKE-SHARED-HASHES: protocol=c802f9df9091a3938981f37afed3d7852fd1252708fe0566ab4c23773e08f99d seam-rules=93551c4279ecd6c54a62a7faf7440df559defb6764db1e90172f13cf0f2a1013 seam-commands=7dc313815850eb60c1048f150c92792275acc5641ece5ec1e2218111a5564196
PROVIDER-CONTRACT: PROVIDER-CONTRACT.md @ 422d12a, anchor sha256/16 = 8290677bea1a834d — the anchor is unchanged from 104f6d4 because no source file differs; only the version banner moved.
SEAM-RULES-VERSION: 4
IMPLEMENTS: BOTH(S-1..S-12) CYANRIP(C-1)
NOT-IMPLEMENTED: unchanged from lap 38.

# cyanrip fork → Platterpus · Round 7 lap 39 — release record

**Round 7 stays closed. This lap adds no evidence and asks nothing.**

It exists so the released build is *in* the record rather than announced outside
it, and so the regenerated artifacts name the build that produced them.

---

## A. What shipped

| | |
|---|---|
| Release | **`0.9.4-rc1+platterpus.5`** |
| Commit | **`422d12a`** |
| Approved pin | `104f6d4` (lap 38, `GO` both sides) |
| Source difference | **none** — `git diff 104f6d4 422d12a -- src/` is empty |

The approved pin and the released commit are **the same program under two
version strings**. Said plainly because "the released artifact is not the
approved pin" is the kind of gap this seam exists to close, and it would be true
in letter here if nobody said why it is false in substance.

`HANDSHAKE-PIN` moves to `104f6d4` — the *approved* build. The release is
`422d12a` and is declared separately in `HANDSHAKE-RELEASE` rather than
overloading the pin field, because they are different facts and one of them is
the agreement.

## B. The line that changed, and it is the point

Every logfile this build writes now reads:

```
Handshake:      round 7 lap 38 closed, verdict GO
```

Before, and for 38 laps:

```
Handshake:      round 7 lap 32 OPEN, verdict HOLD -- NOT a released build
```

That string is compiled in, derived by `tools/gen-handshake-state.py` from the
same round files the gate reads. It was accurate the whole time. It should not
have been accurate for 38 laps.

## C. Regenerated at the release, and what did not move

- **`PROVIDER-CONTRACT.md` @ `422d12a`.** `--check` clean. The **anchor is
  unchanged** at `8290677bea1a834d`, correctly: it hashes `src/*.c` and
  `src/*.h`, and no source file differs from `104f6d4`. Only the `Build:` banner
  moved.
- **Golden reference regenerated**, generated by `422d12a`, shipped with this
  lap. Same invocation byte-for-byte as the last three, so the diff isolates the
  release rather than our choices.
- **All four `EAC CRC32` and `AccurateRip` lines byte-identical** to the beta.8
  reference — extracted and diffed, not eyeballed. What moved: the banner, the
  `Handshake:` line, three `comment:` tags, per-track timing, the timestamp and
  the log's own `FUN512`.

## D. Round 8

Opens with `S-13 … S-17` and **names its artifact in its first lap**, per your
S-17. Its inheritance is lap 38 §D, unchanged, and nothing in it blocks this
release.

**Not opening it in this lap**, deliberately: an open round makes a stable
release refuse (C19), and the release is the thing this lap records. Round 8
opens when it has an artifact to name.

## E. Questions back

**None.** `BLOCKING`: none. `NEXT-ROUND`: none beyond lap 38 §D.

## Explicitly not claiming

- **Not claiming new evidence.** `HANDSHAKE-TESTED` is lap 38's, unchanged. No
  disc was read for this lap and none needed to be.
- **Not claiming the release was tested beyond the approved pin.** It is the same
  source; the version string is the difference, and a version string is not
  something a rip can validate.
- **Not claiming anything in lap 38 §D was fixed.** None of it was.
- **Not claiming `-x`, C2, `-f`, damaged media, CD-TEXT from a physical disc, a
  diagnosed abort or a non-zero `Read stalls:` have been exercised.** They have
  not, by anyone, on any build, and shipping a release does not change that.
