Unreleased
==========
**This is the content of `+platterpus.15`, candidate `61711f1`, cut when round 25
closes.** `+platterpus.14` shipped at `3e01bb3` on 2026-09-22; its notes are
below, under its own heading. `src/` has changed since `.14` once, at
`2af669e`, and the compiled banner once, at `20a5aca`. Both are below.

**`-r` values that are not a multiple of 5 no longer hang on an unreadable
sector** (`2af669e`). libcdio-paranoia compares its per-frame retry counter with
the limit only at multiples of 5, so at the default paranoia level one bad
sector made `-r 3`, and `-r 0`, never return. Measured with
`tests/badsector.c`, an `LD_PRELOAD` shim that fails the reads of one sector of
a disc image, with no drive involved. The per-frame limit is now rounded up to a
multiple of 5, with 5 as the floor. `Retry limit:` gains a second form saying
both numbers when they differ, and it is byte-identical to before when they
agree. The damaged sectors come back as zeros counted in `Ripping errors:`, and
every other sector is byte-identical to the source. The new `bad_sector`
scenario pins it, together with `Track N read with errors.`, which no test had
reached. It is clean under AddressSanitizer and UBSan. **Found alongside and
not fixed:** with paranoia disabled (`-P 0`), a bad sector still hangs at any
limit. It is in `docs/KNOWN-ISSUES.md`.

**A held lap's verdict is marked as a draft in the compiled `Handshake:` line**
(`20a5aca`), as round 23 agreed and nobody built. The open state then reads
`... (draft — lap not released for reading)`. Released and closed states are
unchanged.

**The gate carries v6's close rules, dormant until v6 lands** (`82a6154`).
For a file declaring 6, the gate uses the decision-time reading of C37 and
requires the agreed-change ledger (C44). Tests run them with the gate's
version set to 6.

**Round 25 lap 2 is released** (`bceb35d`). It carries the operator's
instructions: fewer rounds, fix rather than argue, time for physical CD rips,
and every round ending on usable releases of both applications, with the real
test on the released pair opening the next round. It adds that as close
condition §0.3 by recorded override of R1, and proposes it for every round as
v6 R8 and R9. The golden reference was **generated by `bceb35d`** and
**committed at `2516a4c`**.

**Round 24 closed `GO`/`GO` on 2026-09-23, at three laps.** It opened with
`docs/handshake/round-24-lap-01.md`, released by the operator on 2026-09-22,
which declared protocol 5, pin `3e01bb3`, verdict `GO`, and one close
condition: Platterpus's verdict on `.14`. Its §0 said plainly that `.14` went
to stable by the operator's instruction. While the round was open,
`--release-gate` exited 1 and the tip's logs carried `Handshake: round 24 lap 1
OPEN, verdict GO -- NOT a released build`.

Their lap 2 was `GO`: their 0.6.53 parser read our golden reference with zero
unrecognised lines. It closed the round on their gate, and our lap 3,
released on the operator's word, closed it on ours. Their `FORK_PIN` rolled
to `3e01bb3` at `platterpus@86f0547`, and their users get it in 0.6.54. Lap
3 §F records the operator's decision on their N4: a strict release gate for
`v0.*`. `--release-gate` exited 0 again, until round 25 opened.

The golden reference moved with the `Handshake:` line at each lap, each time in
its own commit. **Generated by `ace22cf`**, the commit that released lap 1, and
**committed at `0bc44eb`**. Then **generated by `5f6ca3b`**, the commit that
released lap 3, and **committed at `9a8f436`**.

**Round 25 opened on 2026-09-23** with `docs/handshake/round-25-lap-01.md`,
released on the operator's word. It declares protocol 5, pin `3e01bb3`
(unchanged), verdict `OPEN`, and two close conditions, both text:
`PROTOCOL.md` v6, and `OWNERSHIP.md` v3 with `seam-rules.md` v6, each
byte-identical in both trees. The proposed texts are under
`docs/handshake/proposed/`, committed at `39dee09` and quoted by sha256. v6
carries the round-22 agreements K1–K3, the decision-time reading of §5b, an
agreed-change ledger (`HANDSHAKE-AGREED-CHANGES`), and a definition of what a
citation names. Every other item on Platterpus's round-25 agenda is placed by
name in lap 1 §E. `--release-gate` exits 1 and names round 25. The tip's logs
carry `Handshake: round 25 lap 1 OPEN, verdict OPEN -- NOT a released build`.
The golden reference was **generated by `5164c25`**, the commit that released
lap 1, and **committed at `4a25c43`**.

**Our gate, fixed before round 25 opened**, both being Platterpus's agenda
items D1 and D2:

- **`cc235a1`: every file of a round is read for its version**, peer laps
  included. One declaring more than the gate implements refuses the round
  (C15). From round 25, one declaring less than an earlier lap of either side
  refuses it too (C29). Moving that boundary to 0 reopens round 8, so the
  boundary is load-bearing.
- **`12a85fd`: the coverage check sees lettered rows** and turns rows on by
  each block's heading version. It lists `KNOWN_DIVERGENCES` on every run
  (C13a is the only entry), and three wrong coverage claims were corrected.
  Replaying every round lap by lap found six `GO` laps after a close. That is
  why round 25 proposes amending C13a rather than implementing it.

**Found this cycle, recorded in `docs/KNOWN-ISSUES.md`, and not yet fixed:**

- **`PROTOCOL.md` v5 §5b cannot save the lap it was adopted to save.** Step 1
  and row C37 require the peer lap to be declared in the closing file's own
  `HANDSHAKE-INBOUND-HELD`, and the lap that would save a transcription lap is
  always written after that file. Found by Platterpus's question about
  *"enumerated"*. Our only C40 test uses a fixture that cannot occur. **The
  wording is proposed in round 25's v6**, and our gate changes when it
  implements 6.
- ~~Our gate decides a close from a peer lap declaring a protocol it does not
  implement.~~ **Fixed at `cc235a1`**, above.
- **Row C13a: our gate does the opposite of it.** No longer invisible: since
  `12a85fd` the coverage check sees it and lists it as a known divergence. The
  behaviour is unchanged, because round 25 proposes amending the row.
- **`Track %i read with errors.` is asserted by no test**, and its predecessor
  never was either.

Also on the branch since `.14`: the pre-round-24 document audit (`684e117`), and
`tools/seam-sync-check.py`'s clone hint no longer suggests a shallow clone
(`c449a92`).

0.9.4-rc2+platterpus.14 — 2026-09-22 — **stable**
=================================================
**Released at `3e01bb3`, `release_seq` 24, authorised by round 22** (`GO`/`GO`,
five laps, 2026-09-21). Ledger row 24; `release-manifest.json` regenerated with
both channels resolving `3e01bb3` and `--check` exit 0. **No tag** — tag push is
`HTTP 403` from this environment, so the commit SHA and the manifest row are the
whole identifier.

**The candidate was proved green on its own before publication, two ways.** The
full suite from a removed log in a fresh worktree at `3e01bb3`: 87/87, one run
header, 87 result lines. **And the consumer's exact install path**: a `git
archive` tarball of `3e01bb3` with no `.git`, built with the manifest's own
`meson setup build -Ddeclare_released=true && ninja -C build`, reports
`cyanrip 0.9.4-rc2+platterpus.14 (platterpus-fork-g3e01bb3)`, `Handshake:
round 23 lap 5 closed, verdict GO -- released build`, `Track 1 read
successfully!` and `Encoder errors: none; 1 track encoded`.

**RELEASE CANDIDATE `0.9.4-rc2+platterpus.14`, AND THIS COMMIT IS IT.** Version
bumped at `2e6d97d`; every derived artifact regenerated at `02bc6a7`, which is
**generated by `2e6d97d`** and **committed at** its child, `02bc6a7`. Two commits,
never one: a generated artifact cannot carry the hash of the build that produced
it. **This is the first commit at which the version and every derived artifact
agree**, which is what makes it the candidate — not the bump, which is red on its
own freshness checks by construction and is meant to be.

**Authorised by round 22**, closed `GO`/`GO` on 2026-09-21, which reviewed the
per-track split, graded it P1 on Platterpus's measurement and agreed the ordering
both ways. `tools/release-gate.py --release-gate` exits 0 — round 23 closed
2026-09-22 and round 24 is not open. `.13` is the precedent that a release need
not be its round's reviewed pin.

**The consumer-side prerequisite is met**, which no release of this fork has
needed before. Both renamed lines are Platterpus's `_TRACK_START` block
delimiter, so a `.14` ahead of their parser would have read as a disc with zero
tracks. **Platterpus 0.6.53** at `52b44282` accepts both wordings
(`cyanrip_log.py:237-244`) and no earlier tag does; a pre-release counts, on
their three pieces of evidence, each verified.

**WHAT A CONSUMER ACTUALLY GETS**, all P2, all agreed a round before shipping:

| change | effect |
|---|---|
| `Track %i ripped and encoded successfully!` → `Track %i read successfully!` | printed after the flush signal and before any encoder was joined, so the encode it asserted **did not exist yet** — under a 32 KiB write cap it sat above a 253,742-byte file truncated to 32,768 |
| `Track %i ripped and encoded with errors.` → `Track %i read with errors.` | the other arm; the condition always measured the read alone |
| **new:** `Encoder errors:` below `Ripping errors:` | three states — `none; N tracks encoded`, `N tracks failed (…); M tracks encoded`, `not applicable; no track was encoded` |

`-j` is unchanged and stays `cyanrip-diagnostics/6`. **The provider contract now
ships from this build**, so it and the binary round 24 reviews are one program —
Platterpus's round-23 Q4, discharged.

**STABLE, BY THE OPERATOR'S INSTRUCTION, chosen knowingly over the beta
`docs/RELEASE-PLAN-platterpus.14.md` recommended.** Platterpus's app offers a
newer build from our manifest on the user's channel, stable by default, and
stamps every rip made with it `unapproved` until their `FORK_PIN` rolls — which
they have said waits for a round to review `.14`. That window is accepted on the
operator's judgement that round 24 will be short, and round 24's opening lap
says so in as many words.

**NOT PUBLISHED BY THIS COMMIT.** No ledger row, no manifest change; both
channels still resolve to `2cce60d`. Publication is an act and the ledger is the
one hand-written input; the next commit performs it.

**What this release still does not verify**, because a green suite is not
hardware coverage: **the new wording has never been parsed from a real log** —
both sides recorded it in round 22, and it needs the post-`.14` rig session.
The `Encoder errors:` failure arm has never run on hardware; a clean disc emits
`none`. The loudness block is still measured upstream of the filter graph.
C2 is `UNREACHABLE` on the rig's drive; `-f`, damaged media and CD-TEXT from a
physical disc are *not yet done*; and **do not cite our cache figure.**

**HOW ROUNDS 22 AND 23 RAN, which is most of what this release cycle was.**
These notes were written as each step happened and are kept in that order,
restated in the past tense on 2026-09-22 once the release shipped.

**Round 23 opened** with `docs/handshake/round-23-lap-01.md`, released by the
operator on 2026-09-22. **Round 22 closed `GO`/`GO` on 2026-09-21** at five
laps, 27 days before its close-by, having carried the operator's goal for the
round: fix all known issues, release both applications by the close, then a full
hardware acceptance test to kick off round 23. That session ran on 2026-09-22 —
247 of 247 steps on `2cce60d` with Platterpus 0.6.52, filed byte-exact at
`docs/rig-2026-09-22-2cce60d/` — and it **opens** round 23 rather than closing
round 22, on purpose: round 21 took five laps because its lap 1 named a
condition that needed a drive, so lap 1 here reports hardware instead of asking
for it.

**No release was possible while round 23 was open.** `--release-gate` exited
1, and every log the tree wrote carried `Handshake:      round 23 lap 1 OPEN,
verdict OPEN -- NOT a released build`.

The golden reference moved with that line. **Generated by `8037b73`** — the
commit that released lap 1 — and **committed at `ccbd5fd`**, its own commit,
because an artifact can never carry the hash of the build that produced it and
folding the two together would leave the banner naming an unreachable commit.

**Round 23's lap 3**, released 2026-09-22, landed **`PROTOCOL.md` v5** — §5b,
where a gate may resolve the peer verdict from the newest peer lap it holds and
has enumerated; §5c, Platterpus's condition that such a lap must
declare `HANDSHAKE-READY-TO-READ: yes`, fail-closed; `HANDSHAKE-PEER-VERDICT-SOURCE`,
their field; and §8 rows C37–C42. **Implemented but inert**: every gate path is
keyed on the FILE's declared version and nothing in either tree declares 5, so
no round changed state. **And it omitted three changes round 22 had already
agreed** — K1, K2 and K3 — which the pre-round-24 audit found; see
`docs/KNOWN-ISSUES.md`. The golden reference moved again with the `Handshake:`
line: **generated by `e5008c9`** — the commit that released lap 3 — and
**committed at `275ad4b`**, its own commit, the same pairing as above.

**ROUND 23 CLOSED `GO`/`GO` AT FIVE LAPS**, 30 days before its close-by, once
`seam-sync-check --fetch` exited 0 with all four shared documents byte-identical
at `platterpus@48776b0`. `--release-gate` exited 0 again. `.14` still waited
on Platterpus's both-wordings release at that point; 0.6.53 turned out to be it,
and `.14` shipped the same day. The pin never moved inside the round.

The golden reference moved once more with the `Handshake:` line.
**Generated by `4b2b84d`**, the commit that released lap 5, and **committed at
`e2ed202`**, its own commit. Only the round half of that line moved; a development build from
a closed-round tree still reads `NOT a released build`, because the declaration
needs `--declare-released` on a clean tree and a closed round is necessary
rather than sufficient.

**A near miss is filed as an event, not as a success.** Our lap 3 warned that a
squash merge plus a branch delete orphans a cited commit. It then happened: the
PR squash-merged, the branch was deleted four minutes later, and both commits
round 23's citations name — one in a lap already sent and immutable — became
unreachable until a session clone restored them ahead of GitHub's `gc`. Recorded
in `docs/KNOWN-ISSUES.md` with the shape Platterpus named: the warning was in
the PR body twice, in bold, and the delete is a button that appears when no PR
text is on screen. **A comment where a check belongs is not a fix.** Our own
never-push-a-topic-branch rule holds because the git proxy returns `403`, not
because anything of ours checks, and that is now written down beside it.

**And lap 3 opens with a retraction.** We reported that tracks 3 and 5 of the
acceptance session were superseded by an automatic re-rip. Platterpus refuted it
and the refutation checks out: `replaced: false` on both, a swap happens only on
a converged re-read, and neither converged — so nothing was superseded and the
missing addendum was a correct negative. The cause was ours: the
`.platterpus.json` records were left out of the filed session, and the answer
was one field inside them.

**Two findings from reading the acceptance session's artifacts**, neither a
close condition, and **one of the two was wrong and is retracted.** `-H -E`
against `-H -W` reports identical checksums, peaks, loudness and ReplayGain over
audio that differs, because every audio figure in our log is measured upstream of
the filter graph: the checksums correctly so, the loudness block not. That one
stands, in `docs/KNOWN-ISSUES.md`.

**The retraction:** we reported that tracks 3 and 5 were superseded by an
automatic re-rip and that no addendum recorded it. Platterpus refuted it in round
23 lap 2 §A and the refutation checks out — the session's own report says
`replaced: false` on both tracks, a swap happens only on a converged re-read, and
neither converged, so the first pass's bytes are on disk and the album log is
correct. The missing addendum was a correct negative. The cause was ours: the
`.platterpus.json` records were left out of the filed session as the consumer's
artifact, and the answer was one field inside them. The one that settles it is
now filed.

**THE PER-TRACK BLOCK NOW REPORTS THE READ AND THE FOOTER REPORTS THE ENCODE**
— `89a57d6`, the first of round 22's fixes, and it is P2 contract surface:

| change | effect |
|---|---|
| `Track %i ripped and encoded successfully!` → `Track %i read successfully!` | the line printed after the flush signal went to the encoders and before any was joined, so the fact it asserted **did not yet exist** — under a 32 KiB write cap it sat above a 253742-byte file truncated to 32768 |
| `Track %i ripped and encoded with errors.` → `Track %i read with errors.` | same line, the other arm; the condition was already measuring the read alone |
| **new:** `Encoder errors:` directly below `Ripping errors:` | the encode outcome, at the disc-level moment the collection loop has joined every encoder thread — `none; 3 tracks encoded`, `2 tracks failed (2, 3); 2 tracks encoded`, or `not applicable; no track was encoded` |

Three states and not two, for the reason `Read stalls:` has three: `none` over
an unstated population is an absence of evidence read as evidence of absence,
and a rip interrupted after two clean tracks of fourteen would otherwise render
identically to a clean fourteen-track disc. **`File(s):` is unchanged** and
still built from the request — it prints from the same pre-join moment, so
marking a failed entry there is the same fixpoint one level down and is only
possible now that the footer names the tracks. **`-j` is unchanged**, schema
still `cyanrip-diagnostics/6`.

**The reference pair is now generated by `23c18d2` and committed at the commit
carrying this entry** — releasing lap 5 closed the round, moving the compiled
`Handshake:` line from `round 22 lap 5 OPEN, verdict GO` to `round 22 lap 5
closed, verdict GO`, so both artifacts were regenerated against that build.
`HANDSHAKE_RELEASED` stays **0** and every log a tip build writes still says
`NOT a released build`: a closed round is not a released build, and `23c18d2`
is **35 commits past `2cce60d`** — pinned to that commit, because a count
against "the tip" moves, which this session has now got wrong twice. Earlier pairs in this round: **generated by
`b774582`, committed at `11e09f7`** (lap 5, held), and before those: **generated by `623251c`, committed at `d3e3a4c`** (lap 3),
and before that **generated by `89a57d6` and committed at `ae29818`**, which is
the pair that carries the §0.3 log change itself.

`docs/sample-interrupted.*` is deliberately left alone at `623251c`. Its own
`--check` passes — it carries the current line shapes, keys and schema — and it
is a sample headed `DO NOT DIFF IT` rather than a reference, so its banner
naming an older build is not staleness. That check is what the rotted-input
warning asks for, and the check is the thing to keep green rather than the SHA.
Two commits, never one: a generated artifact cannot carry the hash of the build
that produced it. It was previously generated by `032534e`, when opening the
round moved the compiled `Handshake:` banner. **The released build `2cce60d` is
unaffected** — it is a named commit with its own correct artifacts, not the tip,
and none of this reaches a consumer until `+platterpus.14`.

**THE RIG TOOLS WERE READING THE WRONG FILE, AND THE CHECK THAT NOTICED SAID
`ok`** — `40b2aff`, found by running `tools/rig-check.py` against a Platterpus
bundle instead of re-reading it. **No source change; the ripper is untouched.**

| defect | what it did |
|---|---|
| `find_log()` filtered the EAC export with `"EACcompatible" not in p.name` | the file is `<album> (EAC-compatible).log`, **with a hyphen**, so the filter never matched anything from `387033e` onward. An album folder holds both logs, so every run has been picking between them by mtime |
| `check_checksum_inventory()` | `3 * tracks + both_missed` over a file with no track block is `3*0 + 0 == 0 == got`, so it printed `ok  0 lines (0/0/0/0), rule says 3x0 + 0 = 0` |
| `check_argv()` | reported `-j wrote no record at all, which is the one job it has on a run that fails early` on a run where cyanrip was never executed — `run()` returns exit `None` for a program it cannot start, and that fell through to the `j.exists()` arm |

**The first hid the second**, which is why both are fixed and both are tested.
Selection is now by **content**: a cyanrip log names cyanrip on its first line
(`cyanrip_log.c:724`, whose own comment calls that line contractual) and a
closed log carries the FUN512 marker. Either is enough — they fail in opposite
directions, an elided head keeps the marker and a killed run keeps the banner —
and an EAC export has neither, whatever it is called. Its **second** line names
cyanrip and a version, so anything looser than first-line anchoring matches it.

The bundle's tar flattens mtimes to the epoch. That made the tie
non-deterministic and the wrong pick visible; on a rig the two mtimes differ and
it would have gone on choosing by luck.

**The round-22 tolerant `TRACK_BLOCK` delimiter got its first real exercise and
holds.** Over the bundle's log — which carries the OLD `Track N ripped and
encoded successfully!` wording fourteen times and the new wording zero times —
the inventory now reads `44 lines (14/14/14/2)` against `3x14 + 2 = 44`, the two
being the `Accurip 450:` lines on the tracks whose whole-track checksums both
missed. A pattern pinned to the new spelling alone would have counted zero
tracks in every log filed before the rename.

`1b2d13f` also moves the default output from `~/seam-check-<stamp>` to
`~/cyanrip-rig-checks/seam-check-<stamp>`. The runbook's documented invocation
passes no `--out`, so each pre-flight was leaving another directory loose in the
operator's home.

**WHAT THE BUNDLE ITSELF SAYS, AND IT COUNTS FOR NO RUN** — the operator's
words; it is not a filed rig session, discharges no close condition and appears
in no `HANDSHAKE-TESTED`. Nothing below is cited in a lap or in `SETTLED.md`,
and recording any of it would mean filing the session, which is the operator's
call. Read at
`platterpusbundle20260919T051245Z`, sha256
`eaa6ea96df182e33f09b2a833b441d910df166565b622183aae4fd3b5a1ceb18`:

- **The rig is running `fe4d2c4` = `+platterpus.12`**, a build of 2026-09-12 and
  **two releases behind** — `.13` shipped at `2cce60d` on 2026-09-18 and this rip
  ran on 2026-09-19. `git rev-list --count fe4d2c4..1b2d13f` is 178; say it
  against a named commit, because "behind the tip" is a number that moves. Its compiled banner still reads `round 16 lap 17
  closed`. The bundle's own `MANIFEST.txt` flags a pin mismatch, but against
  **round 21's** test pin `3952c03`, which is stale now that round 21 is closed.
  Round 23's acceptance session needs `2cce60d` or later.
- **`flac` and `metaflac` are absent from the rig's container.** 42 errors in
  `applog/log.txt`, every one `exit 127: executable file not found` — 14
  `flac.verify_failed` and 28 `metaflac.failed`. Counted distinct: `flac --test`
  was attempted on all **14** files and `metaflac` on all **14**, twice each —
  `--export-tags-to=-` and `--remove --block-type=PICTURE`. So **no local decode
  check, no tag read-back and no PICTURE-block removal happened** on any of the
  14 files. AccurateRip v2 at
  confidence 200 on 12 of them is still a checksum match against a database, but
  the local verification leg is missing, and it is missing for an environment
  reason rather than a rip reason. Fix before the acceptance session or round 23
  produces the same gap.
- **A fifth rip of the reference disc on `fe4d2c4` agrees with the other four.**
  13 of 14 tracks carry the identical `EAC CRC32` across 09-12 (Platterpus
  0.6.47), 09-15a (0.6.48), 09-15b (0.6.49), 09-17 (the void session) and this
  one (0.6.51). The outlier is **track 3, and only in the earliest session**
  (`62680376`); the last four all give `3D8FCF0C`. Scope: this compares what
  cyanrip's LOG recorded, not the files, and 09-15b's track 3 is superseded on
  disk by a Platterpus re-read. Extends `SETTLED.md` row 112 from three sessions
  to five, but see the no-run note above.
- **The checksum reproduces while the convergence verdict does not.** Track 5
  gives the same `EAC CRC32` in all five sessions and track 3 in the last four,
  while the `Secure re-read:` line flips under them: track 3 reads `did NOT
  converge` in four of the five, track 5 in four of the five, **and not the same
  four** — track 3 converged in 09-15a, track 5 in 09-15b. `did NOT converge after 3 reads (repeat limit
  hit)` is a statement about whether three reads agreed inside the limit, **not
  about whether the audio is right** — a consumer treating it as "bad rip" would
  quarantine a track whose bytes have now reproduced five times.
- **The sharpest `Scope:` case on record, and it is not the one `SETTLED.md`
  names.** Under `-Z 2 -r 3` the per-track paranoia blocks sum to `READ` 21439
  against a disc total of 64994 (ratio 3.032), `OVERLAP` 477 against 1417
  (2.971), `VERIFY` 3991 against 8742 (2.190) — and **`FIXUP_ATOM` sums to 0
  per-track against a disc total of 45.** Row 84 records `FIXUP_EDGE` 0-against-2
  as the sharpest there has been; this is the same shape at 45, on a counter that
  marks actual audio repair. The `Scope:` line is present and correct on all 14
  tracks, which is the whole reason a consumer is protected here. Row 84's
  superlative is left alone, because the artifact that refutes it counts for no
  run.

**AND THE TWO ALBUM ARTIFACTS DESCRIBE TWO DIFFERENT INVOCATIONS** — read after
the entry above was written, by opening the 6.8 MB `.platterpus.json` instead of
stopping at the log. `outcome.ripper_argv` ends `-r 3 -Z 2 -l 3,5 -N`; the
`Invoked as:` line of the `.log` beside it carries **no `-l`** and the header
says `Tracks to rip:  all`. So the log is the finished 14-track rip
(`Ripping finished at 2026-09-19T00:45:17-04:00`) and the report is a **targeted
re-rip of tracks 3 and 5** — the two that did not converge — merged into an
album record: `tracks` carries all 14 numbers with full per-track data, and
`completeness` reads `tracks_expected 14 / tracks_in_report 14 / complete
true`, which is coherent for a merged record and is **not** a defect. The
diagnostics' 15th and 16th entries (`05:00:40Z`, `05:12:44Z`) are that re-rip's
two tracks, and that is what "mid run" means here. *Read the `Invoked as:` line
before crediting a scenario with what its folder claims* — this repo's own rule,
and one album folder can now hold two rips' worth of artifacts.

**AND TWO THINGS FOUND WRONG IN THEIR OUTPUT, DERIVED FROM THE BUNDLE ALONE —
no code of theirs was read.**

- **`MANIFEST.txt` asserts `waited for post-rip  yes — every post-rip check had
  finished and the report was flushed`, stamped `created 20260919T051245Z`.** The
  report in that same archive declares `generated_at
  2026-09-19T01:12:57-04:00` — **`05:12:57Z`, twelve seconds later.** One
  declared field against another, both inside the bundle: the archive carries a
  report that did not exist at the moment its manifest said the report was
  flushed. The `applog/log.txt` agrees and dates it — it runs to the same
  `01:12:57` and its **first** error is at `01:12:45,978`, after the stamp. `diagnostics.txt`
  says `errors: 0  warnings: 0  info: 16  worst: info` over `scope: process
  session`, and its last entry is `05:12:44+00:00`, **one second before that
  first error**. Each file was accurate when written; the archive's contents span
  `05:12:44Z` to `05:12:57Z` under one apparent "as of", and the field whose
  entire job is to say the snapshot waited is the one that is wrong. This is `CLAUDE.md`'s rule 5 — event
  time and processing time are two independent ages — arriving in a bundler, and
  the same shape as our own `STATUS.md` carrying two disagreeing `released` rows.
- **A verifier that could not run, reported as a verification that failed —
  and it is in the machine-readable field, not only in a UI label.** The warning
  reads `⚠ FLAC verify FAILED for 14 file(s)` where every one is `exit 127:
  executable file /usr/bin/flac not found`, and the report declares
  `verification.gates.flac_integrity: "ran"` with
  `flac_integrity: {ran: true, ok: false, checked: 14, failures: [all 14]}`.
  A consumer reading `ran == true, ok == false` concludes the audio failed its
  integrity check; what happened is that the checker was absent. `gates.ctdb`
  says `"ran"` the same way, with `ctdb.verdict: "lookup_error"` caused by the
  same missing `metaflac`. Their `detail:` and `message:` preserve the exit code
  and the reason throughout, so no evidence is lost — this is the **grade**
  over-asserting, not a data defect, and *a label asserts even when its value
  disclaims*. **We have
  the same defect in our own tree this week**, in `check_argv()` above, found in
  the same session by running our tool against their bundle. Reporting theirs
  without ours would be the over-scoped verification this repo has a rule about.

**ROUND 22 IS AGREED `GO`/`GO` AND ITS CLOSING LAP IS HELD** — their lap 4
released at `platterpus@267a696` and filed at `ce5a819`; our lap 5 written at
`b774582`. Five laps, 27 days before the close-by.

Their lap 4 resolves the circularity lap 3 named, **by route (i), the one lap 3
recommended**: the second clause of their own `GO` condition — *"agreed **and our
side of it is released**"* — conflated a decision with an act. The ordering is
the decision; their release is a consequence that follows a close. Nothing
waived, and their own lap 2 had already drawn that line for *our* release without
noticing it applied symmetrically. They also fixed our §H1 **in the tool rather
than in the author**: `handshake.py --announce` now refuses a lap whose body
still restates the release declaration — and its first version refused lap 4
itself, flagging three *quotations* of the offending sentence made while
explaining the fix, so a lap reporting the defect was indistinguishable from a
lap having it.

**Why there is a fifth lap, since their lap 4 says the round closes at four.**
It closes at four on their gate and cannot on ours. `PROTOCOL.md` §5 requires
`HANDSHAKE-PEER-VERDICT: GO` **in a file of ours**, and our newest file was lap
3, written when their newest lap was lap 2 declaring `OPEN` — the only honest
value it could carry, and it is sent and immutable. So our gate held the round
open on `self.peer_verdict not in CLOSING`, one line *before*
`stale_peer_verdict` was satisfied by their filed lap 4.

This is `SETTLED.md` row 102, from **round 17**, with the roles reversed: *a
round closes on the gate of whichever side sent the last lap, and both
implementations have that property.* They filed a `verified/` record then; we
cannot, because `--release-gate` refuses while any round is open and an unclosed
round 22 would block `+platterpus.14` permanently. **Lap 5 is the close, not an
acknowledgement** — lap 3 pre-committed to a *verdict*, and it never promised the
absence of a lap.

Both round-23 items are in `docs/KNOWN-ISSUES.md` under "Open, joint":

- **The close condition cannot be satisfied by the side that speaks first.**
  Proposed for v5 in lap 5 §H1 and **deliberately not implemented** — the close
  rule is shared, and relaxing a match rule on one side is how two gates come to
  disagree about whether a round is closed. Their assent is required.
- **A HELD lap's draft verdict reaches the compiled `Handshake:` line.**
  Pre-existing — verified by generating the state in a throwaway worktree at
  `623251c`, where held lap 3 produced the same shape. Bounded, because
  `HANDSHAKE_RELEASED` is `0` for any open round so the log also says `NOT a
  released build`, and a released build cannot carry a draft verdict at all. The
  fix is contract surface, so it waits for a round.

**THE DIGEST VERIFIER COULD NOT READ EITHER SIDE'S CELL** — `f08c037`, found
while checking their lap 4's declaration rather than by reading the tool.
`--check` matched a literal `sha256/16 = <hex> over N lap`, hex bare, and both
sides write that cell with markdown in it. Measured over the whole record: of
**121** `HANDSHAKE-ROUND-DIGEST:` lines, 98 parsed, 17 correctly declare no
machine-readable value, and **6 carried a real digest the reader could not see —
three of them ours.**

| spelling | laps |
|---|---|
| ``sha256/16 = `<hex>` over N lap(s)`` | our round-22 laps 1 and 3 |
| ``sha256/16 `<hex>` **over N lap(s)**`` | our round-21 lap 5; their round-21 lap 4, round-22 lap 2, round-22 lap 4 |

So it skipped **every** declaration in round 22 and exited 0, in the tool whose
own docstring says a digest is the one field a human cannot proofread because
every wrong value looks exactly like every right one. The drift began at round 21
lap 4/5 when both sides started emphasising the cell, and nothing noticed for two
rounds because **"declares no digest" and "declares one I could not read" printed
the same sentence** — `none` versus `unknown (reason)`, inside the checker built
to stop that class. Third instance of that family in our tooling in two sessions.

Fixed in two parts. The reader implements the rule its own comment already named
as the v5 answer — **the value is the leading token sequence, prose follows and
is ignored** — taking the head of the line first and stripping inline markup from
that head alone, so the round-9 anchor holds and `not computable in the file it
covers — a digest over every lap would include aaaa…` still declares nothing. And
a head that names `sha256/16` and shows a digest the reader cannot take is now a
third state, `unparsed`, which **fails**.

**Nothing was hiding behind it.** Round 21 goes from 3 matched to 5 and both
newly-read declarations *match*; round 22 reads all four and all four match. The
historical mismatches are unchanged — round 14's five are the crossed-lap round,
round 9's two are the documented lap-7 defect — and no `unparsed` exists anywhere
in the record. **Their `8cca64201759ae74 over 3` now re-derives on our
implementation**, which could not be said the day before.

**Their `handshake_round` observation — checked, and the field has one reading.**
It is `latest["round"]` at `tools/gen-release-manifest.py:233`, taken verbatim
from the **append-only `round` column** of `docs/release-ledger.tsv`; row 23 is
`23 stable 0.9.4-rc2+platterpus.13 2cce60d 21`. So it means *the round that
authorised this release*, by construction, and cannot drift — round 22 reviewing
the same commit does not change what authorised the release, and the row cannot
be edited. **It is load-bearing:** the generator's Property 1 raises
`LedgerError` when `stable` points at an unclosed round, so a field naming 22
would have failed generation for most of the window in which `.13` was published
stable. Their own example gives 21 under both readings, so it does not separate
them either; checked before answering.

**AND THE SUBSTANCE CAME THROUGH A RELAY, WHICH IS IN NEITHER REPOSITORY.** Both
files were fetched and verified: the lap is 15,283 bytes, sha256 `614c6115…`; the
envelope `round22lap04FROMplatterpusTOcyanrip.md` is 16,945 bytes, declares
`1 file(s)`, and its part is byte-identical to the standalone lap, so it adds only
its own 42-line header. **Neither contains the close-gate question, the
`handshake_round` observation, the two flags or the open list** — and the lap says
`F. Questions: None. Not "none blocking" — none at all.`

That is our own defect coming back: *a relay is not merely uncommitted, it exists
nowhere — not in our tree, not in theirs, in no digest, unciteable by either side
forever.* Their round-21 lap 4 §I and §J already cite text nobody can produce,
and that was our doing. Answered anyway in lap 5 §H1, because the question is
about our code and our code is the evidence — and lap 5 is now the only durable
record of it.

0.9.4-rc2+platterpus.13 — 2026-09-18 — **stable**
=================================================
**Released at `2cce60d`, `release_seq` 23, authorised by round 21** (`GO`/`GO`,
five laps, against Platterpus `0.6.50`). Ledger row 23;
`release-manifest.json` regenerated with both channels resolving `2cce60d` and
`--check` exit 0. **No tag** — tag push is `HTTP 403` from this environment, so
the commit SHA and the manifest row are the whole identifier.

**The candidate was proved green on its own before publication** — 87/87,
exit 0, from a removed log, at `2cce60d` — which is the check `+platterpus.5`
failed when it was announced at a commit failing 2 of 33 from a fresh clone.

**How it was built.** Version
bumped at `51dc7c9`; every derived artifact regenerated at `8bdb19e`, which is
**generated by `51dc7c9`** and **committed at** its child. Two commits, never
one: a generated artifact cannot carry the hash of the build that produced it.
**This is the first commit at which the version and every derived artifact
agree**, which is what makes it the candidate — not the bump, which is red on
its own freshness checks by construction and is meant to be.

**Authorised by round 21**, closed `GO`/`GO` on 2026-09-18 against Platterpus
`0.6.50`, five laps, 32 days before its close-by. `tools/release-gate.py
--release-gate` exits 0.

**WHAT A CONSUMER ACTUALLY GETS, and unlike `.12` a rip does change.** Both are
P2 contract surface and both were agreed a round before they shipped:

| change | effect |
|---|---|
| `Frame retries:  N` → `Retry limit:    N (per frame, and per whole-track re-read)` | the label named half of what `-r` does; it caps paranoia's per-frame retries **and** the whole-track re-read loop |
| `cyanrip_log_finish_report()` moved below the encoder-status loop | **`Ripping errors:` now counts encoder failures** |
| `-j` key `frame_retries` → `retry_limit`, schema → `cyanrip-diagnostics/6` | a rename is not additive; a consumer keyed on the old name gets nothing |
| `-j` gains a `cache_probe` block | calibration reads, threshold, ratio, one entry per run — additive |

**The second row is why this release matters rather than tidies.** Under a
32 KiB write cap the old placement wrote `Ripping errors: 0` and `Rip
completed:  yes` over a 253,742-byte file truncated to 32,768, while `-j` said
`2` and the process exited `1`. **Two records of one run, disagreeing, and the
human-readable one wrong** — and the log is the archival record, the one that
outlives the exit code. `+platterpus.12` stamps `No errors occurred` onto a rip
that lost data.

**NOT PUBLISHED BY THIS COMMIT.** No ledger row, no manifest change, both
channels still resolving to `fe4d2c4`. Publication is an act and the ledger is
the one hand-written input; the next commit performs it.

**What this release still does not verify**, because a green suite is not
hardware coverage: **the encoder-failure arm has never run on hardware** — the
rig rip that authorised it had `Ripping errors: 0`, and a zero-error rip emits
byte-identical output under both placements, so it cannot distinguish the fixed
build from the broken one. `Retry limit:` is read by no Platterpus field, by
their design. C2 is `UNREACHABLE` on the rig's drive; `-f`, damaged media and
CD-TEXT from a physical disc are *not yet done*; §4b's cache calibration series
is still unrecorded, so **do not cite our cache figure.**

**ROUND 21 IS CLOSED, `GO`/`GO`, ON TEST PIN `3952c03`.** Our lap 5 was released
by the operator on 2026-09-18 and Platterpus's lap 4 on the same day; both
verdicts read `GO` and `tools/release-gate.py --release-gate` exits **0** for the
first time since the round opened. **That authorises a release and does not
perform one** — `docs/RELEASE-PLAN-platterpus.13.md` §4 is the sequence, and the
release is named at the first commit where the version and every derived
artifact agree, never at the version bump.

**§0.1's three conditions, scored individually rather than as three ticks.** The
`fast_verified` whole-disc path ran on hardware on the agreed test pin —
`docs/rig-2026-09-17-3952c03/`, 14 of 14, `Ripping errors: 0`. `Ripping errors:`
was read from the build carrying the move, with the caveat stated in the lap
that **a zero-error rip cannot discriminate the move at all**: with no encoder
failure the pre-move and post-move builds emit byte-identical output, so the
evidence that the fix does anything remains
`sc_encode_failure_reaches_the_log()` in our own suite and is not hardware
evidence. And condition two — *"your parser reads `Retry limit:` on real logs"* —
**was never satisfiable as worded, which is our defect**: the label is an entry
in Platterpus's `_IGNORED_DISC_LINES`, from which no field is derived, and their
comment saying so was in their tree when our lap 1 was written. The risk it
guarded is retired; R1 freezes a close condition at lap 1, so it could only be
explained.

**The reference pair is generated by `19621d3` and committed at the commit
carrying this entry.** Closing the round moved the compiled `Handshake:` banner
to `round 21 lap 5 closed, verdict GO`, so both artifacts had to be regenerated;
a file cannot carry the hash of the build that produced it, so the two commits
are necessarily different. **This entry is where the build is named because the
closing lap is immutable the instant it is released** — the case
`sc_golden_reference_is_from_a_clean_build()`'s own comment anticipates, and the
reason `Changelog.md` is its second home.

**ROUND 21's LAP 4 WAS WRITTEN BY US AND WITHDRAWN UNSENT.** Both sent laps
allocate the number to Platterpus — their lap 2 declares `HANDSHAKE-NEXT-LAP:
ours`, our lap 3 declares `yours` — and our draft was the only document saying
otherwise. It cited *"the blocker goes first"*, a rule about one side's **defect**
preventing the other from producing evidence, which a finding they merely need is
not. The collision existed because we held the lap across a lap boundary, which
`CLAUDE.md` says not to do; Platterpus's round-22 proposal, **a lap number is
claimed on release rather than on writing**, is a shared-protocol change and we
agree in principle.

**The 2026-09-17 rig session ran `fe4d2c4`, the release pin, not the test pin
`3952c03`.** All eight rips carry the old `Frame retries:` label and
`Handshake:  round 16 lap 17 closed … released build` — the line that exists so a
rip on disk can be checked against the pair that was agreed, doing that job for
the first time. §0.1 is **one of three**: the `fast_verified` whole-disc path is
established and is a property of Platterpus's script rather than of our build,
while the two items about `Retry limit:` and the moved `Ripping errors:` are
unreachable from a build that has neither. Filed byte-exact at
`docs/rig-2026-09-17-fe4d2c4/`. **One whole-disc `fast_verified` rip on
`3952c03` covers what is left** — not another six-hour sweep.

**The reference pair is generated by `193fea1` and committed in the commit
carrying this entry.** Removing the withdrawn lap file moved the compiled
`Handshake:` banner back to `round 21 lap 3`, so the artifact had to be
regenerated; a file cannot carry the hash of the build that produced it, so the
two commits are necessarily different. This entry is where the build is named
because the only unsent lap was the one withdrawn, and lap 3 is immutable.

**THE CACHE PROBE NOW RECORDS THE EVIDENCE ITS VERDICT WAS FORMED FROM**, so a
hardware session can be used to fix the calibration instead of only
demonstrating it again. `-j` gains a `cache_probe` block: the three calibration
reads, the median that became the threshold, the ratio in force, and one entry
per run with the re-read time and what the current rule scored it. Schema moves
to **`cyanrip-diagnostics/6`**.

**The decision rule is untouched.** The predicate is still
`t * CACHE_HIT_RATIO < miss_cost`. Nine filed rig sessions report *"at least
2048 sectors … search ceiling reached"* and **not one recorded the dozen timings
behind it**, so all nine are unusable for fixing the defect they demonstrate —
a read time is a measurement of a drive at a moment and cannot be re-taken. A
rule redesigned before its evidence exists is what this repository has a rule
against, and the last prediction made about this code was falsified by the first
run it ever had.

The reference pair and the contract are **generated by `b2c9527`** and committed
in the commit carrying this entry; the contract picked the new fields up by
derivation rather than by being told.

**ROUND 21 IS OPEN, AND THE TEST PIN IS `3952c03`** (it was `2c3deff` when this entry was written; it moved once while lap 1 was still held, before anything was agreed, which is the only window R4 allows)**.** Two close conditions, fixed
in lap 1 and unable to grow: one hardware acceptance session on that pin with
`0.6.50`, and Platterpus's ruling on `Rip completed:` sitting beside a non-zero
error count. The released pin `fe4d2c4` is frozen by S-15 and a test pin is
explicitly not a release.

**The reference pair and the contract are generated by `8ffb311` and committed in
the commit that carries this entry.** Their `Handshake:` line now reads
`round 21 lap 1 OPEN, verdict OPEN -- NOT a released build`. The test pin's own
line reads `round 20 lap 3 closed` instead, and that is correct rather than
stale: a build cannot contain the lap that reviews it. Lap 1 says so in
`HANDSHAKE-TEST-PIN-NOTE` because this repository has already lost time to a
round where *"lap 6 named a test pin whose log says lap 4"*, and an unstated
property is how that trap works.

**The suite result in lap 1 was measured at the test pin itself**, not at a tree
that resembles it — 85 of 85, 0 fail. Round 20 lap 3 revised that same field
three times for one cause, a number written before the command producing it had
exited; this one waited.

**ROUND 21'S TWO LOG CHANGES ARE LANDED, BOTH AGREED IN ROUND 20 AND NEITHER
SHIPPED INSIDE IT.** `Frame retries:` is now
`Retry limit:    N (per frame, and per whole-track re-read)`, the exact string
Platterpus assented to, and `cyanrip_log_finish_report()` has moved below the
encoder-status loop so `Ripping errors:` counts encoder failures. Under a 32 KiB
write cap the log and `-j` now agree at **2** where the log used to say **0**
beside `Rip completed:  yes` and a 32768-byte file whose intact form is 253742.

**`docs/golden-reference.log`, `docs/golden-reference.diagnostics.json`,
`docs/sample-interrupted.log` and `docs/sample-interrupted.diagnostics.json` are
generated by `293c1c2` and committed in the commit that carries this entry**,
along with `PROVIDER-CONTRACT.md`. `-j`'s key follows its line — `frame_retries`
becomes `retry_limit` — and the record's schema moves to
**`cyanrip-diagnostics/5`** at that commit, because a rename is not additive: a
consumer keyed on the old name silently gets nothing, which is worse than an
unknown key it can ignore. **It moved again inside the same round**, to `/6`,
when §4b's `cache_probe` block was added — so `/5` is correct about `293c1c2`
and wrong about what round 21's test pin emits, which is `/6`. Both numbers are
kept because they describe different commits; `src/diagnostics.c:371` is the
artifact for the current one.

**And the comment justifying that bump was wrong about their code.** It claimed
Platterpus allowlists diagnostics schema strings and cited
`SUPPORTED_SCHEMAS = {1, 2}`. Read at
`platterpus@d94bd113:src/platterpus/deps/ripper_manifest.py:89`, that constant
gates `release-manifest.json` — its own docstring says so — whose schema is 2.
Nothing in their tree parses `cyanrip-diagnostics` at all. **That is round 12's
defect re-imported into our own source and carried for four rounds**, and it
survived on its own closing clause: *"we cannot read their source and do not
claim to"*, false since 2026-09-13. An unfalsifiable excuse is what keeps a
checkable claim unchecked.

**Two things the fix makes visible rather than fixes**, both now named in the
scenario's docstring and in `KNOWN-ISSUES`: `Rip completed:  yes` still sits
beside a non-zero error count — the loop did complete and the encoders did fail,
which are two facts — and `File(s):` is still built from the request rather than
from what was written, beside a `Track N ripped and encoded successfully!` that
prints when the READ finished. Round 21 lap 1 puts the first to Platterpus
rather than answering it here.

**`tools/round16-accept.py` deliberately still expects `/4`.** It grades a
**closed** round against a condition fixed before its data existed, and its own
header says a checker edited after the results are in is how a close condition
quietly moves. Bumping it was drafted and reverted.

**ROUND 20 IS CLOSED — `GO`/`GO` on `fe4d2c4`, in three laps.** Their lap 2
answered both conditions fixed in lap 1: `HANDSHAKE-CLOSE-BY` is **ENFORCE** in
R2's sense — print, never block — built on both sides rather than proposed and,
independently, by the same structural placement; and the `Frame retries:` →
`Retry limit:` rename has their **assent**, with their parser already accepting
both labels permanently so our first shipped rip log does not fail their
completeness sweep. `tools/release-gate.py --release-gate` now exits **0**.

**`docs/golden-reference.log` and `docs/golden-reference.diagnostics.json` are
generated by `c8c192f` and committed in the commit that carries this entry** —
named here rather than in a lap because the lap that would have named it is
`round-20-lap-03.md`, and releasing it made it immutable. That is the ordinary
case from now on: **a release commit moves the compiled-in `Handshake:` line, so
the artifact it stales can only be named in the changelog.** Banner:
`cyanrip 0.9.4-rc2+platterpus.12 (platterpus-fork-gc8c192f)`, and the line it
now carries reads `round 20 lap 3 closed, verdict GO -- NOT a released build` —
**closed** is the round and **NOT a released build** is this build, which are
two different facts and are supposed to be able to disagree.

**The round in one line each:** their §G asked whether our close-by reporter
flags rounds 13 and 14 as set in lap 2, and the answer was **no — a
disagreement, not an elision**: ours says lap 1, theirs says lap 2, on files
that are byte-identical in both trees, and the record says lap 1. They found a
real defect in ours by publishing their output unabridged beside ours — round 8
lost a provenance row to an early return, and fixing it exposed an `elif` hiding
the same thing one level over. Both fixed, two regression tests, each
revert-proved alone. And a lap-count table drafted from memory turned out to be
wrong: rounds 15 and 16 took **16** and **17** laps, so round 14's reform failed
its own falsifiable test twice before convergence began at round 17.

**Queued for round 21, deliberately not landed inside round 20:** the
`Retry limit:` rename, and the `Ripping errors:` footer placement at
`cyanrip_main.c:2690`. The close is what authorises a change to contract
surface, and landing either sooner would have falsified lap 3's own
`HANDSHAKE-PIN-POLICY` line — across `fe4d2c4..3121764`, exactly one commit
touches `src/` and it changes zero non-comment lines.

**A SETTLED ROW THAT HAD NEVER RUN, FOUND BY TRIPPING OVER A NEW ONE.** Adding
a row whose check contained a regex `|` split it into five cells, and
`check-settled.py` **silently skipped it** — `cells()` returned `None` both for
prose and for a broken row, and the caller `continue`d. Not reported, not
counted, while the summary went on saying *"0 stale"*. **That is the failure
this file's own docstring names**: *"a row nobody can run is indistinguishable
from a row that passes."*

Fixing it surfaced a **second** row that had never run since it was written —
the published-pair row, whose `||` was unescaped. It claims eight logs verify
and nothing had been checking it; it passes now that it does. **77 runnable,
0 stale**, up from 74.

`cells()` now returns three things — prose, two cells, or `MALFORMED` — and a
wrong cell count is reported with its own message rather than collapsed into
*"no command"*, because the two need different fixes. The legend above the main
table is a genuine three-column table and is exempted by keying the check to
the main table's own `| fact | check |` header, which is the document's
structure read from the document rather than a list of exempt lines.
Revert-proved both ways: with the fix, a re-broken row is NAMED and the count
drops 77 → 76; with the fix reverted, the same row goes silent at 76. Files
restored byte-exact.

**THE 2026-09-15b SESSION IS FILED — `0.6.49` (`c57025e`) on the same pin**,
`docs/rig-2026-09-15b-fe4d2c4/`. The MP3s and WavPacks are present, so the
absence audit is clean. **Their reporting fix works, checked against their code
before saying so**: the new `superseded` gate state fires on three of eight
rips, and the three that still show `"ran"` beside a null block are each flagged
by their own backstop as `verification_result_missing`. We nearly filed "still
broken".

**THEIR SECTION F INHERITED ITS GOAL, AND WE HAD THE EVIDENCE AND MISSED IT.**
Platterpus found it in their own acceptance script: section F is the FAST
whole-disc rip, N the uniform secure re-read, and F set no `rip_goal` — so on
the 2026-09-15 `12:01` run, which began with the config already on `archival`,
**F and N ran the identical test.** Six hours twenty-one proving one thing
twice, and the `fast_verified` whole-disc path — the default most users have —
got **no hardware coverage at all**. Every section passed, because nothing
asked.

**Confirmed here with one grep of the two `Invoked as:` lines we filed
yesterday**: in `12:01` both carry `-r 3 -Z 2` and 14 `Scope:` lines; in `00:58`
the full-acceptance rip carries **no `-Z`** and zero. Our session README noted
the change — *"now has `noconv=1`"* — and moved on. **A test's own invocation
changing between runs is a question, not a detail, and `-Z` appearing where it
had not been is a coverage LOSS in the costume of a coverage gain.** Third time
this week a difference was read without asking what it meant.

Nothing is retracted: every measurement stands, and the eight-session
convergence table does not depend on it. But the `12:01` session is **one
whole-disc test observed twice, not two**, and the session README and round 20
§1.6a now say so.

**AND `Ripping errors:` IS READ BY THE CONSUMER WE HAVE, not by a hypothetical
one.** §5.4 called the risk *"a log-only consumer"*; verified from their source
instead — `platterpus@d43b8cd:src/platterpus/parsers/cyanrip_log.py:431`
compiles `^Ripping errors:\s+(?P<count>\d+)` and `:1723` registers it as a line
rule. **And they have already routed around that field once**:
`parsers/rip_log.py:187` carries their 2026-07-01 finding that the count *"stays
0 even when a track never converges"*, which is why they trust
`secure_rerip_converged` instead. **So the field has under-reported for two
independent causes** — non-convergence, theirs in July; an encoder failing after
the footer is written, ours this week. One name, two scopes it does not cover.

**THE SHARED COMMAND SHEET'S THREE WRONG ROWS NOW LIVE IN ONE PLACE.**
`docs/seam-commands.md` publishes §7's *"Every value either took effect or was
refused with a message"* (49 of 111 rows were graded from exit status alone),
`-p '99=drop'` as accepted at line 504 (the binary refuses it), and `-D` as an
output directory at line 97 (it is `folder_scheme`, a **relative** scheme).
They were recorded in two different files, one of them an 1,100-line standing
status — which is how a set of three reads as three unrelated one-offs instead
of a document to fix. **None is fixed and not for want of knowing the answer**:
the file is shared, a one-sided edit is how two copies of one spec come to
disagree, and they go in together at the next joint version bump. **Deliberately
not added to round 20** — R1 fixes the conditions at lap 1, and none of the
three breaks anything in `fe4d2c4`.

**AND THE TABLE THAT WAS WRONG TWICE IS NOW DERIVED-CHECKED.**
`sc_cache_table_matches_the_transcripts()` resolves every row of the
cache-probe table to `docs/rig-<row>-<build>/session/transcript.txt` and
compares both figures, **in both directions** — a row naming no session fails,
and a session with no row fails. The second direction is the one that catches an
omission, and an omission is what actually happened, twice. It found a real
defect on its first run: the table labelled a session `2026-09-15a`, which
resolves to nothing; the rows now name their directories.

Revert-proved by deleting a row and watching it name the missing session. **And
one claim in its docstring was refuted by its own revert-proof** — it said the
`(?<!un)` lookbehind was load-bearing, but the pattern reads both figures in one
anchored match and passes without it. The lookbehind stays, and the docstring
now says it is belt-and-braces rather than claiming a guard it does not need.

**THE NETWORK IS OUT OF THE GATE.** `SETTLED.md`'s AccurateRip row re-checked a
fact about **our own parser** by calling `accuraterip.com` — 80.2 s on
2026-09-13, **38.1 s** on 2026-09-15, a factor of two decided by somebody else's
server, and it has timed `Settled facts` out. The run is now filed verbatim at
`docs/accurip-probe.log` and the row's check asserts the **claim against the
artifact**: edit the row's numbers without re-running the probe and it fails.
`check-settled.py` drops from ~100 s to **54.6 s** — 45% of the timeout instead
of 84% — and no verdict in the suite depends on a third party being up.

**The first check written for it was near-vacuous and the revert-proof caught
it.** `--toc-only` re-derived the reference TOC; pointed at a *different*
session's log it returned the same `14 268707`, because every session is the
same disc. The flag stays as a tool affordance; the check moved to the
comparison that can actually fail.

**The real fix is deferred for a named reason.** Asserting the parser against a
recorded response needs the parse split out of `crip_fill_accurip()`, which
touches `src/` — and round 20's `HANDSHAKE-PIN-POLICY`, plus Platterpus's
round-19 §F1, rest on the span from `fe4d2c4` containing exactly one `src/`
commit changing zero non-comment lines. R4 says fixes queue; this one queues
until the round closes.

**TWO LIVE CLAIMS WERE INCOMPLETE IN THE SAME WAY, AND THE AUDIT FOUND BOTH.**
The cache-probe table said *"three rig runs"* and then four; **eight** filed
sessions carry a `Cache probe:` line, and the missing rows were hiding the
strongest datum — 2026-09-05 sits at **95%** of its threshold, so
`CACHE_HIT_RATIO` comes within five percent of swinging the answer on noise
rather than merely by a factor of sixteen on its third significant figure. Four
runs calibrate `miss_cost` to within **0.7 ms** of each other and their verdict
margins still split into 68% and 90%, which turns an argument from the source
into a measurement. `CLAUDE.md` carried the same *"all three post-chunking
runs"* and is corrected.

The `-Z` convergence rows named four sessions; **eight** have a
`secure-reread.log`. Derived whole: track 3 has produced **four** distinct
values and track 5 **two**, and `3D8FCF0C` is reported **converged twice and
not-converged three times**. That settles *convergence status and the reported
checksum are independent* over sixteen track-observations instead of two, and
it is sound across builds because `src/checksums.h` is byte-identical across
all four and no commit between them touches `last_checksums`, `total_repeats`
or `max_retries` — checked, not assumed.

**Both were right in direction and short on evidence, and both were found by
scanning `docs/rig-*/` rather than by adding the sessions anyone remembered.**
The first derivation of the cache table also read `uncached read` into the
`cached` column — **`uncached` contains `cached`** — and produced a margin of
exactly 400% on every row, which is what a nearly-matching pattern looks like
when it is wrong plausibly.

**AND IT FALSIFIED A CLAIM WE HAD ALREADY WRITTEN DOWN TWICE.** Two sessions
supported *"the single differing track is exactly the track whose convergence
status differed"*; in `0.6.49` track 3 did **not** converge and reported the
value it produced under `0.6.48` when it **did**. **Convergence status and the
reported checksum are independent.** Same shape as the paranoia-sum
"invariant" — a correlation that held on every case anyone had constructed is
not a rule. 13 of 14 tracks identical across all three sessions stands, and the
`SETTLED.md` row is rewritten rather than deleted.

**Their `.platterpus-addendum.txt` is new and respects the contract exactly** —
a separate file *"so that `cyanrip --verify-log` still verifies"* our log
byte-exact. Its re-read of track 3 turned a `450`-only partial match into
`Accurip v1 — accurately ripped, confidence 128`: **track 3's offset-variant
reading was a read artefact and track 5's is not**, two tracks that looked like
one phenomenon. Neither addendum carries a timestamp — round 8 `J14`,
re-confirmed against the newest artifact rather than remembered.

**ROUND 20 OPENED, HELD, on 2026-09-15** — `docs/handshake/round-20-lap-01.md`,
`HANDSHAKE-READY-TO-READ: no` until the operator announces it. It carries a
`HANDSHAKE-CLOSE-BY` **in lap 1**, which is the correction Platterpus's
round-19 §E asked for. Two close conditions, fixed at lap 1 and not growing.
The pin is still `fe4d2c4`. The span drifts with every commit, so it is quoted
against one: **57** commits at `d34a0c8`. **The claim that does not drift is the
one that matters — exactly one commit in the whole span touches `src/`, and it
changes zero non-comment lines.**

**The golden reference was regenerated because opening the round moved the
compiled-in `Handshake:` line** to *"round 20 lap 1 OPEN, verdict OPEN -- NOT a
released build"*. **Generated by `54f2d35`**, the commit that adds the lap;
**committed at** the commit that carries this paragraph, which is the next one
(`git log -1 -- docs/golden-reference.log`). Those two can never be the same
commit — an artifact cannot contain the hash of the build that produced it —
and naming only one of them is what laps 12 and 14 got wrong in round 7. No log
line was reworded; the only other deltas are timings and `creation_time`.

**THE ROUND TABLE IN `docs/handshake/README.md` WAS STALE IN A WAY ITS OWN
CHECK COULD NOT SEE.** `sc_docs_do_not_contradict_themselves()` asserted that
every round in the record appears in the table — so the row
`| 19 | **OPEN** | ... | round-19-lap-01.md |` passed, listed and wrong about
both the state and the file, after round 19 had closed `GO`/`GO` at lap 3. A
reader following that cell reads the opener and never sees the close. Found
only because adding round 20 made the presence check fire and the row above it
turned out to be stale too. **The check now also derives each round's newest
lap from the filenames on disk and requires the row to name it**, wildcard rows
included. Revert-proved: restoring the stale cell fails with the round-19 row
named.

**THE GATE NOW PRINTS `HANDSHAKE-CLOSE-BY`, which R2 has required since it was
written.** `close_by_lines()` is deliberately kept out of `check()` rather than
added there behind a guard — R2 says a gate *prints* and **never enforces**,
"because enforcement lets a clock skew block a release", and a rule that must
not affect a verdict is safest when it cannot reach the code that forms one.
`test_close_by_never_enforces()` asserts that a `CLOSE-BY` eight months past
changes neither the verdict nor the problem list.

What it says about our own record is why it was worth building: **rounds 15–18
declared none, round 19 set one in lap 3 rather than lap 1, and round 8 lap 7
declared a bare date** — reported as `unknown (a bare date names no timezone)`
rather than assumed to mean midnight, because R2 forbids that form by name.

**Two things the first version got wrong, both found by running it.** It
reported five "extensions" in round 9, all of them the identical instant — every
lap carries the whole wire header, so re-declaring the same value is the header
working and only a *different* value is an extension. And the bare-date test
**passed with the bare-date guard reverted**, because `fromisoformat` accepts a
bare date and returns a naive datetime the `tzinfo` check then refuses, so
`"timezone" in why` matched both messages: a pattern that matches both branches
asserts nothing about either. All three behaviours revert-proved one at a time,
each with the edit confirmed landed and the file confirmed still parsing.

**`tools/seam-sync-check.py --fetch` was shallowing the peer clone it warns
about.** It ran `git fetch --depth 1 origin HEAD`, and that makes a full clone
shallow — measured on a throwaway repo, not reasoned about: 5 commits become 1
and `is-shallow-repository` flips to `true`. So the tool manufactured the exact
condition the warning fourteen lines below it exists to catch, and a `--fetch`
would have undone the `fetch --unshallow` that repaired that clone the day
before. **A tool that creates the hazard it warns about is worse than one that
does neither.**

**THE 2026-09-15 RIG SESSION IS FILED, AND IT IS THE FIRST TIME TWO SESSIONS
HAVE SHARED A PIN.** `docs/rig-2026-09-15-fe4d2c4/`: Platterpus `0.6.48`
(`a7fdf98`) driving `fe4d2c4`, 242 recorded steps, 241 pass, 0 fail, 0 error,
0 unreachable, 1 info — **and it was NOT a pass.** Platterpus reported later
that day that no MP3 and no WavPack were written; confirmed from the bundle,
where `gates.derived` reads `"ran"` beside a null `verification.derived` on
both, and `MANIFEST.txt` lists no `.mp3` and no `.wv` for them. **That file
exists so an absence is readable and we had quoted it without asking it what
was missing** — a completeness verdict taken from the runner under test. Our
own half of the session stands; it is derived from our logs and checksums, not
from their verdict. All eight logs verify with our own `-Y`, run here, at a
build 54 commits later (`411c80a`) — which also proves the filing altered no
byte.

Because the pin did not move between their 09-12 and 09-15 sessions, the pair is
a reproducibility experiment nobody designed: **13 of 14 tracks carry the
identical `EAC CRC32` and both AccurateRip checksums across three days, and the
one that differs is exactly the track whose convergence status differed.** Track
5 hit the repeat limit in *both* and reported the same checksum in both — so
`did NOT converge` says *no two reads within the limit agreed* and entitles a
reader to nothing further. Both arms now exist on one disc in one week.

**`Ripping errors:` IS WRITTEN BEFORE THE ENCODERS ARE ASKED HOW THEY DID —
found because Platterpus sent us three portable shapes from their own code and
we ran them against ourselves.** Their first, *a completeness field computed
from the REQUEST read as the OUTCOME*, is in our log. Demonstrated rather than
argued, in `sc_encode_failure_is_absent_from_the_log()`: cap every write at
32 KiB, rip `mixed.cue`, and the muxer's trailer write fails. The failure **is**
caught — `-j` reads `ripping_errors: 2` and we exit `1` — but the log, written
moments earlier, says `Track 2 ripped and encoded successfully!`,
`Ripping errors: 0`, `Rip completed:  yes`, names a file truncated to 32768
bytes from 253742, and signs it with a `Log FUN512:` that `-Y` accepts. **The
diagnosable lines are in the logfile SIX LINES ABOVE that zero** — `Error
writing trailer: File too large!` at line 204, `Ripping errors: 0` at line 211
— so that rule held; what failed is that no FIELD reflects them, and a parser
grades fields.

**Two records of one run disagreeing, with the human-readable one wrong** — the
changelog-versus-ledger shape from two days ago, one document over. The
placement is deliberate (`cyanrip_main.c:2686`: *"moving it below would
silently fold encoder failures into a contract line"*) and that reasoning is
right; what went unrecorded is that the log then contradicts the next file the
same program writes. **The fix is one line and is not made here**: it changes
what a P2 contract line counts, so it is a handshake proposal. The scenario
pins the defect and is proved to SEE the fix — moving the footer makes it fail
with *"the log and -j now AGREE (2)"*, build green throughout, source restored
byte-exact.

**AND THEIR SECOND SHAPE SWEPT RATHER THAN ASSUMED.** Their sharpest sentence
was *"It was itself a fix from an earlier incident, which is why nobody
re-asked can this be satisfied by finding nothing? of it."* So an AST pass over
every `sc_*` scenario found the ones where **every** `fail()` sits inside a
loop — the shape that passes on an empty population. **Five flagged, all five
safe on inspection**: two iterate literal tuples, `sc_interrupt` already
refuses with *"no per-track state in the record"*, `sc_artifacts_are_tracked`
is a hardcoded list carrying *"A glob over docs/ would pass by finding nothing
if the directory moved"*, and `sc_info` also asserts outside its loops.
**`unknown (swept, found none)`, not `none`** — the scan is a heuristic over
one file.

**`Frame retries:` NAMES HALF OF WHAT `-r` DOES — found, recorded, and
deliberately not fixed.** Line 18 of that session's `secure-reread.log` says
`Frame retries:  3`; line 425 says `did NOT converge after 3 reads (repeat limit
hit)`. Both threes are the same knob: `-r` is passed to
`cdio_paranoia_read_limited()` (`src/cyanrip_main.c:534`) *and* used as the
repeat-loop ceiling (`:1011`). **The generated contract is right and the
hand-shaped log label is what under-states** — P1 carries genopt's own "for
frames and repeated rips" because P1 is derived from `--help`. `-j`'s
`"frame_retries"` has the same gap. It is a P2 contract line, so the rename is
a handshake proposal and not a drive-by reword; `docs/KNOWN-ISSUES.md` carries
the entry and round 20 §3 carries the exact proposed text.

**A FOURTH CACHE PROBE, AND IT IS A CONTROLLED ONE.** 2026-09-10 and 2026-09-15
calibrated `miss_cost` at 363.2 ms and 362.6 ms — 0.17% apart — and classified
reads at 82.0 ms and 61.7 ms, 25% apart, moving the margin from **90%** of
threshold to **68%**. So the margin's variance comes from the read being
classified, not from the calibration. That `CACHE_HIT_RATIO` is coincidental
rather than calibrated was argued from the source in August; it is now measured.
Still deliberately unfixed — the shape of the correct fix changed the moment the
magnitude arrived.

**Two findings withdrawn before filing, recorded so the silence is not read as
nothing having been looked at.** `Accurip 450:` is identical across both
sessions on the tracks whose other checksums differ — read from
`src/checksums.h:74`, it accumulates over **one sector** (588 samples at sector
450), which is expected and is why the value carries "partially accurately
ripped". And our `Invoked as:` naming `/usr/local/bin/cyanrip` where every
Platterpus record names `/home/rmccann/.local/bin/cyanrip` is a
`distrobox-enter` shim they probe on every run and record in their own report.

**ROUND 19 CLOSED `GO`/`GO` on 2026-09-14**, on the unchanged pin `fe4d2c4`,
against `platterpus 0.6.47` at `abd2eb8`. A procedure round: no build reviewed,
no hardware, no behavioural change. `tools/release-gate.py` now prints
*"Release allowed: every round is closed"* — **which is not an instruction to
release.** Nothing has changed in `src/` since `fe4d2c4` but comments, so there
is nothing to put in a release; the gate says only that nothing is blocking one.

**The golden reference was regenerated TWICE for this round, and both were
forced by the same fixpoint.** Adding lap 3 moved the compiled-in `Handshake:`
line to *"round 19 lap 3 OPEN, verdict GO"* (generated by `dda4318`); releasing
it moved the same line to *"round 19 lap 3 closed, verdict GO"* (generated by
`bd1f43a`). **Closing a round changes the binary**, so the artifact that records
the binary cannot be written until after the close — each regeneration is its
own commit, whose parent is the build it names. No log line was reworded either
time.

**THREE READERS OF ONE RULE IN THIS TREE DISAGREED TWO WAYS, and Platterpus's
lap 2 is what made us look.** Their §B4 asked whether our lap enumerator selects
by filename, as theirs had. It does not — `tools/round-digest.py` runs §5a's
content test. **It was being defeated anyway:**

- **`round-digest.py` counted well-formed VALUES and called it counting
  DECLARATIONS.** `ROUND_RE` requires `(\d+)$`, so a second declaration reading
  `HANDSHAKE-ROUND: not-a-lap (transport envelope)` matched nothing and
  `len(m) != 1` never fired. Their envelope declares all three identity fields
  twice and `is_a_lap()` returned `('18','2','platterpus')` on it — **the
  disclaimer written to say "this is not a lap" was invisible to the check it
  was written for.** Only the `envelope-` filename kept it out of our digest,
  which is the exclusion §5a forbids, reached from the opposite direction.
- **`release-gate.py` had the same counter**, and setting `lap = None` was not
  enough — that only sorts the file last and the round still closed. It now
  refuses the verdict as `AMBIGUOUS`, failing closed loudly rather than
  excluding the file silently.
- **`tests/release_gate.py`'s name check read the RAW file**, while §5a applies
  its test after fenced blocks are stripped and the gate itself strips them. So
  the gate conformed and the test checking the gate's record did not. Found by
  writing lap 3, which quotes the envelope in a fence to explain the first two —
  the exact case Platterpus's §B3 says they wrote a test for.

**Every digest in the record is unchanged, same as theirs.** The naming
coincidence held for every lap either side has committed, so all three were
latent. **Our round-19 digest over 1 lap is `d261f77040b90ba9`, byte-identical
to what their independent implementation computed** — two implementations, one
number, neither copied.

**And the first test written for it was decoration.** Reverting the gate fix
alone left the suite green. Two tests now, one per tool, each revert-proved
individually and parse-checked during the revert so a syntax error could not
masquerade as a failing test.

**`HANDSHAKE-CLOSE-BY` has been dead for five rounds on both sides, and
`EXPIRED` is therefore unreachable.** Platterpus found the field missing;
measured here it is absent from every rounds-15–19 lap and from all three of our
gate/test files, while present in rounds 9, 10, 12, 13 and 14 — it decayed.
**The consequence they did not name: `PROTOCOL.md:281` defines `EXPIRED` as
*"`CLOSE-BY` passed with no terminal state reached"* and `:292` makes
`OPEN → EXPIRED` legal, so a terminal verdict state in the shared spec cannot be
entered.** It exists because round 7 ran to 36 laps. R2 also says a gate
*prints* whether `CLOSE-BY` passed. **Ours did not, because it did not parse it.
It does now — see the round-20 entry at the top of this section.** Left in place
rather than rewritten, because this paragraph is what round 19 found and the fix
is what round 20 did; a file that quietly edits its own history is worse than one
that says what changed.

**`+platterpus.12` HAD NO CHANGELOG HEADING FOR TWO DAYS, and the operator
found it by reading the file and asking why we were still on `.11`.** The
release shipped 2026-09-12 at `fe4d2c4`: `release-ledger.tsv` gained row 22,
`release-manifest.json` resolved both channels to it, and `meson.build` carried
the version — while this file's newest heading still said `+platterpus.11` and
`.12`'s notes sat under `Unreleased`. **Three machine-read artifacts against one
human-read one, and the human-read one was the one that was wrong.**

The layout is why. `009a573` prepended the `.11` heading ABOVE `Unreleased`
instead of below it, leaving `.11`'s own notes in the unreleased block; `.12`
then shipped with no heading at all. Corrected here: `Unreleased` is first,
`.12` and `.11` follow in `release_seq` order, and every line moved was checked
byte-for-byte against `git show 009a573:Changelog.md` so no claim about which
release contained what was invented in the move.

**The ledger and the manifest moved with the release because both are
mechanised — the ledger is append-only and the manifest is generated and
`--check`ed. The changelog heading was prose, and prose enforces nothing.**
`tests/rip_images.py` `sc_changelog_names_every_release()`, registered in the
same commit, derives the expectation from the ledger: every published row needs
a heading, the newest row must be the first heading, and headings descend by
`release_seq`. It asserts against the heading's POSITION, not the document —
`.12` is named twenty times in this file, so a substring check would have been
satisfied by the file being wrong, which is `sc_status_is_current()`'s defect
verbatim. Revert-proved on four branches: the missing heading fires two
messages, the `009a573` layout fires the ordering check, an inverted pair
deeper in the file fires it too, and a changed ledger format fires the vacuity
guard rather than silently passing.

**Golden reference regenerated for round 19 lap 1: generated by `71626e8`,
committed at `c57c9c3`.** Two commits, never one. Opening the round moved the
compiled-in `Handshake:` line; no log line was reworded.

**ROUND 19 OPEN.** Two close conditions, fixed at lap 1 and neither needing
hardware: whose `SKIPPED`/`BLOCKED` tokens move, and whether a transport
envelope counts as a lap under §5a — our two gates disagree about the second
today, and a digest over different bytes is the one thing §6a-ter says no
override may excuse. The pin does not move.

**A full documentation audit found this repository's own rules rotting three
ways, all weeks old, none caught by anything — and all three are now
executed rather than written down.** `tests/rip_images.py`
`sc_docs_do_not_contradict_themselves()`, registered in the same commit:

- **A rule declared RETIRED still stood as a live instruction.** `CLAUDE.md`
  stated *"send a file every round even when nothing changed"* in the seam
  section and retired that exact rule a thousand lines below, where the
  round-14 reform records it as the reason two lap 13s crossed. The check
  **derives** the retired set from the reform's own verbatim quotes.
- **A status table went stale in the precise way its own warning described.**
  `docs/handshake/README.md`'s round table stopped at 13 through five closed
  rounds while claiming *"every round is closed"* and naming a release
  superseded twice — having already gone stale once, stopping at *"round 7 is
  open"* through five closed rounds. **The warning is the defect**: it makes a
  table feel handled and changes nothing.
- **The consumer map never named the laps.** Harmless while laps were mailed;
  under pull transport the lap path *is* the transport.

**Revert-proved on four branches, one at a time, restored between** — including
rewording the reform so the extraction matches nothing, which fires a guard that
says so. A check that cannot fire is worse than a missing one, and that fourth
branch is what proves this one reports when its own derivation rots.

**Also written down for the first time: where WE read in THEIR repository.** It
lived only in one session's scrollback. Their laps are
`docs/handshake/outbound/` under a different naming convention from the agreed
one, their standing status is `outbound/platterpusstatus.md` rather than
`STATUS.md`, and their protocol copy is at a different path from ours. Measured
with a fresh fetch: **their round-18 lap 2 is not in their public repository**,
so reading works both ways but publishing does not yet on their side.

**Four documents corrected**: `CLAUDE.md` (three self-contradictions and one
disagreement with `SETTLED.md` — it said `-x` alone had never been shown while
row 87 records it settled 2026-09-11); `docs/handshake/README.md`;
`docs/handshake/STATUS.md` (still said round 18 was open);
`docs/RELEASE-PLAN-platterpus.12.md` and `docs/BETA-NOTE.md`, both given dated
outcome headers with bodies kept verbatim — a plan is evidence of intent and a
beta note is the only account of a build that existed.

**Golden reference regenerated for round 18 lap 3: generated by `08b9e0f`,
committed at `5653a3d`.** Two commits, never one — a generated artifact cannot
contain the hash of the build that produced it. Laps 2 and 3 moved the
compiled-in `Handshake:` line, which the reference carries; no log line was
reworded and no behaviour changed.

**ROUND 18 CLOSED at lap 3, `GO`/`GO`, three laps.** The tiered acceptance
specification is agreed. The pin did not move and no release was cut: the round
decided a procedure, not a build. `release-gate.py` now reports every round
closed.

**Their lap 2 found a silent inversion and it is the finding of the round.**
Verified from their source rather than accepted —
`platterpus@abd2eb8:src/platterpus/uiscript/report.py:32-33` — their `SKIPPED`
means *never reached (the batch aborted before it)* and their `BLOCKED` means
*refused: needs the escape hatch the user has not enabled*. **The two tokens are
swapped against ours**: our `SKIPPED` is a decision and theirs a consequence.
Our own standing status had told them *"we would rather adopt your word than
mint a second"* — acting on that in either direction inverts both meanings in
transcripts that stay well-formed, and no gate on either side could have seen it.
The remedy we proposed would have caused the defect it was aimed at. Adopted
their structural fix: **the spec names the concept and the token separately.**

**The vocabulary is seven concepts, not five.** Their `ERROR` (the harness
failed, not the script) closes the hole our ordering rule was built to work
around; their `INFO` (a step that gathers rather than asserts) is load-bearing
for tier 4, which we proposed without noticing it needed a state. Each of the
seven implies a different action.

**We filed their envelope as their lap and our gate graded it 14 OK, 0 FAIL.**
The 31,949-byte transport envelope went in before anything was extracted, on a
file whose own preamble says *"it cannot be counted as a lap."* Reading the
preamble caught it; the gate did not. Corrected — the extracted part is 30,287
bytes and verifies to their manifest's sha256. Had it stood, our next digest
would have covered different bytes from theirs, which §6a-ter says no override
may excuse.

**The cause is an underspecified spec, not a wrong implementation.**
`round-digest.py` spells the matchers `HANDSHAKE-LAP:\s*(\d+)`, so their
envelope's `not-a-lap (transport envelope)` markers do not match at all and the
file reads as declaring each field exactly once. Their `emit_envelope.py` asserts
the opposite. §5a's exactly-once test has two defensible readings and both gates
pass their own suites. Round 19's.

**§8 has 37 rows, not 36 — `C13a` is the extra**, and it is the row most easily
missed by a counter written for `C[0-9]+`. Reported to them before their coverage
ratchet lands on the wrong denominator. **Ours names 36 of the 37; `C13a` is
named by nothing of ours**, and `release-gate.py`'s docstring still describes the
v2 reopen behaviour that C13a's v3 text changed — a stale documentation claim,
**not** a demonstration that the code is non-conformant. Round 19's.

**Standing status rewritten for round 18, carrying a supersession our lap 1
cannot.** A sent lap is immutable, so when operator direction on 2026-09-13
withdrew lap 1 §2's *"only when the tier below has passed"* clause and replaced
§3's three states with five, the correction had nowhere else to live.
`docs/handshake/STATUS.md` is the channel that exists for exactly this — round
12's precedent, used by the other side first.

**No lap file was written for it, deliberately.** A lap is counted by the
enumerator the moment it exists on disk, so drafting one unsent would have put
it in our `HANDSHAKE-ROUND-DIGEST` and the compiled `Handshake:` line while the
consumer holds no such lap. Round 18's digest is unchanged at
`0200464c2dfd0386 over 1 lap(s)`. `tests/release_gate.py` records the cost of
getting this wrong: round 14 carried two lap 2s and two lap 5s across four
crossings, because *"the number is chosen when a lap is WRITTEN and the
divergence appears when it is not immediately sent."*

**`src/cache_probe.c`'s header comment contradicted itself for a month.** It
said `cd-paranoia -A` *"has not been run"*; the block thirty-five lines below
quoted its 137-sector result. Blame dates them one day apart. The same block
said *"a miss at 64"* where the lower one says the 64-sector read FAILED — two
different claims about the drive, collapsed in the header of the very commit
that taught the composer to separate them. Corrected at `7b2fda6`; contract
regenerated at `5d29b08` (generated by `7b2fda6`), source anchor
`c8bbf607d499ba2d` → `2a3d4f2934b39d6a`. Comments only; no behaviour change.

**The `-x` calibration question is answered, and the answer moved the fix.**
`docs/KNOWN-ISSUES.md` predicted that one rig run showing *"an uncached read in
the hundreds of milliseconds beside a cached read of a few"* would confirm the
diagnosis and make the fix arithmetic. Three runs (`978f9b0`, `ddc1e8c`,
`fe4d2c4`) confirm the first half and **falsify the second**: 42–82 ms, not 2.2.
`last_hit_us` is overwritten on every hit, so the printed figure is the re-read
after the 2048-sector run — the longest backseek the search performs — while
2.22 ms came from a 1-sector run. They were never the same measurement. One
fixed `miss_cost` cannot be right at both ends, so the fix is not arithmetic and
is still not made. **Raising `PROBE_MAX_SECTORS` is not it either**: the search
is stopped by the comparison, not the limit.

**Golden reference regenerated for round 18 lap 1: generated by `f4f33b1`,
committed at `f1f1a98`.** Round 18 is a procedure round: it proposes a tiered
acceptance specification and does **not** move the published pin, which stays
`0.9.4-rc2+platterpus.12` at `fe4d2c4`.

0.9.4-rc2+platterpus.12 — 2026-09-12 — **stable**
=================================================
**Published 2026-09-12 at `fe4d2c4`, `release_seq` 22, both channels.** This
heading was missing for two days: the release went out, `release-ledger.tsv`
row 22 and `release-manifest.json` both named it, and this file's newest
heading still said `.11` — so the one artifact a human reads disagreed with the
three a machine reads. Added 2026-09-14 with the notes below moved out of
`Unreleased`, where they had been left. `sc_changelog_names_every_release()`
now derives this check from the ledger.

**RELEASED: `0.9.4-rc2+platterpus.12` at `fe4d2c4`, seq 22, authorised by
handshake round 17 (`GO`/`GO` at lap 3, against `platterpus 0.6.46` at
`45663c3`).** Both channels resolve to it; the published build stops being
`978f9b0`. Verified from a clean detached worktree before the verdict — 81/81,
0 fail — which is the check `+platterpus.5` failed.

**Golden reference regenerated for lap 3: generated by `393e8dc`, committed at
`034e08b`.** Does not move the published pin: `fe4d2c4` is internally consistent and
stays what the manifest names.

**Golden reference regenerated for round 17 lap 1: generated by `2c168dd`,
committed at `06aa79f`.** Opening the round moved the compiled-in `Handshake:`
line, which the reference carries — so the candidate's artifact set moves with
it. Which commit is finally published is settled in the closing lap, once the
artifacts have stopped moving; a round file cannot name a build containing
itself.

**RELEASE CANDIDATE `0.9.4-rc2+platterpus.12`.** Version bumped at `6a9a080`;
every derived artifact regenerated at `a2523c4`, which is generated by `6a9a080`
and is the first commit where the version and all derived artifacts agree. That
property is what the announcement names — naming the bump instead is how
`+platterpus.5` shipped a commit failing 2 of 33 from a fresh clone.

**NOT PUBLISHED.** `release-ledger.tsv` has no new row and
`release-manifest.json` still resolves both channels to `978f9b0`. Round 17
reviews this candidate against `platterpus 0.6.46`; publication is what its
close authorises, and the two are released together for testing as one pair.

**The contract moved in its banner and nowhere else.** `contract-delta` reports
"No section changed": no P1 flag, no P2 line, no P4 exit code and no P5 message
differs from `.11`. What a consumer sees differently is the delta from the
PUBLISHED build `978f9b0`, recorded below, and three rows of it are breaking.

**Consumer-observable delta since the released pin `978f9b0`, for the next
release announcement.** Derived by reading the diff and re-checked command by
command; each row names where it was read. Three of these change the shape of
something a consumer parses and must be announced as breaking, even though all
three were already declared in round 16 lap 1 §C and handled on their side.

| surface | change | where | breaking |
|---|---|---|---|
| `creation_time:` and `Ripping finished at` | gained a `±HH:MM` UTC offset — `2026-08-26T23:15:06` became `2026-09-11T12:25:49+00:00` | `8d465f1`; both forms readable in `git show 978f9b0:docs/golden-reference.log` vs the current one | **yes** for a consumer using `%Y-%m-%dT%H:%M:%S` |
| `-j` record `schema` | `cyanrip-diagnostics/3` → `/4` | `git show 978f9b0:src/diagnostics.c` vs `src/diagnostics.c` | **yes** for a schema allowlist |
| `-j` record fields | `started_at` and `finished_at` added, always present | `started_at`: 0 occurrences at `978f9b0`, 6 at HEAD | no — additive |
| `Error parsing string: %s!` | **removed**; invalid UTF-8 now substitutes rather than refusing | present at `978f9b0:src/naming.c`, absent at HEAD | **yes** for a consumer matching that string |
| `-a` / `-t` values | bare apostrophes escaped before `av_dict_parse_string()` | `crip_escape_bare_quotes`: 0 at `978f9b0`, 2 at HEAD | no — it fixes a value that was destroyed |
| curl transfers | every handle now bounded | `CURLOPT_TIMEOUT`: 0 at `978f9b0`, 1 at HEAD in `accurip.c` | no — additive |
| completion footer | now written on the `log_init`/`cue_init` failure paths | `a79ac9e` | no — additive |
| `-j` given twice | last path wins, and a line says so | `12f2081` | no — additive, and cannot fire for a caller passing it once |

**The CLI surface did not move**: no flag added, removed or renamed, and no
declared bound changed. `tools/probe-argv-surface.py --gate` reports 111 probes,
0 crashed, 0 refused-without-a-message, 0 silently ignored.

**HOW THIS WAS PRODUCED, because the method is part of the claim.** A
parallel audit was dispatched over eight surfaces; **37 of its 44 agents died on
a spend limit** and it returned `confirmed: [], breaking_count: 0`. That is not
"nothing found" — it is "every verifier died", and reading it as a clean result
is the trap `CLAUDE.md` already records from a fan-out that lost 16 of 17
agents. Seven audit lanes did return, and every row above was then re-derived
inline with a command. One lane drifted off its own subject entirely, and one
agent claim that looked wrong on first check turned out to be right and the
check too narrow — it reads `src/naming.c`, not `src/cyanrip_main.c`.

**Golden reference regenerated for lap 17: generated by `d1080a0`, committed
at `17fb4c3`.** **Round 16 is CLOSED** — the `Handshake:` line now reads
`round 16 lap 17 closed, verdict GO`, and `tools/release-gate.py` reports
"Release allowed: every round is closed" for the first time since round 14.

**Golden reference regenerated for lap 15: generated by `1316738`, committed
at `98e31c8`.** The `Handshake:` line now reads `round 16 lap 15 OPEN, verdict GO`
— still OPEN, correctly: our GO is one of the two verdicts a close needs.

**Golden reference regenerated for lap 13: generated by `84ce459`, committed
at `13d984e`.** Same mechanism as the entries below.

**`tools/round16-accept.py`'s clause-2 audio gate was `max` where it must be
`min`.** Round 16 lap 12 §C1: it fired only when NEITHER arm carried audio, so
one silent arm beside one real arm PASSED clause 2 — because the hashes differ
precisely when one side is empty. The `disabled` message separately asserted
`-A` as the cause, which the status line cannot establish (§C2:
`CYANRIP_ACCUDB_DISABLED` is `0` and `"disabled"` is the bare `else` of the
ternary at `cyanrip_log.c:786`); it now reads `Invoked as:` instead. A third
instance of the same class — a hash differing because one arm was TRUNCATED —
found here by asking what the passing fixtures had in common.

**Golden reference regenerated for lap 11: generated by `57e8a6e`, committed
at `6c7e4be`.** Same mechanism as the entries below. `docs/sample-interrupted.*`
regenerated in the same pass: the contract's diagnostics field table is derived
from the committed records, so a stale sample publishes an inventory that is
true of the sample and false of the binary.

**`tools/lap-commits.py` derives §C's commit list.** Round 16 lap 10 §D: our
lap 9 §C said "three commits since lap 8" and named no range. Platterpus
derived five from the send pin and nine from lap 8's `HANDSHAKE-FROM-COMMIT`;
two were missing, one of them the commit that corrected the Run A block. The
defect is the missing baseline -- a count naming no range cannot be re-derived,
so they had to guess two and report both. `tests/lap_commits.py` cross-checks
the tool against both of their numbers.

**Golden reference regenerated for lap 9: generated by `636b18e`, committed
at `28db713`.** Same mechanism as the entries below.

**Golden reference regenerated for lap 8: generated by `cff835a`, committed at
`69b008c`.** Same mechanism as the entries below.

**Golden reference regenerated again: generated by `c8a75b0`, committed at
`3d52cf9`.** Round 16 lap 6, the same mechanism as the entry below -- a lap file
moves the compiled-in `Handshake:` line and that line is inside the reference.
A lap cannot name its own child, so both numbers land here.

**Golden reference regenerated: generated by `a324ad0`, committed at `ffe32aa`.**
Two commits, and they can never be one. `a324ad0` is round 16 lap 4, whose own
commit moved the compiled-in `Handshake:` line from `round 16 lap 2` to
`round 16 lap 4` — and that line is *inside* the reference, so committing a lap
makes the artifact stale by construction. The reference's banner names
`platterpus-fork-ga324ad0`, its parent; the file itself lands at `ffe32aa`,
because a generated artifact cannot contain the hash of the build that produced
it. Named here rather than in a lap for the same reason: **lap 4 IS `a324ad0`,
so it cannot name its own child.**

Everything else that moved in it is the expected non-determinism — extraction
speed, elapsed time, `creation_time`. No parsed field changed.

 - **A DISK-FULL RIP IS RECORDED AS A CLEAN COMPLETE ONE.** Measured on a 64 KiB
   tmpfs: three tracks failed to write, three
   `Error writing trailer/packet: No space left on device!` lines printed, the
   process exited **1** — and the logfile says **`Ripping errors: 0`** and
   **`Rip completed:  yes (3 of 3 tracks)`**. The `-j` record from the *same
   run* says `ripping_errors: 3`, `exit_code: 1`.

   **Two surfaces, one name, one run, different numbers**, and the
   human-readable one — the archival record, the one a consumer parses — is the
   one that under-reports.

   **The placement is deliberate and documented**, which is why this is
   reported rather than fixed. `cyanrip_main.c:2690` carries a comment putting
   `cyanrip_log_finish_report()` *"BEFORE the encoder-status loop so that
   `Ripping errors:` counts exactly what it counted before — moving it below
   would silently fold encoder failures into a contract line"*. The encoder
   failures increment at `:2698`, after the footer is written; the `-j`
   snapshot at `:2708` is later still and sees the true count.

   **Not fixed, and it must not be fixed unilaterally.** Moving that call
   changes the value of a line Platterpus parses on a real failure mode — a
   handshake change, not a drive-by reword. Carried to round 16 lap 3 with a
   proposed remedy for their assent. `src/` has zero `ferror()` calls, which is
   the wider shape: audit lead (e), re-derived.

   The portable reproduction does **not** work: a directory where the `.flac`
   goes fails at encoder *init*, is correctly counted, and the footer correctly
   says `no (aborted, 0 of 3 tracks)`. Only a failure at *finalisation*
   separates the two surfaces, and that needs a filesystem that fills.

 - The references at lap 2 are **generated by `2b341dd`** — the lap-2 commit —
   and **committed at `b485c89`**. Never the same commit: a generated artifact
   cannot carry the hash of the build that produced it, and since r3 adding a
   lap file changes the binary, so the reference must come from the build that
   knows its own round state.

 - **ROUND 16 LAP 2 — a TEST PIN, `ddc1e8c`, so a rig session can happen without
   a release.** The operator wants both repositories converged on a physical
   test as soon as possible, and a release cannot be what converges them:
   `release-gate --release-gate` exits **1** naming round 16 as open, and that
   is not overridden. No ledger row, no manifest change, both channels still
   `978f9b0`.

   `PROTOCOL.md` §6a exists for this exact deadlock — a close needs hardware
   evidence, hardware runs the pinned build, and neither side may move the pin
   while the round is open, so the rig would forever run the build *without*
   the changes under review. A test pin breaks it and **never** closes a round;
   `test_a_test_pin_does_not_close_a_round()` asserts that and was re-run.

   **The test pin is the same program as the production pin**, quoted so it can
   be checked rather than believed: `git diff a9aedf0..ddc1e8c -- src/
   meson.build` is empty and `git rev-parse <ref>:src` is the same tree
   object `bc446254fce57c98…` at both. **The `8c2817219f6aa087` this entry
   first quoted is withdrawn: it cannot be re-derived by any of 600
   constructions, nor by the six Platterpus tried.** The invariant holds; the
   number did not, and it was published with no method to check it against. It is preferred only because `a9aedf0` writes `Handshake: round 15
   lap 14 closed` — so a rip gathered for round 16 would label itself with the
   previous, closed round — and because `tools/rig-round16.sh` landed after it.

   **§K pre-commits under S-18**: our next lap agrees whatever test pin their
   reply names, verbatim. A lap spent agreeing which of two byte-identical
   `src/` trees to build is a lap spent on nothing.

 - **The name-attribution rule misfired once in 166, and the token was `tmp`.**
   `tools/blackbox.py` attributes an appearance in `/`, `/tmp` or `$HOME` to
   the run under test when its name carries a string the harness passed the
   binary. The probe `contain/folder/unicode/absolute` passes
   `-a album=/tmp/escaped`, which splits to `{album, tmp, escaped}` — and
   **`tmp` is a substring of every name Python's `tempfile` produces**, so a
   foreign `/tmp/tmpf_wrg7bd` was reported as this binary escaping the sandbox.
   The other **165** appearances in that sweep were correctly left
   unattributed, so the rule was right 165 times and wrong once.

   Tokens naming a scanned root are now dropped. Those names are exactly the
   strings most likely to recur in the naming conventions used *inside* those
   directories, so they can never discriminate. Revert-proved on the exact
   probe, both ways: with a foreign tempfile planted mid-window it is now
   unattributed rather than gated, and a genuine rip landing in
   `/tmp/BlackboxEscapeProbe` is still gated by the token `BlackboxEscapeProbe`.

   **It is load-dependent, which is why two clean 73/73 runs did not show it**:
   the false positive needs a foreign temp file to appear inside one specific
   probe's window. A test that passes when the machine is idle and fails when
   it is busy is flaky, and a flaky test is worse than none.

   **Also learned: concurrent `meson test` runs against one build directory
   corrupt `testlog.txt` with NUL bytes.** Same corrupted-machine-readable-
   record shape this project already hit from a different cause; the failing
   block was only readable with `grep -a`.

 - **ROUND 16 IS OPEN, on `a9aedf0`.** One close condition, fixed at lap 1
   under S-13 and unable to grow: **a hardware acceptance run on this pin**
   establishing that the AccurateRip path still succeeds with the rewritten
   response parser, that `-H` together with de-emphasis produces correct
   de-emphasised audio, and that no line Platterpus parses has moved except
   the ones §D names.

   The references are **generated by `9fae503`** — the lap's own commit, not
   the pin — and committed immediately after it. Since r3 the handshake state
   is compiled in, so adding a lap file changes the binary: `a9aedf0` writes
   `Handshake: round 15 lap 14 closed` and `9fae503` writes `Handshake:
   round 16 lap 1 OPEN`. The reference has to come from the build that knows
   about the round it belongs to, which is also why **a lap can never name a
   build containing itself** and why the pin it announces is always the parent.

   `§E` of the lap was revised before sending to say so — it had named the
   previous regeneration. **§310 permits revising an unsent lap**, and this one
   had not gone; the revision is recorded here rather than left to be inferred
   from a hash that moved.

 - **A SPURIOUS `pthread_cond_wait()` WAKEUP SEGFAULTED THE RIP, and the fix is
   three characters per site.** All three of the FIFO's waits were guarded by a
   single `if` rather than a `while`. POSIX permits `pthread_cond_wait` to
   return without the predicate holding, and this program makes that concretely
   reachable rather than theoretical: it installs SIGINT and SIGTERM handlers
   and its encoder threads sit in exactly these waits.

   With the `if`, `fifo_pop()` fell straight through to
   `out = ctx->queued[0]` with `num_queued` still **0** — a read of a slot
   nothing had written — and then decremented the count to **-1**. The `assert`
   below it catches that only in a build that keeps asserts. `fifo_peek()`
   cloned the same slot; `fifo_push()` merely exceeded its own bound, which is
   the mildest of the three.

   **It cannot deadlock as a `while`**, and that was checked rather than
   assumed: the end-of-stream sentinel is a **NULL push**, which really does
   increment `num_queued`, so the pop-side predicate genuinely becomes false;
   and the loop re-reads `block_flags` each iteration, so a shutdown that
   clears the flag still lets a waiter leave. With no spurious wakeup the
   `while` is byte-for-byte the same behaviour as the `if`.

   **`tests/fifo.c` constructs the wakeup instead of waiting for one.** To the
   waiter, a spurious wakeup is exactly *"the condition variable was signalled
   and the predicate did not change"* — so the test signals `cond_in` with the
   queue empty. Waiting for a real one gives a test that fails once a year on
   somebody else's machine.

   **Revert-proved, and the result is not subtle: restoring the single `if`
   makes that test SIGSEGV — exit 139, not a failed assertion.**

   Upstream's. Found as lead (k) of the 2026-09-05 audit, which reported it as
   a style observation; re-derived here, it is a crash. The merge-back list is
   now **seven**.

 - **The `-j` record carries two instants now, and it carried none before.**
   Round 15 lap 14 §5 item 7 named two halves — the two log timestamps had no
   UTC offset, and *"the `-j` record carries no wall clock at all"*. `8d465f1`
   fixed the first; this fixes the second. The item had been reported closed
   and was half done.

   The record's reason to exist is the runs that open **no logfile at all** —
   it is written from `atexit` and `-j` is found by a pre-pass before genopt.
   Until now such a run could say what happened and never when. Verified:
   `-J -I -j FILE` opens no logfile, exits 1, and its record now carries both.

   **Two fields, never one.** The file is written from `atexit`, so a single
   timestamp taken there names when the process **ended** while a reader takes
   it for when the rip **happened** — event time and processing time collapsed,
   which is CLAUDE.md's rule 5 and the exact defect this project already found
   in its own log. `started_at` is captured in `crip_diag_enable()`, because
   `-j` is what creates the record.

   Schema moves to `cyanrip-diagnostics/4`. What is known about the consumer's
   allowlist is recorded **with its citation** — their round-11 lap 2 prints
   `SUPPORTED_SCHEMAS = {1, 2}` under a `[MEASURED]` heading — and nothing is
   claimed about what their build does with a `/4`, because we cannot read
   their source. That mistake cost a whole round once.

   **What the test can and cannot establish is said out loud.** Shape and
   ordering are deterministic and asserted. Whether the two are *independent
   captures* cannot be forced: the values are second-resolution and no rip this
   suite drives takes a second (measured — the slowest, `-Z 30` on
   `pregap.cue`, is 0.81 s, and 0.81 s crosses a boundary by luck rather than
   guarantee). So independence is asserted **structurally**, and reverting
   `finished_at` to reuse `started_at` fails that check and **nothing else**.

 - **The interrupted sample's LOG was shape-checked and its RECORD was not.**
   `--interrupted --check` verified the committed log still carried the line
   shapes a rip interrupted right now produces, and looked at nothing else. Its
   companion `.diagnostics.json` sat at schema `/3`, from a build three
   releases back.

   Not cosmetic: `PROVIDER-CONTRACT.md` derives its diagnostics field table
   from the **committed records**, so with a stale sample among them it
   published `started_at` as **not in every record** — true of the samples,
   false of the binary. The check now compares the record by **key set** and
   schema, never by value, because an interrupted rip's counts and timings
   differ every run for the same reason its log's do.

 - **The golden reference is regenerated, and it had gone stale in FORMAT
   while every test passed over it.** **Superseded within the session, and both pairings are
   recorded rather than the first being overwritten:** first generated by
   `23cd4fc` and committed at `3a7e867`, then regenerated by `6ce94bb` once
   the `-j` fields and the interrupted-record check had landed. Never the same
   commit as its own build, because a generated artifact cannot carry the hash
   of the build that produced it.

   It had been stale since `8d465f1`, three commits and a whole round-15 close
   ago. `sc_golden_reference_is_from_a_clean_build()` checks the banner for a
   `-dirty` marker and for this tree's version; **neither says anything about
   format**, so the reference went on carrying a bare
   `creation_time: 2026-09-06T02:49:53` after the binary started emitting a UTC
   offset, and 69 of 69 tests passed over an artifact describing a binary that
   no longer existed. That file is what Platterpus diffs against and the only
   one they can see.

   *"A reference log guards only the paths it exercises"* is already in this
   project's rules. This is that one level up: **it guarded no format at all.**

   `sc_golden_reference_matches_a_fresh_rip()` re-rips using the reference's
   OWN `Invoked as:` line — so the comparison cannot drift onto different
   options, which is how a previous regeneration dropped `-Z` and removed the
   secure-re-read surface from the artifact both sides check against — and
   compares SHAPES: every alphanumeric run collapsed, punctuation and field
   order kept. Two refinements, each paid for by a false positive:
   a `-dirty` marker is normalised away (a property of the tree, not the
   format), and a run of 16+ base64url characters collapses to one token,
   because `Log FUN512:` is a digest whose punctuation falls in a different
   place every run. The 16-character bound is what keeps the timestamp out of
   that rule: `2026-09-06T23:04:45+00:00` contains colons, and its longest
   qualifying run is 13.

 - **ALL SEVEN HELD ITEMS FROM ROUND 15 LAP 14 §5 HAVE LANDED.** Each was
   announced before it shipped, and each is revert-proved with the build
   confirmed green during the revert. `1` completion footer on the two paths
   that skipped it (`a79ac9e`); `2` **`-H` no longer discards de-emphasis**
   (`b866900`); `3` bare apostrophes escaped without double-escaping the
   consumer's (`c59dea3`); `4` and `5` U+FFFD substituted for invalid UTF-8
   instead of truncating and logging (`c3482b0`); `6` **every curl transfer
   bounded** (`e7835c3`); `7` both record timestamps name an instant
   (`8d465f1`).

   Their lap 16 §D closes four of the seven on their side too, and says item 2
   *"never reaches us — we never pass `-H`"*.

   **Item 2 is the one to read.** `init_filtering()`'s filter string was a
   ternary cascade, so `hdcd` matched first and `aemphasis` was never reached:
   on `preemph.cue`, `-H`, `-H -W` and `-H -E` produced byte-identical audio,
   both the disable and the force flag inert. The log printed
   `(deemphasis applied)` and the cue omitted `FLAGS PRE`, both reading the
   SETTING, so **audio, log and cue were self-consistently wrong** — checking
   one against another found agreement.

   The composed chain needs two explicit `aresample` bridges. libavfilter's
   `hdcd` filter calls `avfilter_graph_set_auto_convert(AVFILTER_AUTO_CONVERT_NONE)`
   in its own init — it must see the exact samples, since the control code it
   decodes lives in their low bits — and **that setting is graph-wide**, so
   `hdcd,aemphasis=type=cd` does not mis-render, it fails to configure.
   `aformat` cannot substitute: it constrains a link and relies on the
   converter that is switched off. Both were tried against libavfilter directly
   before either was written. The input that separates a cascade from a
   composition is **both flags at once**, which the suite had never used.

 - **THE PROVIDER CONTRACT PUBLISHED LINES THE BINARY NEVER PRINTED**, and it
   reached **P2** — the surface we undertake not to reword without a round.
   The generator deleted every `\n` in a format string, including **interior**
   ones, fusing two printed lines into one string:

       P2 published  `Embedded cover art:    %s: %ix%i %s`
       a real rip prints the label and the value on separate lines

   P5 was worse: `...for writing: %s!Invalid folder name? Try -D <folder>.` ran
   two sentences together with no separator at all, and P5's stated purpose is
   that a consumer derive error matching from it. **Nine rows** across P2, P3
   and P5. Fixed at `1c96c8d`, contract regenerated in the commit after.

   `sc_contract_covers_log()` could not have caught it: it extracts column-0
   labels only, and tests `label in contract` — a substring check over the whole
   document, satisfied by the document being wrong.
   `sc_contract_line_shapes()` is the other half, and **the discriminator is the
   padding, not the label**: a fused row still starts with the right label, so
   any check that strips before comparing passes on both readings.

 - **`tools/message-witness.py`** answers what P5 cannot: which of its failure
   messages does anything in this tree actually assert? The 2026-09-05 audit
   said "~20 of ~128" as a lead. **Measured: 7 witnessed, 107 with no witness,
   6 unprobable** — a literal prefix too short for a probe to discriminate,
   counted in neither column rather than folded into either. It found the fused
   rows on its first run. It deliberately does not gate on full coverage and
   must not become one: most of P5 needs a drive, a network or an allocation
   failure, and a permanently red gate is one nobody reads.

 - **`check-settled.py` truncated any check command containing a backtick** and
   handed the fragment to the shell, which failed on an unterminated quote — so
   the row was reported **STALE**, as a fact that had stopped being true rather
   than as a row that could not be read. Fixed at `c399f44` with a
   double-backtick span plus a `shlex` well-formedness check, and the guard is
   load-bearing: the `seam-commands` row's own check is spelled with the table's
   cells, so the file exercises both escapes on every run.

   **The first diagnosis was wrong** and is removed rather than left standing:
   the note blamed the `\|` escape, which survives to the shell fine.

 - **`tools/blackbox.py`: two harness defects and a fatality.** Its containment
   check took `argv.index("-D")`, the FIRST occurrence, while genopt takes the
   last — so a probe passing `-D out … -D rerun` had four correct writes
   reported as breaches. A `-D` holding a naming SCHEME expands from metadata
   the harness does not model, so the literal directory never exists and the
   real one read as an escape; the sandbox is now the bound for those, weakened
   **visibly** rather than silently, and what still fires is the case that
   matters — an expansion leaving the sandbox entirely. And an `OSError` on exec
   killed a 838-probe sweep at 475 with a traceback; it is now `UNPROBED`,
   because a probe that could not run is not a probe that found nothing.

   Its `KNOWN` map is **removed**. It named one defect, claimed to keep the
   report "honest in both directions", and was referenced by nothing.

 - **`README.md` displayed upstream's green CI badge** at the head of a section
   in this fork's README, for a repository whose own CI has never executed a
   run (`6e74343`). The links in that section are upstream's and correctly so;
   the section now names its subject.

 - **`docs/seam-commands.md` has a SECOND wrong row**, found by Platterpus in
   their lap 16 §B3 while deriving `-D` semantics for their own guard: line 97
   publishes `-D` as `directory` / `str, path` / `writable` / "output
   directory". At the pin it is `folder_scheme`, *"Directory naming scheme"* — a
   **relative scheme**, with `-F` its per-track sibling. Read from our source at
   `978f9b0` and re-checked here. **Two is a pattern where one was a
   coincidence**, and the row is not incidental: `-D`'s real semantics are
   exactly why held item 4 mattered. Not corrected — jointly owned, so it takes
   a version bump both sides ship.

 - **ROUND 15 CLOSED — `GO`/`GO` on `978f9b0` + `platterpus 0.6.37`.** Lap 14
   records `GO` on both sides, which is the single file their lap 13 §K asked
   for: a close is read from the newest file on each side, and our lap 12
   carried `HANDSHAKE-PEER-VERDICT: OPEN` — true when written, stale the moment
   their verdict moved. **No release is cut here**; the round closing and a
   release are different acts, and the build still says `NOT a released build`.

   The golden reference is **generated by `a8343c1`** (the revised lap-14
   commit) and committed at the commit after it, which is the only order
   possible: an artifact cannot contain the hash of the build that produced it.
   It names `a8343c1` and not `39aa928` because **lap 14 was revised before
   being sent** — see below. **Their acceptance run arrived, completed and passed** —
   `ok = True`, 227 pass, 0 fail — and our half is clean on every criterion
   their §J names: 8 of 8 rips carry the pin's banner, `Ripping errors: 0`,
   `Rip completed: yes`, and `cyanrip -Y` returns 0 on every log under a later
   build. Filed at `docs/rig-2026-09-05-978f9b0/`.

   **We answered a draft.** The first lap 13 we were handed (`25e949e4…`) was
   their unsent draft, declaring `OPEN` and `0.6.38`. Their sent lap
   (`7adffe7d…`) declares `GO` and `0.6.37`, and its §A1 names the first as a
   draft — §310 permits revising an unsent lap, so nothing was wrong on their
   side. We had written a whole §H establishing from the bundle that the run
   used `0.6.37` at `f3b60a0`; **their §A1 discloses it unprompted and their §B
   goes further**, declining to bank `pass=227 fail=0` and verifying each
   archival claim directly from the artifacts. Recorded as a confirmation, not
   sent as a finding.

   Swapping the draft for the real lap moved the thirteen-lap digest from
   `25c903c294d88e82` to `6044c992bfe49c41`, and `seam-check` caught it as
   **`SAME COUNT, DIFFERENT HASH`** — a count-only check cannot see that. The
   draft is kept at `docs/handshake/inbound/drafts/`, deliberately not beside
   the laps, because the gate and the digest both `glob("round-*.md")`
   non-recursively in `inbound/`.

   **Also corrected here:** our lap 12 said their escaping layer *"just does not
   cover the apostrophe."* It does. Measured on our own binary, the
   backslash-escaped form lands `title`, `artist` and `isrc` — and our own lap
   12 table two lines above already recorded it. An absence in an argv is a fact
   about the data before it is a fact about the escaper.

 - **Round 15 lap 12 sent** — references generated by `f4b921f`, committed
   after it. **A defect in the pin, found here:** `-H` silently discards
   de-emphasis (the filter string is a ternary cascade, so `hdcd` wins), the log
   prints `(deemphasis applied)` from the settings anyway, and the cue drops
   `FLAGS PRE`. Audio, log and cue self-consistently wrong. All three halves are
   upstream's. **Not fixed this round** — S-15 freezes the pin and their run is
   in flight; unreachable in their usage (0 uses of `-H`).

   Found by chasing a surviving mutant at `cyanrip_encode.c:510`, which looked
   equivalent because the gate above it is the same expression — and is not,
   because that gate also has a `decode_hdcd ||` term.

 - **Round 15 lap 10 sent.** References regenerated behind it, **generated by
   `996798d`** (the lap-10 commit), committed at the commit after it.

   **Their bundle said the run failed and we published that it passed — with
   the file that said so in our hands.** `session/transcript.txt:293` reads
   `[ FAIL ] L366  wait-for-rip 10800   (10800.1s)` and `report.json` carries
   `"ok": false`. Both were in the 2026-09-03 bundle; we filed neither and read
   neither. Lap 8 called the CC-1 claim a scope error, which it was, but the
   verdict was never missing — only unread. Both are filed now with
   `rig-check/`, and `SHA256SUMS` covers all 31.

   The filing note is what hid it: *"what is NOT here, and it is a choice rather
   than an omission"* named the JSONs and the screenshots and not these two,
   because it was written from memory of what was dropped rather than derived
   from it. **A "not filed" list must be generated from the difference, not
   recalled.**

   It also corrects a claim we published about *their* artifacts: the `cancel
   me` scenario **did** cancel, and `transcript.txt:458` documents it in their
   words. We had reported the interrupted artifact as existing-but-absent.

 - **`PROVIDER-CONTRACT.md` P5's preamble described five mechanisms for a
   predicate with seven.** Platterpus's lap 9 §E1 reasoned from that sentence.
   `return -N`, `err = N` and `ret = N` were in `FAIL_PATH` and in no sentence a
   reader could see. The preamble now carries a table generated from the
   predicate, and **`FAIL_PATH` is built from that table** — two lists that must
   agree is how the drift happened; there is now one. Every `| \`` row is
   byte-identical, verified by diff, so their fixture does not stale.

   16 rows rest only on a construct that does not end the run — `total_error_count++` 8,
   `ret = N` 6, `err = N` 1, and one on two of them — not the single mechanism
   their ask named.

 - **`docs/handshake/PROTOCOL-v5-PROPOSAL-evidence-transport.md`** — drafted, not
   shipped. v4 contains `bundle` 0 times and `transcript` 0 times while three
   transport practices run at once. `PROTOCOL.md` is untouched and its hash
   verified unchanged; editing the shared spec mid-round would break the
   shared-hash check on both sides.

 - **Round 15 lap 8 sent**, and the correction in it is ours. References
   regenerated behind it, **generated by `b16ccfa`** (the lap-8 commit),
   committed at the commit after it.

   **We published `CC-1 IS MET` and it was not.** Our lap 3 declared `CC-1 NOT
   MET`; we then read Platterpus's 2026-09-03 bundle, judged lap 3 falsified,
   and wrote the opposite into `docs/SETTLED.md`, the standing status and the
   rig README. Their lap 6 §C1 shows what happened to that run: section F
   budgeted `10800`s for work needing about twice that, timed out at
   `10800.1`s, and the ARCHIVAL section downstream produced no evidence at all.
   **We verified the rips inside the run and reported the run** — *"I verified
   the list you sent" is not "I verified your inventory."* All three documents
   are corrected.

   A second, smaller one: the withdrawn lap 5 asked them for `0.6.34`'s commit
   while the bundle's own per-rip JSON carried `generator.build_fingerprint:
   dba2ab2`, a field we had printed while reading it.

 - **Their laps 4–7 filed**, arriving in one envelope with their acceptance
   script. All five parts verified against the envelope's own manifest on size
   and hash before anything was read. **All four of their declared digests
   reproduce here exactly** — they adopted our method and built it
   independently, so two implementations of one written spec now agree on four
   consecutive values.

 - **`PROVIDER-CONTRACT.md` now publishes `PROJECT_FORK_ID`.** Reading their
   acceptance script found the gap: it matches `platterpus-fork`, exactly as
   every rule we have written instructs, and the contract had never published
   the value. Derived from the banner, omitted rather than guessed when the
   banner carries no tag.

 - **Audit against a fresh clone, which is what a consumer actually gets.**
   `meson test` was green here and **red from a clone**, and three separate
   causes came out of it. Every commit this session builds; the ones that are
   red on tests are red for one of these and nothing else.

   1. **Four `docs/SETTLED.md` rows read `git show master:…`.** A clone has
      `origin/master` and no local `master`. Two rows then failed honestly and
      **two passed for the wrong reason** — one asked `grep -c SIGTERM` to be
      `0`, which an unreadable file satisfies, and one ended in `| tail -3`, so
      its exit status was `tail`'s and **it could not fail anywhere**. A third
      hazard surfaced while fixing them: with the ref resolved to an empty
      string, `git show :path` is valid git and reads **the index**, so every
      row would have compared our own tree against itself.

   2. **A latent flake I had just introduced.** A new row ran `seam-check.py`
      over all 96 laps — 20.7 s of a 21.7 s sweep — and timed `Settled facts`
      out at 30 s under load. Now 0.08 s, with a floor so it cannot pass by
      finding nothing.

   3. **One ordering mistake, recorded rather than rewritten.** `796e2b1`
      files Platterpus's lap 2 and `8c011ed` adds the allowlist entry that
      lets the gate test accept its digest, so `796e2b1` is red on `Release
      gate` — one logical change across two commits. The history is pushed and
      rewriting pushed history is not available; this is the note instead.

   The other intermediate failures are the documented artifact pattern: a code
   commit is briefly red on freshness until the commit that regenerates behind
   it, because a generated artifact cannot carry the hash of the build that
   produced it.

 - **A round 15 lap 5 was drafted and WITHDRAWN unsent**, at `475f427` and
   `56e7d71`. Platterpus's lap 4 is being fetched first, so it will be
   rewritten against that rather than revised — most of what it said depends on
   what lap 4 declares: the peer pin it asks for, the verdict it transcribes,
   and whether correcting lap 3 is still the right shape.

   **It came out of the tree rather than sitting there as a draft**, because
   the handshake state is compiled in and every logfile said `round 15 lap 5`
   — naming a lap in no correspondence either side holds. The latest lap
   actually exchanged is 3. References regenerated twice as a result,
   **generated by `475f427`** and then **by `56e7d71`**, each committed at the
   commit after it.

   It was never pinned in `tests/sent_laps.py`; pinning an unsent lap records a
   send that never happened.

 - **`PROVIDER-CONTRACT.md` P5 stopped asserting a failure path it had no
   evidence for.** Platterpus's 2026-09-03 record carried five error-severity
   entries quoting `Done; (no matches found, but hit repeat limit of 3)` against
   a rip reading `status: success`, `exit code 0`, `14 of 14 tracks`. In our log
   that string is immediately followed, three times, by `Track N ripped and
   encoded successfully!` — and P5 listed it under *"Every string reachable on a
   failure path"* on the strength of `goto finalize_ripping` alone.

   A jump is not evidence of a failure path; it is the *absence* of evidence
   plus a note about where control went. Seven rows moved to **`P5a`, "Strings
   this document does NOT classify"** — among them the *convergence* line and
   the loop that echoes the cue sheet. No label list: `end:`, `end_meta:` and
   `finalize_ripping:` are all fall-through-reachable from success, so any rule
   separating them is about the label's name.

   Same section, same cause: the summary claimed `128 distinct strings` above a
   breakdown totalling **114**, because it iterated a hardcoded tuple of class
   names — three classes counted in the total and named in no line a reader
   could see. 121 + 7 = 128, and the generator now asserts each tally sums.
   `src/` is untouched, so no rip behaves differently.

 - **`docs/rig-2026-09-03-978f9b0/`** — the session filed byte-exact, with the
   four things it took off the never-run list and the two it did not.

 - **`docs/handshake/STATUS.md` rewritten in place** to correct lap 3 in the
   only channel a sent lap's facts can be corrected in, and
   **`tests/sent_laps.py`** pins both round-15 laps.

 - **Lap 3 declared a `HANDSHAKE-FROM-COMMIT` nobody could fetch, and
   `seam-check.py` passed it.** The field named `c784153`, a commit `git log -1`
   resolves happily and which is reachable from **no branch** — the pre-amend
   version of the commit that adds the lap, orphaned when a `git commit --amend`
   folded a fix in. `git gc` destroys such an object and a fresh clone never has
   it, so the lap named a build its reader could not resolve. Caught by hand,
   one command before the file was handed over.

   **Resolving is not reachable.** A local clone holds every commit its reflog
   still names, so the weaker test passes on exactly the object the check exists
   to catch; `merge-base --is-ancestor` is the one that separates them. The
   checker now runs it, for our laps and — inverted — for inbound ones, whose
   from-commit must *not* resolve here. Revert-proved on all four arms.

   Two things the sweep settled. All 43 laps carrying the field were checked and
   **only this one was wrong**. And 28 of them carry prose rather than a SHA, so
   the check silently examined a third of its input and read exactly like one
   that examined all of it — those now print `UNPROBED` out loud, the same
   remedy as the sanitizer assertions that could not fail.

   The rule this broke was already written down: *never fold a derived artifact
   in with `git commit --amend`*. It was written about generated artifacts
   naming their build; a lap naming its from-commit is the same fixpoint one
   surface over, and prose did not stop it. A check does.

 - **The regression test for the above had to BUILD an orphan, and the first
   two attempts at checking it did not discriminate.** Both are worth recording
   because they are this repository's two standing traps, hit in the act of
   closing a third.

   The first was a grep: `grep -c 'is-ancestor' tools/seam-check.py`. The string
   occurs **twice** — once in the code and once in the comment above it — so
   deleting the code leaves the count at 1 and the check passes. *A grep hit is
   not a fact.*

   The second looked behavioural and was not: running the checker over the
   corrected lap and asserting it printed `reachable from HEAD`. Replace the
   ancestor call with `anc = resolves` and it prints exactly the same line,
   because **a reachable commit resolves too** — the pattern matches both
   branches. Measured by reverting, not reasoned about.

   So `tests/release_gate.py` now builds the topology in a throwaway repo:
   commit, amend, and grade a lap against the pre-amend SHA. Five cases —
   orphan, reachable, absent, inbound-naming-our-tree, and prose — and with
   `anc = resolves` in place the suite fails with the orphan graded `OK`.
   `docs/SETTLED.md` carries two rows, each revert-proved separately.

 - **Round 15 lap 3 sent, `GO`**, and the references regenerated behind it —
   **generated by `796e2b1`** (the lap-3 commit), committed at the commit after
   it. Same ordering as every lap: commit, **rebuild**, regenerate.

 - **Round 15 opened at lap 1** on the released pair. The references were
   regenerated twice behind it — the first attempt used a binary built before
   the lap file existed, so it produced the round-14 handshake line; the second,
   **generated by `759e8ee`**, is the one committed. Adding a lap file moves the
   compiled-in handshake state, so `Handshake:` changes in every log and the
   binary must be rebuilt before the reference is regenerated.

0.9.4-rc2+platterpus.11 — 2026-08-26 — **stable**
=================================================
**The first stable release since `+platterpus.7`, and the first ever cut on a
round that reviewed a RELEASE rather than a test pin.** Round 14 closed
`GO`/`GO` on `d9c058c` / Platterpus `b524936`, with its single close condition
met by two independent hardware runs on the same build.

**The version string carries upstream's `-rc2` and this release is stable
anyway.** `0.9.4-rc2` is `cyanreg/cyanrip`'s string, copied verbatim because we
may not mint in their namespace; the part that advances is `+platterpus.N`,
which SemVer says MUST be ignored for precedence. **Order by `release_seq` and
read the `channel` column. Never parse the version.** A shape-based check will
read this stable release as a pre-release, forever.

There is no tag. Tag pushes are `HTTP 403` from the environment this is built
in, so the commit SHA and the `release-manifest.json` row are the whole
identifier.

The released commit is the one where the version and every derived artifact
agree, **generated by `009a573`** (the bump) and committed immediately after it
— never the bump itself. `+platterpus.5` was announced at the bump and failed
2 of 33 checks from a fresh clone; the consumer installed it on our say-so.

**No release, and none is coming until round 14 closes** — the pre-commit below
binds and nothing here breaks it. **Nothing in `src/` has changed since
`+platterpus.10`**, so the binary reads discs exactly as the pin `d9c058c` does
and the provider contract's source anchor is unmoved.

 - **Round 14 closed `GO`/`GO` on `d9c058c` / `b524936` at lap 19**, and the
   references were regenerated twice behind it — **generated by `9752cf5`** at
   lap 18 and then **by `8fac7a9`** at lap 19, each **committed at the commit
   after it**. Those are
   never the same commit and cannot be: adding a lap file moves the compiled-in
   handshake state, so `Handshake:` went from `round 14 lap 17` to `round 14 lap
   18` in every log, and an artifact cannot carry the hash of the build that
   contains it. Regenerating inside the lap commit would not fix that — it would
   only move the mismatch, and an `--amend` would leave `9752cf5` reachable from
   the reflog alone, where `git gc` destroys it.

 - **Rig session 2026-08-26 on `d9c058c`**, filed at
   `docs/rig-2026-08-26-d9c058c/` with checksums. **T1 is done**: secure re-read
   on hardware, `converged after 3 reads` on 14/14 tracks, which is the half of
   round 14's CC-2 the 2026-08-25 acceptance pass could not produce. **A
   non-zero `Read stalls:` count exists for the first time anywhere**, and its
   populated singular rendering matches both the format string and the shape
   `tests/stall.c:370` pins. The `Cache model:` sector arm was seen on a real
   drive. All six logs verify `-Y` exit 0 against a later binary. The two rips
   named `cancel me` and `after cancel` **do not show a cancel** — both were
   invoked with a narrowed `Tracks to rip:` and both footers read
   `Rip completed:  yes`.

 - **`tests/logrender.c`, and the measurement that produced it.** A mutation
   sweep of `src/cyanrip_log.c` scored **44.3%**: 49 of 88 single-operator
   changes passed the entire suite, twenty of them in the AccurateRip verdict,
   which was unreachable because every scenario passes `-A` and the suite has no
   network. The new test drives `cyanrip_log_track_end()` and
   `cyanrip_log_finish_report()` against a `tmpfile()` and compares the real
   bytes, taking the file to **67.0%** and then further. Every assertion was
   born killing a named mutant. The rig disc, being in the AccurateRip database,
   independently confirmed every rule it encodes — including one the first draft
   had backwards.

 - **`tools/mutate.py`.** Its first run reported **100% over 100 mutants and was
   worthless**: `contract_build` hashes `src/` into the contract's source anchor
   and so killed every mutant on the *edit* rather than on the *defect*. Found
   with a behaviourally inert edit — a comment at EOF, moving no line number —
   and asking which tests failed. Exactly one did; it is excluded by name and
   the exclusion prints on every run.

 - **`Cache model:` stopped denying a probe that had already happened.** The
   line read `(drive cache size not probed)` on every physical drive, whatever
   `-x` did — and on 2026-08-25 the first `-x -I` ever to complete on real
   hardware put it in a log whose next block read
   `Cache probe: at least 2048 sectors`. A false parenthetical in an archival
   record, and it reads first. It gains a third arm naming the probe. The
   choice is a pure function in `utils.h` because all three image drivers take
   the image arm, so no fixture reaches the branch; `tests/diag.c` asserts it.
   **Not in `d9c058c`** — this is a log-text change and therefore handshake
   material for round 15.

 - **`tests/handshake_wire.py` checks that a pin field names the repository it
   claims to.** `HANDSHAKE-PEER-PIN` is meant to be the *peer's* commit, and
   ours has declared a **cyanrip** SHA there in eleven sent laps since round 7 —
   including the two that closed rounds 11 and 13, so two closes record "which
   two programs agreed" naming one program twice. It propagated: Platterpus
   transcribed it back as their own pin for nine laps, correctly, because the
   protocol says to transcribe what the peer declared. Neither gate could catch
   it, since neither side can resolve the other's SHAs — so the check is local
   and about each side's own half: our pin must resolve here, the peer's must
   not. Reported in round 14 lap 14 §C as ours rather than theirs.

 - **Round 14 laps 7, 9, 11, 13 and 14**, and their inbound counterparts. Lap 13
   accepts a correction that matters beyond this round: `fullacceptance.txt`'s
   §A promised to stop a wrong ripper in seconds and could not, and **we relayed
   that promise to the operator unhedged** after writing in the same lap that it
   was a claim about their code we would not state. A hedge that does not travel
   to the person acting on the claim is not a hedge. Lap 13 also fixes
   `tools/rig-c1-probe.sh`, which watched the last child of `timeout` and would
   therefore have sampled a container launcher on the rig rather than cyanrip.
   Lap 11 withdraws lap 9's §B — a claim about our own binary drawn from an absence in
   Platterpus's capture, without ever establishing that the capture retained
   presence — and applies the same test to our own C1 evidence, where the rig's
   0-byte `05-minus-j.txt` turns out to be a measurement of the capture rather
   than of cyanrip's silence.

   > **Generated by, and committed at, different commits — as they must be**,
   > because an artifact cannot carry the hash of the build that produced it.
   > The provider contract was generated by `12d77c1`, the golden reference by
   > `b75ffae`; each was committed at the commit after the one that built it.
   > Earlier references in this cycle came from `a2589d1` and `b27f3b8`, named
   > here because a log claiming any of those builds has to stay resolvable.
   > The only thing that moved each time is the compiled-in handshake state.

 - **`tools/release-gate.py` reads a declared-none test pin as an answer**
   rather than as a build. `HANDSHAKE-TEST-PIN: none.` printed as
   `test pin none. -- for the rig to gather evidence`, naming a build called
   "none." — a label asserting what its value disclaims. Closure never consulted
   the field and a test asserts it cannot, so nothing was mis-decided; a human
   reading the gate was told a build existed. Both projects declare the field
   this way and both are right to, so the reader is what changed.

0.9.4-rc2+platterpus.10 (2026-08-25)
====================================
**The build to run the round-14 acceptance pass on, and the LAST release before
round 14 closes.** `release_seq` 20, channel **`beta`**;
`0.9.4-rc2+platterpus.7` (`237a4ff`) remains `stable` and is untouched.

> **Pre-commit, and it binds:** no further release will be cut until round 14
> closes or the acceptance pass reports. Platterpus's acceptance script asserts
> an exact build tag, so every release we publish mid-round invalidates a script
> they have already written — twice now. **The churn is ours and it stops here.**

**Nothing in `src/` changed since `+platterpus.9`.** The provider contract's
source anchor is identical, so the binary reads discs exactly as `+platterpus.9`
and `+platterpus.8` do. Said first because "a new release" normally implies
changed behaviour and three releases running it has not.

**What did change, and it is the reason to cut this at all:** the **compiled-in
handshake state**. Every logfile records the round it was produced under, and
`+platterpus.9` stamps `round 14 lap 1 OPEN, verdict OPEN` — the state before the
pin was reconciled and before the acceptance plan was reviewed. This build stamps
`round 14 lap 3 OPEN, verdict HOLD`.

That matters because the acceptance run's logs are **archival records of a
measurement that will not be repeated**, and the round they name is part of the
record. A log from the pass saying `lap 1` would describe a state two laps stale
at the moment it was written — the same defect as a superseded track's
`creation_time` describing a read that was thrown away.

**Contents, all of it correspondence and none of it behaviour:**

 - **Round 14 lap 2** — the pin moved from `796df32` to `f2c0506`, declared as an
   S-15 departure rather than smuggled, because four fixes had landed after
   `796df32` was cut and testing it would have meant spending a disc on a build
   no consumer would install.
 - **Round 14 lap 3** — Platterpus's acceptance plan reviewed against the seven
   questions published in advance (seven for seven), plus one blocking amendment:
   their script asserts an exact build tag and their own install route now
   delivers a different one.
 - **Round 13 lap 8 identified as never delivered.** Their gate was correctly
   holding round 13 open; the lap existed here and no envelope was ever built for
   it, because it was a closing lap with no artifacts. **A lap with nothing
   attached is exactly the one that gets forgotten.**
 - **A correction to our own record.** The round-13 digest divergence is resolved:
   transcribing Platterpus's six rows and re-hashing them here reproduces *both*
   declarations exactly, with one row differing — the same file before and after
   they renumbered it. Neither implementation was wrong. Our earlier note filed
   the renumbering hypothesis as **rejected**; it was not refuted, because we
   varied the lap number with the file's hash held fixed and could not do
   otherwise without holding their copy. **A hypothesis that cannot be fully
   tested from one side is not a refuted one**, and the note said it was.
 - **A stale comment in `meson.build`** that named the version it was written for
   (".7") and had been wrong for two releases. A comment beside a value must not
   restate the value.

**Artifact provenance.** All five derived artifacts — `PROVIDER-CONTRACT.md`, the
golden reference and its diagnostics record, the interrupted sample and its
diagnostics record — were:

> **Generated by `7b53688`** — the bump commit, in the default build
> configuration — **and committed at the commit immediately after it**, which
> this paragraph cannot name, because a file cannot carry the hash of the commit
> that contains it.

One build tag between all five and one version across them, counted rather than
assumed.

**Still no disc.** 52 of 52 in four build configurations — default,
`-Ddeclare_released=true`, ASAN+UBSAN, and both — and not one sector read from a
drive. That is what round 14 exists to change.

0.9.4-rc2+platterpus.9 (2026-08-25)
===================================
**A BETA, and it supersedes `+platterpus.8` as the build round 14 tests.**
Published as `release-manifest.json` `release_seq` 19, channel **`beta`**;
`0.9.4-rc2+platterpus.7` (`237a4ff`) remains `stable` and is untouched.

**Cut while handshake round 14 was OPEN, deliberately and on the maintainer's
explicit instruction.** `tools/release-gate.py --release-gate` exits 1 on this
tree and names round 14; that is recorded here rather than worked around,
because a release the gate refused is a fact a reader is entitled to know. What
the gate protects is `stable`, and `stable` did not move — `gen-release-manifest.py`
enforces separately that a stable release can never point at an unclosed round,
and that assertion is untouched and still passing. A `beta` pointing at an open
round is what the beta channel *is*: the manifest reports `round_closed: false`
for this row, truthfully.

**Why it exists at all: `+platterpus.8` was already stale as a test subject.**
Round 14's one close condition is a hardware acceptance pass on *the build that
ships*. Four fixes landed after `796df32` was cut, so testing `796df32` would
have meant testing something nobody would install — which is the precise defect
that made round 13's CC-2 unsound and got it moved. The coherent repair is the
same one as last time: **release what is current, then test that.** Round 14's
pin moves to this build, which is a departure from S-15 and is declared as one
rather than smuggled.

**What is in it that `+platterpus.8` is not:**

 - **A false structural claim removed from `PROVIDER-CONTRACT.md`, and the
   generator taught to derive what it used to assert.** P2 printed one sentence
   under *every* buffer-composed row — *"Segment 0 is always present; the rest
   are appended conditionally"* — as a hardcoded string that derived nothing. It
   is correct for the progress line, whose segments genuinely are
   `snprintf(line + line_len, …)` appends, and **flatly wrong for `Cache
   probe:`**, whose nine segments are arms of a `switch` that each write the
   whole buffer and `return`, so exactly one is ever emitted. A consumer building
   a matcher from that sentence writes a concatenation pattern that can never
   match a real probe result — and `Cache probe:` is the one line round 14's T3
   exists to put on a drive for the first time.

   The fix derives it from the call rather than restating it: a whole-buffer
   `snprintf(buf, …)` writes from the start and NUL-terminates, so it *replaces*;
   only `snprintf(buf + n, …)` can *append*. The contract now says which of the
   two each write is, and reports three outcomes — all-replace (*"these
   ALTERNATE, match them as alternatives, never as a concatenation"*), all-append,
   or mixed, naming which segments do which. **It still refuses to claim any
   segment is unconditional**, because that is control flow and needs a run.

   The defect's shape is the point and is worth keeping: not a mistaken fact, but
   a **hardcoded prose claim inside a generated document** — the same shape as the
   fatal-message wording allowlist and P4's `1, Every failure, without exception`
   row, both of which Platterpus found from the other end. Found here by writing
   an acceptance spec against our own contract and noticing the document
   disagreed with the source.

 - **`contract_composed`, a regression test that asserts against the binary.**
   Re-deriving the structure in the test and comparing it to the generator's
   derivation would prove only that two of our own regexes agree. So it runs
   `-x -I`, takes the `Cache probe:` line cyanrip actually wrote, and checks the
   value contains exactly **one** segment head from P2's table: concatenated
   segments would show two, a stale table none. It reads the contract
   positionally — from the cache-probe row's own heading to the next composed
   row's — because P2 has three composed rows and a check satisfied by finding
   the right words *somewhere* is satisfied by the document attributing them to
   the wrong row. Revert-proved on both assertions separately. 52 tests now, up
   from 51.

 - **`docs/round-14-acceptance-spec.md`** — what cyanrip expects round 14's
   acceptance pass to *observe*, per test, produced by running the binary rather
   than described from memory. It states **no acceptance criteria on purpose**:
   we report measurements with provenance and Platterpus makes judgements. It
   carries the measured `-T` table across all five modes, both directions of the
   paranoia relationship, the `Cache probe:` vocabulary, and the seven questions
   we will ask of their plan — published in advance so our review is a diff and
   so we cannot invent new criteria later.

 - **The paranoia relationship, measured in both directions rather than reasoned.**
   A single-pass rip gives per-track `15+10+5 = 30` against a disc total of **30**
   and emits **no** `Scope:` line at all; three passes give the same 30 against
   **90**. So the general rule is `sum(per-track) ≤ disc total`, with equality
   exactly when every track was read once. **The ratio does not generalise** — on
   this fixture every pass does identical work, which is not true of real media,
   and a consumer asserting `disc == repeats × sum` would fail on a correct rip.

**Nothing in `src/` changed.** The source anchor is unchanged from
`+platterpus.8`, so the binary behaves identically; what improved is the
contract's account of it, the test that holds it there, and the specification
built on it. Said explicitly because "a new release" normally implies changed
behaviour and here it does not.

**Artifact provenance.** All five derived artifacts — `PROVIDER-CONTRACT.md`, the
golden reference and its diagnostics record, the interrupted sample and its
diagnostics record — were:

> **Generated by `6ff687e`** — the bump commit, in the default build
> configuration — **and committed at the commit immediately after it**, which
> this paragraph cannot name, because a file cannot carry the hash of the commit
> that contains it.

One build tag between all five and one version across them, counted rather than
assumed. Those are always two commits and only the second is unnameable; naming
neither, which an earlier release attempted, is what
`tests/rip_images.py`'s `reference` check now refuses.

**Still no disc.** 52 of 52 in four build configurations — default,
`-Ddeclare_released=true`, ASAN+UBSAN, and both — and not one sector read from a
drive. `-x` has still never executed on real hardware anywhere outside
Platterpus's rig, and this release does not change that. It is what round 14 is
for.

0.9.4-rc2+platterpus.8 (2026-08-24)
===================================
**A BETA, deliberately, and the channel is the point.** Authorised by handshake
round 13, closed `GO`/`GO` on `9f8592e` in eight laps. Published as
`release-manifest.json` `release_seq` 18, channel **`beta`**;
`0.9.4-rc2+platterpus.7` (`237a4ff`) stays `stable`.

**Why a beta rather than a stable.** Round 13's original CC-2 required a
hardware acceptance pass, and it was mis-specified: it would have measured a
test pin, then shipped a release that could not be that pin -- so the released
pair would have carried no hardware evidence, which is exactly the gap
Platterpus has refused to accept on their own side. The condition was MOVED to
round 14 by explicit bilateral agreement (our lap 6 §N1, their lap 7 §W1), where
it tests the build that actually ships.

So this release is jointly verified and has never been run on a drive, and the
`beta` channel is how a record says that. `stable` stays where it is so nobody
is moved onto it by accident; `beta` resolves to the newest row of any channel,
so opting in reaches it. **It is promoted to `stable` when round 14's hardware
pass closes** -- one appended ledger row and a regenerated manifest.
**The artifacts shipped with this release are regenerated at the bump**, so the
golden reference, the interrupted sample and `PROVIDER-CONTRACT.md` all carry
`0.9.4-rc2+platterpus.8` and one build tag between them:

> **Generated by `f925a29`** — the bump commit, in the default build
> configuration — **and committed at the commit immediately after it**, which
> this paragraph cannot name because a file cannot carry the hash of the commit
> containing it.

Those are always two commits and always will be, and only the second one is
unnameable. An earlier draft of this line used that to justify naming *neither*,
deferring the generating build to the release commit's message — where
`tests/rip_images.py`'s `reference` check cannot read it and, more to the point,
where a consumer reading the changelog will not look. The draft before *that*
one did name a SHA, computed before an `--amend` that changed it, leaving a
dangling reference in the very paragraph whose subject is dangling references.

Naming it here rather than in the closing lap is forced rather than chosen:
adding a round file changes the compiled-in handshake state, so a lap can never
name the build its own attached artifacts were generated by, and round 13's
closing lap was sent before `f925a29` existed.

 - **The filename substitution table is published, derived, as `P7`.**
   Platterpus's round-13 blocking ask. Which characters each of the four `-T`
   modes rewrites, to what, and which mode is the default — generated from
   `naming.c`'s table, `cyanrip_main.c`'s option handling and `os_compat.h`'s
   availability macros, with both compile-time branches reported rather than
   collapsed to the generating machine's. It also carries the two behaviours a
   table cannot express: `/` is decided by the call site rather than the mode,
   and the two quote glyphs alternate on a parity that every substituted
   character advances and that resets at each `{tag}` boundary.

   The `sanitize` scenario parses the table back out of the committed contract
   and rips with each mode, so the document is checked against the binary
   rather than against itself.

 - **A CD-Extra session gap could publish a garbage disc ID at exit 0.** A data
   track in last position is read as a second session and `11400` frames come
   off the preceding audio track. The subtraction was unguarded, so on a TOC
   where the gap does not fit the LSN went negative, `discid.c` left-shifted a
   negative int, and the run emitted `toc=1+2+4294956496+150+375` and
   `CDDB ID: FFFF6E02` with no diagnostic of any kind in a default build. It is
   now refused with a line at column 0, and `tests/fixtures/ecd.cue` is the
   first fixture here with a data track in last position.

 - **`End LSN:` stopped calling that session gap a read offset.** The value
   differs from the signalled one for two independent reasons and both printed
   ` (with offset: %i)`, so an 11400-frame session adjustment was reported as a
   field normally worth one frame. The offset-only wording is unchanged byte
   for byte; the session case gets its own suffix, so this adds a shape rather
   than breaking one.

 - **The log names the track a rip was interrupted in.** `Interrupted at:`,
   with both arms — `track %i, mid-read` and `between tracks, no read in
   progress` — because an interrupt that lands between tracks means no audio
   was in flight, and that is a different claim. The `-j` record has always
   answered this and the log did not.

 - **The `-j` diagnostics record is published as `P8`**, derived from the
   emitter's key literals and from four real records, with any disagreement
   between the two reported rather than reconciled.

 - **`docs/seam-rules.md` is v5** and defines `S-13` through `S-18`, which five
   rounds of correspondence had cited by number against a spec that assigned
   `S-1..S-12`. Shared file: not in force until Platterpus ships it too.

0.9.4-rc2+platterpus.7 (2026-08-21)
===================================
**First release on upstream's `0.9.4-rc2` base**, and the first whose version's
left half moved because upstream released rather than because we did.
Authorised by handshake round 12, closed `GO`/`GO` on `64ae7bc` in four laps
against round 7's thirty-seven.

Published as `release-manifest.json` `release_seq` 17, channel `stable`.
`0.9.4-rc1+platterpus.6` (`c4d1a00`) stays in `docs/release-ledger.tsv` as seq
16, which is where a downgrade target comes from.

**Three cancellation defects, and the first one is why the other two were
found.** Platterpus reported that a rip they killed at 300 s held the drive and
could not be reaped, and filed it against `-x`. `-x` was behaving as documented.

 - **A signal could deadlock cyanrip with the drive held.**
   `on_quit_signal()` called `cyanrip_log()`, which takes a mutex and uses
   stdio -- neither permitted in a signal handler. When the signal landed while
   the main thread held that mutex, the handler blocked on it forever, on the
   thread that would have released it. A progress line is printed per frame, so
   the window is one frame wide: roughly one run in forty idle, reliably under
   load. **Upstream has this verbatim.**
 - **SIGTERM was not handled at all.** A supervisor's kill took the default
   disposition, so the logfile was cut off mid-sentence with no `Log FUN512:`
   footer and -- because the record is written from `atexit` -- no `-j`
   diagnostics record at all. `-j` exists for runs that open no logfile, so it
   was lost exactly where it was most needed.
 - **`-Z` kept repeating after being told to stop**, rebuilding the encoders
   once per remaining `-r` attempt. A cancelled `-Z 200 -r 200` printed
   `Stopping, ripping incomplete!` 182 times before it would exit; it now
   prints it once.

**Log and record changes, all declared in round 12 and verified by Platterpus
against their real parser:**

 - `Rip completed:  no (interrupted by user, ...)` now names the signal --
   `SIGINT` or `SIGTERM`. "by user" was accurate while SIGINT was the only
   signal handled and became a wrong claim the moment SIGTERM was, since a
   supervisor's timeout is not a user.
 - **`--verify-log` gives every verdict its own exit code**: 0 valid, 2
   mismatch, 3 no checksum, 4 trailing data, 5 unreadable. `1` stays reserved
   for "cyanrip failed before reaching a verdict". A log with no footer is an
   incomplete record, not a tamper claim, and until now both exited 1.
 - **The diagnostics record no longer publishes a checksum for a track that
   never finished.** Checksums are finalised per pass, so an interrupted track
   carried a real `eac_crc` over a partial read -- two runs interrupted at the
   same point produced `3697A1BF` and `03EEE452`. Schema `cyanrip-diagnostics/3`
   adds `rip.interrupted_by` and per-track `audio_ripped`; when that is false
   the checksum fields are `false` and `null`.

**Upstream `0.9.4-rc2` merged** (`1ee56fc`), never rebased: rebasing would
orphan 22 published SHAs including the one Platterpus runs. Inbound: QR codes
for MusicBrainz submission URLs (optional `libqrencode`, stdout only and
TTY-gated), flatpak packaging, MusicBrainz track-title and artist-credit
preference, a macOS CI job. No CLI flag came in, and **no audio changed** --
all twelve checksum lines in the golden reference are byte-identical across the
merge.

**Two generated documents were describing programs we do not have.**

 - **`PROVIDER-CONTRACT.md` P4 said exit `1` for every failure** in the same
   release whose breaking notice declared five distinct codes. The rows were
   literal strings inside the generator, and the derivation beneath them
   scanned integer literals inside `main()` -- which returns `rc` from
   `cyanrip_run()`, so every real exit was out of scope. It found `1` and
   missed even `0`. Now derived by following the program one hop from the entry
   point, resolving enum constants and assigned variables, and reporting
   anything unresolved with `file:line`. `sc_contract_exit_codes()` runs the
   real binary and asserts P4 is a superset of what comes back.
 - **The contract's build line was a literal `<commit>` placeholder.** It now
   names the build that generated it, normalised in `--check` only -- the
   mechanism `gen-golden-reference.py` already used.

**New: `docs/sample-interrupted.log`**, one real rip stopped by SIGTERM, so a
consumer parsing a cancelled rip has an artifact rather than a description of
one. Not a golden reference, and its own header says so.

**What this release does not have:** any hardware evidence. 47 of 47 from a
clean clone in four build configurations including ASAN and UBSAN, and not one
disc. `-x` has still never run to completion on a real drive anywhere except
Platterpus's rig. Platterpus is deliberately **not** moving its pin to this
build for that reason, and we think they are right to.

Full round-12 correspondence: `docs/handshake/round-12-lap-01.md`, `-03`, and
their `-02`, `-04` under `docs/handshake/inbound/`.

 - **Merged upstream `0.9.4-rc2` (`4f28cf0`) at `1ee56fc`.** A merge, never a
   rebase: rebasing would orphan 22 published SHAs including the one Platterpus
   runs, and every rip ever made writes its build SHA into its logfile
   permanently. The merge-base is now `4f28cf0`, so the next delta measures only
   what is genuinely new. Inbound: QR codes for MusicBrainz submission URLs
   (optional `libqrencode`, stdout only and TTY-gated), flatpak packaging,
   MusicBrainz track-title and artist-credit preference, a macOS CI job. No CLI
   flag came in and no audio changed — all twelve checksum lines in the golden
   reference are byte-identical across the merge.

   The full outcome, including the three conflicts and why upstream's
   `Sample peak:` line was not taken, is `docs/upstream/sync-2026-08-18-rc2.md`
   §10.

 - **A signal could deadlock cyanrip with the drive held.** `on_quit_signal()`
   called `cyanrip_log()`, which takes a mutex and uses stdio — neither
   permitted in a signal handler. When the signal arrived while the main thread
   held that mutex, the handler blocked on it forever, on the thread that would
   have released it. A progress line is printed per frame, so the window is one
   frame wide. **Upstream has this verbatim.** Found by looping the new
   interrupt test until it hung and reading the backtrace.

 - **SIGTERM was not handled at all**, so a supervisor's kill left a truncated
   logfile with no `Log FUN512:` footer and — because the record is written from
   `atexit` — no `-j` diagnostics record at all. Both signals now shut down
   gracefully, and the log names which one arrived rather than saying
   "interrupted by user".

 - **`-Z` kept repeating after being told to stop**, doing one encoder teardown
   and rebuild per remaining `-r` attempt. A cancelled `-Z 200 -r 200` printed
   `Stopping, ripping incomplete!` 182 times before it would exit; it now prints
   it once.

 - **`--verify-log` gives every verdict its own exit code** (0 valid, 2
   mismatch, 3 no checksum, 4 trailing data, 5 unreadable). 1 stays reserved for
   "cyanrip failed before reaching a verdict". A log with no footer is an
   incomplete record, not a tamper claim, and until now both exited 1.

 - **The diagnostics record no longer publishes a checksum for a track that
   never finished.** Checksums are finalised per pass, so an interrupted track
   carried a real `eac_crc` computed over a partial read — two runs interrupted
   at the same point produced `3697A1BF` and `03EEE452`. Schema moves to
   `cyanrip-diagnostics/3`, adding `rip.interrupted_by` and per-track
   `audio_ripped`; when that is false the checksum fields are `false` and
   `null`.

 - **`docs/sample-interrupted.log`** ships one real rip stopped by SIGTERM, so a
   consumer parsing a cancelled rip has an artifact rather than a description.
   It is a sample, not a golden reference — where the signal lands is not
   reproducible — and its own header says so at the top of the file.

 - **`-x`'s help now says how to measure without ripping.** `-x -I` already did;
   nothing said so. The flag's meaning is unchanged and a test pins that too, so
   changing it later goes through a round.

And two additions from before the merge, both about seeing upstream clearly
rather than changing what this fork does.

 - **`docs/upstream/` — the fork explained to upstream.** This file stays what
   it is: our releases, by version, for people installing the fork. The new
   directory is the other half, and the split is deliberate:

   > "The normal changelog by version lives as it does. One for the original
   > cyanrip repo maintainer that is in more detail and gives the full shape and
   > reasoning — that can be in a different location, maybe linked to in the
   > other files so they can easily find it."

   So `docs/upstream/README.md` explains the fork to whoever maintains
   `cyanreg/cyanrip`, and each `sync-*.md` is one upstream sync analysed
   **before** anything is merged. A file saying "we did not take this, here is
   why" is a complete entry.
 - **`tools/upstream-delta.py`** generates the facts for those files: versions,
   inbound commits, CLI flags *measured from both binaries' `--help`*, log-line
   inventories from both trees, and dependency deltas. It judges nothing.

**Full audit: `docs/upstream/audit-2026-08-21-prs-and-forks.md`.** All 42
upstream PR heads and both topic branches, content-checked against our tree.
Result: 17 of the 21 with a shared history are fully covered, 21 more are
ancient and long merged, and **four are outstanding — two of which we already
implement independently**, including the ebur128 `peak=true+sample` fix that
upstream still has open as PR #116. Verdict: stay on our own line, merge
upstream forward rather than rebasing onto it, and never rebase — 22 published
SHAs, including the one Platterpus runs today, would be orphaned.
**Third-party forks could not be enumerated** (GitHub API is not available for
unattached repositories) and the audit says so rather than reporting "none".

**First analysis: `docs/upstream/sync-2026-08-18-rc2.md`.** Upstream reached
`0.9.4-rc2`; at the time of writing we were 15 commits behind it and 299 ahead,
and nothing was merged. **It has since been merged** — see the top of this
section and §10 of that file, which was appended rather than superseded.
Headline: no CLI change inbound, one new track-level log line that collides by
prefix with ours (`Sample peak:` against our `Sample peak level:`), MusicBrainz
title and artist selection changed, and one new optional dependency.

It also carries a correction. An earlier reading of this sync reported that
upstream had reintroduced a bare `Peak:` label — that was read from the commit
that *introduced* the line, and two later commits in the same series renamed it.
The label never shipped. **Compare merged trees, never individual commits in a
series**, and the tool uses `git show <ref>:<path>` because of it.

0.9.4-rc1+platterpus.6 (2026-08-17)
===================================
**Leaves beta.** Released at `c4d1a00`, published as `release-manifest.json`
seq 16, channel `stable`, authorised by handshake round 10. `0.9.4-rc1+platterpus.5` (`ddf7ac3`) is
seq 11 in `docs/release-ledger.tsv`, which is where a downgrade target comes
from. It is **not** named in `release-manifest.json`: both channels now
resolve to seq 16, because `stable` is the newest stable row and `beta` is the
newest row of any channel, so opting into pre-releases can never move a user
backwards. The manifest names current channels, not history.

The golden reference shipped with this release was **generated by `bde52d2`, the
version bump, and committed at `c4d1a00`**. Those are always two commits and
always will be: a generated artifact cannot contain the hash of the build that
produced it.

 - **The released-build claim is declared, not derived, and now reachable.**
   `HANDSHAKE_RELEASED` previously required the tree's own latest handshake lap
   to name the commit containing it -- a fixpoint, measured at 0 for 16 across
   every commit in the release ledger plus the tip. No build this fork ever cut
   could set it. It is now a build-time declaration rendered with the same
   disclaimer `Consumer:` carries:

       Handshake:      round 10 lap 5 closed, verdict GO -- released build
                       (declared at build time, not verified by cyanrip)

   The released branch used to print *nothing*, making the strongest claim in
   the line by omission. It now discloses who declared it and that cyanrip did
   not check it.
 - **`-Ddeclare_released=true` must be passed at build time, and nothing in this
   tree passes it.** Default is off, deliberately: a mis-set flag must produce a
   release that under-claims, never a working tree that over-claims. **A build
   of this release made without that option prints `NOT a released build`**, and
   the only install path the manifest offers is a source tarball the consumer
   builds themselves. So the rendering above does not appear until the installer
   is updated to pass it. Stated here rather than discovered later.
 - **`_head_is` is deleted rather than repaired.** It compared SHA prefixes in
   whichever direction was shorter with no minimum length, so `_head_is('b')`
   was true for roughly one commit in sixteen. Latent behind a constant `False`,
   live the moment the constant was fixed.
 - **BREAKING, diagnostics JSON:** `released_build` becomes
   `released_build_declared` and the schema becomes `cyanrip-diagnostics/2`. The
   logfile disclaimed in words and the JSON did not, so the two surfaces
   disagreed about one bit; a bare `true` has nowhere to put a qualifier, so the
   provenance goes in the key name. Platterpus confirmed by `grep` that they
   consume neither, and declined a compatibility alias on the grounds that it
   would carry the exact assertion the rename removed.
 - **The release gate now sees a round the other side opened.** `load_rounds()`
   enumerated only our own lap files, so a round Platterpus opened and we had
   not answered was invisible -- not merely open, absent -- and a release was
   permitted during it. Mirror of a defect they reported in the same round,
   failing in the worse direction.

Suite: **41 of 41 green from a fresh clone at the release commit**, verified by
cloning and building rather than asserted. Round 10 closed `GO`/`GO` on
`56413d2` in five laps.

0.9.4-rc1+platterpus.6-beta.4 (2026-08-11) -- PRE-RELEASE
=========================================================
Released at `2ce8993`, published as `release-manifest.json` seq 15, channel
`beta`. `0.9.4-rc1+platterpus.5` (`ddf7ac3`) remains stable and retained.

**The binary differs from beta.3 in exactly one line.** `Handshake:` now reads
`round 8 lap 7`, because the handshake state is compiled in and the round was
renumbered. `PROVIDER-CONTRACT.md` regenerates with no content delta beyond the
version, which is the evidence for that rather than the claim itself.

 - **Round 9 is folded back into round 8, and round 9 is deleted.** It was
   opened in this tree and never sent; neither was the lap 5 that withdrew round
   8. From the consumer's side the record has always been round 8, laps 1 and 3,
   so two unclosed rounds was our bookkeeping and not a fact about the seam.
   `round-08-lap-05.md` is **not** edited, merged or deleted -- its `WITHDRAWN`
   verdict is superseded by lap 7's `OPEN`, which is the mechanism `PROTOCOL.md`
   already provides. Laps 1 and 3 are untouched because they were sent.
 - **The release-gate deadlock reported in beta.3 is gone as a consequence, not
   as a fix.** With no round in a `WITHDRAWN` state the gate sees one open
   round, which is normal. The protocol hole is unchanged and real -- the next
   use of `WITHDRAWN` hits it -- so it survives as a next-round question with a
   protocol-2 proposal rather than a one-sided patch.
 - **Everything the seam needs is now one file**, `docs/handshake/round-08-lap-07.md`:
   the retraction, the close conditions and deadline, the EAC parity measurement
   across seven independent read sessions, the log-format delta, the cache
   probe, seven of our own defects with a disposition each, the revert-proof
   table, findings in the consumer's output, the contract, and eight questions.

Suite: **38 of 38 green from a fresh clone at `2ce8993`**, verified by cloning
and building rather than asserted.

0.9.4-rc1+platterpus.6-beta.3 (2026-08-11) -- PRE-RELEASE
=========================================================
Released at `7ac6820`, published as `release-manifest.json` seq 14, channel
`beta`. `0.9.4-rc1+platterpus.5` (`ddf7ac3`) remains stable and is retained, so
downgrade stays possible.

**Entries for beta.1 and beta.2 are written retroactively here**, reconstructed
from `git log` and their round files rather than from memory. Both shipped with
no changelog entry at all, which is a gap this entry closes rather than hides;
the reconstruction is labelled as such, following the beta.6 precedent.

**The binary differs from beta.2 in exactly one line.** `Handshake:` moves from
`round 8 lap 3` to `round 9 lap 1`, compiled in by
`tools/gen-handshake-state.py`. `PROVIDER-CONTRACT.md` regenerates with no
content delta beyond the version, which is the evidence for that claim rather
than the claim itself. **This is a tooling release**: what changed is the two
scripts the operator runs on hardware nobody here can reach.

 - **`tools/rig-check.py` and `tools/audio-checksums.py` stopped reporting
   checks that never ran.** `cdparanoia-cache` said cd-paranoia *"did not fail,
   it declined to answer"* on a run where it had printed `Approximate random
   access cache size: 137 sector(s)` -- `re.M` anchors `^` after a newline and
   not after the `\r` cd-paranoia separates progress with. `audio-vs-log` graded
   a file it could not decode as *"differ -- expected for any track a re-rip
   superseded"*, and could return OK saying *"0 track(s) checked; every one
   matches its log"*.
 - **`audio-checksums.py check` now distinguishes its exit codes**: `0` match,
   `1` different audio, `2` no comparison was possible. Both were `1`, so a
   caller could not tell a superseded track from a broken file. **Callers must
   stop treating non-zero as one thing.**
 - **New: `audio-checksums.py digest DIR`.** The audio cannot travel -- one
   track of the reference disc is 32.8 MB and no channel between the two
   projects would carry it -- so this runs where the files are and prints about
   60 bytes per track. A parity question is settled by pasting a block instead
   of moving a rip. Hardened against eight ways it produced a block that looked
   complete, every one found by probing it rather than reading it.
 - **The AccurateRip lead/tail skip is now covered.** `--first`/`--last` looked
   broken on two EAC tracks and were not: those skip regions are digital
   silence, so the skip is a no-op on that audio. The consequence is the finding
   -- **the reference disc cannot discriminate a correct skip from an
   unimplemented one**, and neither can any golden log derived from it. The
   self-test carries three synthetic vectors that can.
 - **New: `tools/gen-golden-reference.py`.** The reference was rebuilt by hand
   from the previous reference's own `Invoked as:` line, and that went wrong
   twice invisibly -- beta.8 shipped a reference generated at beta.7, and round
   5's lost the whole secure-re-read surface when `-Z` and `-G` were dropped
   from the retyped command. The recipe now lives in one place and `--check` is
   a `meson test`. It immediately found that **the shipped reference was stale**:
   it said `Handshake: round 8 lap 1` while the tree was at round 9 lap 1, and
   the only existing guard read the banner's version and its dirty marker, both
   of which were correct.
 - Round 8 withdrawn (`round-08-lap-05.md`); round 9 opened. **Withdrawing it
   deadlocked our own release gate**, permanently: `CLOSING = {"GO"}` and round
   8 can never reach `GO`, so `--release-gate` exits non-zero forever. Not fixed
   unilaterally -- `PROTOCOL.md` is a shared file and relaxing a close rule on
   one side is how two gates come to disagree. It is round 9's `J7`.
 - **Handshake wire conformance** is now a `meson test`. Two round-8 laps were
   sent missing three fields `PROTOCOL.md` C9 requires, because `meson test` was
   green and `tools/release-gate.py` was never run.

Suite: **38 of 38 green from a fresh clone at `7ac6820`**, verified by cloning
and building rather than asserted.

0.9.4-rc1+platterpus.6-beta.2 (2026-08-10) -- PRE-RELEASE
=========================================================
`310dbd2`, seq 13. **Entry written retroactively in beta.3.**

 - **The cache probe reads its warm-up run in chunks** (`8a045de`). Its first
   hardware run reported a bound of 32 sectors; the read had *failed*, because
   64 sectors is 150,528 bytes against an `sr` device's typical 128 KiB
   `max_sectors_kb`. cd-paranoia reports the same drive's cache as 137 sectors.
 - `tools/rig-check.py` added (`387033e`): every rig check that is not a full
   rip, writing nothing into the music library. Two of its own defects fixed
   the same day (`8a9b25c`, `6bd6f75`) -- an absence that meant two things, and
   an empty album directory binding silently to the parent's log.

0.9.4-rc1+platterpus.6-beta.1 (2026-08-10) -- PRE-RELEASE
=========================================================
`cb440bd`, seq 12. **Entry written retroactively in beta.3.**

 - **Every `cyanrip_log()` format string is now checked** (`d1c9e57`), via
   `av_printf_format` plus `-Werror=format`. Nothing had ever verified them.
 - **The cache probe reports the bound it is, and why the search stopped**
   (`1f00653`). `N sectors measured` asserted a measurement it had not made;
   there are now nine distinct wordings, pinned by `tests/cacheprobe.c`.
 - **genopt's messages entered the provider contract** (`fadddc6`, `add8054`,
   `b247172`). Ten fatal messages were always emitted and never scanned, so the
   document was incomplete while presenting itself as derived.
 - **`release-manifest.json` published** (`3492a65`), with the released commit
   chosen after the artifacts agree rather than at the bump (`22719b7`).
 - stdout is flushed after a captured libav message (`1fe78f4`).

0.9.4-rc1+platterpus.5 (2026-08-07) -- RELEASE
==============================================
**The first release of this fork since `+platterpus.4`, and the first one whose
logs do not say `NOT a released build`.**

Round 7 closed at lap 38 with `GO` on both sides against
`cyanrip 0.9.4-rc1+platterpus.5-beta.8 (platterpus-fork-g104f6d4)` and
`platterpus 0.6.4b15`. This release is that code with the version string
finalised: **no source file differs between `104f6d4` and this commit.** The
approved pin and the released artifact are therefore the same program under two
version strings, which is stated here rather than left to be discovered.

**What the close means, exactly.** One pin, one app version, one disc, on the
evidence named in the round's `HANDSHAKE-TESTED`. It is not a claim that either
program is defect-free or that the seam is fully explored.

Everything in this release shipped through betas 1-8; see their entries for the
detail. The headline items:

 - **`INDEX 00` is no longer written for a zero-length pre-gap**, and `ISRC` is
   written in the cue's appended-pregap branch. Both verified on hardware:
   `INDEX 00` on exactly tracks 2/4/5/7/8/9/10/13/14 and nowhere else, and 14
   ISRCs of 14.
 - **`-t N` with no `=` no longer publishes adjacent process memory** into the
   tags, the log and the cue at exit 0.
 - **`-c /`, `-c //`, `-p =`, `-p ==` no longer segfault** with no output.
 - **`-s` is bounded**, out of three undefined behaviours at `INT32_MIN`.
 - **A source-tarball build names the commit it came from**, instead of
   `platterpus-fork-g-dirty`.
 - **The `\:` escape is proven end-to-end on hardware** — a real colon reaches
   the cue's `TITLE` and the log's `Album:`.

Known and deliberately not fixed here, carried to round 8:

 - **Per-track paranoia counters describe the final read only under `-Z`**, while
   the disc-level total accumulates every read, so the two diverge by roughly the
   repeat count. Measured in the J1 rip's refix pass: per-track `READ` 1538
   against a disc total of 7738 over 5 reads. Pre-dates this release and breaks
   nothing in it; a caveat for anyone rendering disc-level tallies.
 - **`-x` has never executed on a real drive**, in any session, on any build.
   Nor have C2, `-f`, damaged media, CD-TEXT from a physical disc, a diagnosed
   abort, or a non-zero `Read stalls:` count.

0.9.4-rc1+platterpus.5-beta.8 (2026-08-06) -- PRE-RELEASE
=========================================================
**A build from a source tarball could not name itself.** Found while packaging
beta.7 for the rig — the first time this fork has been distributed as anything
other than a clone. Round 7 is still open and both sides declare HOLD, so this
remains a pre-release and every logfile says `NOT a released build`.

Fixed -- the build tag from a source archive
 - A build with no `.git` produced the banner:

       cyanrip 0.9.4-rc1+platterpus.5-beta.7 (platterpus-fork-g-dirty)

   Two wrong claims in one string. It names **no build** — the sha is empty —
   and it asserts the tree was **modified**, on a tree freshly extracted from
   `git archive` that provably was not. Every logfile records the build tag
   permanently, so a rip from such a build is unusable as evidence: it claims a
   provenance that resolves to nothing.

   The cause is that the failure looked like a success. `git rev-parse` printed
   nothing to stdout, `git diff --quiet` exited non-zero so `printf -- -dirty`
   ran, and the enclosing `sh -c` then exited **0** — so meson's `fallback:`
   never fired. A command that half-worked was indistinguishable from one that
   worked.

   `git archive` now substitutes the commit into `src/archive-version.txt` via
   `.gitattributes` `export-subst`, and the version command falls back to it
   when there is no repository. A tarball build reports its real commit and no
   `-dirty`; a git build is byte-for-byte unchanged, including its `-dirty`
   marking. If neither source is available the tag is the explicit string
   `unknown`, which is a claim a reader can act on rather than a malformed one.

   Regression test in `sc_cli`: the build tag must be a commit, optionally
   `-dirty`, or exactly `unknown`. Revert-proved by rebuilding from a tarball
   with the old command restored, where it fails.

0.9.4-rc1+platterpus.5-beta.7 (2026-08-06) -- PRE-RELEASE
=========================================================
**Three memory-safety defects on the argument surface, one of which reached the
archival record.** None is near a drive; all three are reachable from a command
line a consumer builds. Round 7 is still open and both sides declare HOLD, so
this remains a pre-release and every logfile says `NOT a released build`.

Fixed -- `-t` without its `=` published adjacent process memory
 - `-t 12` (no `=`) made cyanrip `strtol()` the number and then step one past
   the terminator without checking a separator was ever there.
   `append_missing_keys()` then `strlen()`/`memcpy()`d whatever followed the
   argv string in memory and parsed it as a metadata dictionary.

   Measured on `tests/fixtures/basic.cue` with a marker in the environment:

       $ env -i ZZMARKER=QQQQLEAKEDQQQQ PATH=... cyanrip -d basic.cue -t 1 ...
       TAG:ZZMARKER=QQQQLEAKEDQQQQ          <- in the FLAC
       ZZMARKER:      QQQQLEAKEDQQQQ        <- log line 90

   Exit 0, nothing printed. Adjacent process memory reached the tags, the log
   and the cue, and no re-read of the disc can tell a later reader that a field
   was invented. Now `Missing "=" in track metadata "%s"`, exit 1.

   **ASAN and UBSAN are both silent on this.** argv and environ strings share
   the initial stack block, so the overread crosses no redzone either maintains
   -- confirmed by running the pre-fix binary under both. The regression test
   therefore asserts on output, not on a clean sanitizer run.

   Reported by Platterpus in round 7 lap 31 J3, from reading the source. Their
   report said it reads one past the NUL; it also publishes what it reads.

Fixed -- two NULL dereferences on an argument that is only a separator
 - `-c /`, `-c //`, `-p =` and `-p ==` segfaulted: exit 139 with **not one line
   of output**, the undiagnosable non-zero exit the seam rules single out as the
   one failure a consumer cannot explain to a user. An argument consisting only
   of its delimiter tokenises to no token at all, so `av_strtok()` returns NULL
   and `strtol()` dereferences it (`cyanrip_main.c:1575`, confirmed by ASAN).

   In both functions the *second* `av_strtok()` was always NULL-checked and the
   first never was. Now `Missing discnumber` and `Missing track idx for pregap`.

Changed -- the argv probe can now find this class of defect at all
 - `tools/probe-argv-surface.py` gains a malformed-shape axis (separator absent,
   doubled, empty either side, trailing) for every structured argument, not just
   the one that was reported: 82 probes -> 111. It found the four segfaults on
   its first run.
 - **It also stopped grading a segfault as a clean refusal.** Every non-zero exit
   mapped to `refused`, and Python reports signal death as a *negative* return
   code, so `-11` was filed as a successful refusal -- the gate printed
   `0 silently ignored` and exited 0 while the binary was crashing. `crashed` is
   now its own class, checked first, and a non-zero exit with no message is a
   gate failure in its own right.

   The generalisable part: a summary that counts only the failure mode you
   thought of reads as all-clear. *"82 probes, 0 silently ignored"* was true,
   complete about silent-ignores, and silent about crashes because nothing
   looked.

0.9.4-rc1+platterpus.5-beta.6 (2026-08-06) -- PRE-RELEASE
=========================================================
**Entry written retroactively in beta.7.** The version was bumped in `862d3e3`
and no `Changelog.md` entry was ever written, so this string named a build the
changelog could not resolve. The contents below are taken from that commit and
the lap 30 round file, not reconstructed from memory. beta.6 was declared to
Platterpus as a `HANDSHAKE-TEST-PIN` (`dc21958`) rather than a release, which is
why the omission went unnoticed -- but a version string that exists owes the
record an entry whether or not it was released.

Fixed -- the cue sheet, and the argument surface
 - **`ISRC` is written in the cue's appended-pregap branch.** A track with an
   appended pregap took a different branch that emitted `TITLE` and `PERFORMER`
   and not `ISRC`, so every ISRC we were handed went missing on exactly those
   tracks. Found by Platterpus in round 7 lap 29. The branch is upstream's,
   verbatim on `master` since `a0de6a0`; the fork changed its reachability, not
   the branch.
 - **`-s` is bounded to ±1048576 samples.** It took the full int32 range and
   UBSAN reaches three distinct undefined behaviours on `INT32_MIN`.
 - **The `Offset:` line takes its magnitude unsigned**, which is what printed
   `Offset:  --2147483648 samples` -- a doubled sign in a contract line.

Changed
 - `docs/seam-rules.md` (SEAM-RULES-VERSION: 4) and `docs/seam-commands.md`
   adopted byte-identical from Platterpus.

0.9.4-rc1+platterpus.5-beta.5 (2026-08-05) -- PRE-RELEASE
=========================================================
**One fix, and it is why this is a new beta rather than a patch to beta.4.** An
`INDEX 00` that used to be written and now is not is a behavioural change on a
contract surface, and two builds answering to one version string is the
ambiguity `+platterpus.N` exists to prevent.

Round 7 is still open and both sides declare HOLD, so this remains a
pre-release and every logfile says `NOT a released build`.

Fixed -- the cue sheet, breaking, and found by a rip rather than by a test
 - **A zero-length pre-gap no longer gets an `INDEX 00`.** Found by cross-checking
   our own two artifacts from the 2026-08-05 rig session: on four tracks the log
   said `Pregap length: 0 frames` and the cue declared an `INDEX 00` anyway, at a
   timestamp **one frame past the end of the previous `FILE`**. Track 3 is the
   clearest -- log `Pregap LSN: 28067 / length 0 / Start LSN: 28067 (with offset:
   28068)`, cue `INDEX 00 03:01:05`, which addresses frame 13580 of a file holding
   frames 0..13579.

   `setup_track_lsn()` overwrites `start_lsn` with the offset-accounted first
   frame *after* the gap decisions are taken, so by the time the cue is written a
   `-s 667` offset has moved it one frame past `start_lsn_sig`. The guard
   compared against `start_lsn`; the length two lines below already used
   `start_lsn_sig`. A pre-gap's length is defined against the signalled start --
   the offset is a property of the read.

   **Present in all three cue sheets on record**, including the 2026-08-04
   beta.1 session, so it is as old as the sub-channel pre-gap search.

   Unreachable from any disc image, so the decision is now a pure predicate in
   `cue_writer.h` exercised by `tests/cuegap.c`: it needs a pre-gap that is
   signalled *and* zero frames long, and a bincue track whose `INDEX 00` equals
   its `INDEX 01` comes back as `unknown (sub-channel unreadable)` -- measured on
   a fixture built for the purpose, not assumed.

Added
 - **`tools/audio-checksums.py`** -- recompute cyanrip's EAC CRC32, AccurateRip
   v1/v2 and +450 over any audio file, compare a file against a log, or localise
   where two files differ. It turns "the rip matches EAC" from a report into a
   command. Validated twice: against EAC's own audio for three tracks of the rig
   disc, and against Platterpus's independently-computed addendum values, which
   agree on all four numbers for track 5.
 - **`tests/rip_images.py contract_build`** -- fails when `PROVIDER-CONTRACT.md`
   describes a different version than `meson.build`. Six of this fork's seven
   version bumps shipped a contract one build stale, including this one's. Every
   beta note then published `PROVIDER-CONTRACT.md @ <release commit>`, sending a
   consumer to the wrong file. Run against `f5e11ba` the check fails, quoting both versions.

Archived, not changed
 - `docs/rig-2026-08-05/` and `docs/rig-2026-08-05b-0.6.4b8/` -- beta.4's first
   two runs on hardware. Both `--verify-log` clean as shipped. Both carry the
   first hardware execution of the two log changes proposed in round 7 lap 25:
   `Tracks ripped accurately: 12/14` with `Tracks ripped partially accurately:
   2/14`, where the old denominator would have printed `2/2`.
 - They also settle by artifact something both projects had described backwards:
   the replayed pre-log block sits **below** the header. `Release ID:` is line 27
   and the replay runs 34-41.

0.9.4-rc1+platterpus.5-beta.4 (2026-08-05) -- PRE-RELEASE
=========================================================
**Two log lines change, and unlike beta.3 this build is *not* observably
identical to the one the rig tested.** Both were carried as known imprecision
in `docs/AUDIT-2026-08-05.md` §4, both were raised by Platterpus as non-blocking
notes, and both are contract-frozen P2 lines. They are **proposals**, shipped in
a beta so they can be run rather than argued about, and they are withdrawable at
Platterpus's word -- round 7 lap 25 carries the proposal. No other source file
changes: `git diff e61e75a..f5e11ba -- src/` touches `coverart.c` and
`cyanrip_log.c` only. One further commit lands between the two betas and it is
a test-harness fix that does not enter the binary -- see the second section.

Round 7 is still open and both sides declare HOLD, so this remains a
pre-release and every logfile says `NOT a released build`.

Changed -- log text, breaking, proposed
 - **`Release ID unavailable, cannot search Cover Art DB!` is now
   `No MusicBrainz release ID at cover art lookup, cannot search Cover Art
   DB!`.** A reader who sees the old wording and the header's `Release ID:
   <uuid>` in one log concludes one of them is wrong. Neither is: `-R` and a user
   `-a musicbrainz_albumid=` are merged into `ctx->meta` *after*
   `crip_fill_coverart()` runs, so cyanrip genuinely has no ID of its own at
   that point. The line now names which ID and when, and reports the
   observation -- the field is unset here -- rather than the stronger claim that
   the release has none. The trailing `cannot search Cover Art DB!` is
   unchanged, so a substring match on it still works.
 - **`Tracks ripped partially accurately: %i/%i` now divides by the disc's
   track count**, as the `Tracks ripped accurately:` line above it always has.
   It divided by `nb_tracks - accurip_verified`, so the rig disc printed
   `13/14` above `1/1` -- a denominator derived from the first line's result,
   which read as one disc-level tally over-reports. The numerators, and which
   tracks fall in each, are unchanged; the same disc now reads `1/14`.

   **This changes a number, not only text**, and it is the one change here
   that no test in this repository can reach: the block needs an AccurateRip DB
   hit, `crip_fill_accurip()` fetches over curl with no local-file input, and
   the synthetic fixtures are not in the database -- measured, a fixture rip
   with lookups enabled reports `AccurateRip: not found` and prints no tally.
   It needs the rig disc to verify.

Fixed -- test harness, and it means the previous betas' notes were wrong
 - **The suite did not pass in a clean clone, and had not for at least two
   betas.** `version_matrix` verified P6's upstream claim with
   `git show master:src/cyanrip_main.c`, and `git clone` creates a local branch
   only for the remote's HEAD -- `platterpus-fork` -- so a fresh clone has
   `origin/master` and no `master`. It failed there with *"master is
   unreachable; P6 cites it"* while passing in a working tree that happens to
   carry the branch.

   Measured on both: a clone fails at `e61e75a` the same way. **So `beta.3`'s
   note claimed 28/28 from a clean checkout and that was not true for anyone
   who cloned the repository.** Each spelling is now tried, and the check still
   fails when none resolves -- an absent ref stays a refusal rather than a
   silent skip, which is the only reason this was visible at all.

   Found by running the clean-checkout verification in a clone instead of in
   the tree that produced the artifact. That difference is the whole finding.

0.9.4-rc1+platterpus.5-beta.3 (2026-08-05) -- PRE-RELEASE
=========================================================
**One code change over beta.2, and it alters no observable surface** -- measured
rather than asserted (`docs/AUDIT-2026-08-05.md` §5): same fixture and flags,
both binaries, log body identical across 275 lines, cue sheet identical, decoded
PCM identical, `-j` record identical but for `rip_time_us`. **The 2026-08-04 rig
evidence for `c5fb909` therefore transfers.**

Round 7 is still open and both sides declare HOLD, so this remains a
pre-release and every logfile says `NOT a released build`.

Fixed
 - **`dev_path` leaked on every argument-validation refusal.** Twenty refusals
   return between the option table and `cyanrip_ctx_init()`, and only
   `cyanrip_ctx_end()` frees it, so `-d <dev> -J -I` leaked 100 bytes. The
   allocation moved to after the last refusal rather than being freed on each
   one: nothing in that window reads it, so allocating late cannot leak by
   construction, whereas twenty cleanup sites work until the twenty-first is
   added.

   **The size is not the point.** The leak aborted the suite under
   AddressSanitizer, so the sanitizers could not be run at all. They can now --
   **28/28 under `address,undefined`**, including a full `-Z 2 -G -j` rip, and
   that is a class of check this project had never applied to itself.

Audited, nothing found
 - `warning_level=3`: 8 `-Wsign-compare`, all in pre-existing upstream code.
 - Dead struct fields: `cyanrip_ctx.success` only, already excluded from `-j`.
 - Provider contract staleness, release gate behaviour, `TODO` inventory.
 - Full detail, including the gaps that remain: `docs/AUDIT-2026-08-05.md`.

0.9.4-rc1+platterpus.5-beta.2 (2026-08-04) -- PRE-RELEASE
=========================================================
**A beta, not a release, and round 7 is still open.** Both sides declare HOLD,
`tools/release-gate.py --release-gate` refuses a stable release against this
record, and every logfile this build writes says `NOT a released build`. It
exists so the rig can test the work below, which the previous beta does not
contain.

**Supersedes `…+platterpus.5-beta.1` (`9003e6f`) as the build to install.** That
beta is what the 2026-08-04 rig session ran; everything here landed after it.
`N` is unchanged at 5 -- only the beta counter moves, and the stable release of
this line will be `0.9.4-rc1+platterpus.5`.

**No git tag.** The git proxy refuses tag pushes with `HTTP 403`. **The commit
SHA is the identifier.**

An error-reporting audit -- the question asked throughout was not "is this
message worded well" but **"can this run leave no explanation on disk?"** --
plus one real bug found by Platterpus reading our golden reference.

Fixed
 - **Track 1's pre-gap length counted the 2-second lead-in twice** on any disc
   whose TOC signals an HTOA. The per-track block added it unconditionally, so
   the same 150 sectors were counted twice and one log disagreed with itself:
   `300 frames` against a `Gaps:` block, an LSN subtraction and a cue sheet that
   all said 150. They are never additive -- track 1's pre-gap *is* the lead-in,
   and an HTOA is audio recorded inside it. Found by Platterpus cross-checking
   four sources in the golden reference; the rig's disc has no HTOA, so a
   hardware session had already passed over it.
 - **The diagnostics record kept only the first 10000 messages.** A tool's fatal
   message is the last thing it prints, so a head-only cap discarded precisely
   the line explaining a failure while `messages_dropped` still made the record
   look accounted for. Head plus a ring-buffer tail now. Found by Platterpus
   reading the design; no rip can reach the cap, so nothing here could have.
 - **Everything said before the logfile existed reached stdout and nowhere
   else.** The drive open, the MusicBrainz lookup, the cover-art and
   AccurateRip lookups and seven refusal paths all report before
   `cyanrip_log_init()`, so a consumer archiving the log and not the terminal
   lost every one of them, and an aborted rip could read from the archive as a
   quiet success. Output is now buffered and replayed into the logfile as its
   own delimited section, after the header. Correct by construction: there is
   no list of early call sites to keep current.
 - **libcdio's own messages never reached us at all.** Its default handler
   prints to stderr and then *exits the process itself* on `CDIO_LOG_ERROR`,
   which is why `-d` on an unparseable CUE produced a run whose exit code
   cyanrip could not even report. A handler now records the message first; the
   terminating behaviour is preserved exactly, because libcdio's internal
   callers are written assuming `cdio_error()` does not return.
 - **genopt's messages did not either.** It `vprintf()`s unless `GEN_OPT_LOG`
   is defined, so every argument-parsing error went to the terminal only --
   including the one that once read to Platterpus as "cyanrip is not
   installed".
 - **The provider contract published two P2 lines truncated mid-conversion**,
   ending in a bare `%`, because a format written as `"… %" PRId64 "s"` is
   three tokens and only the string literals were joined. P2 is the set of
   lines we undertake not to reword, so a truncated one cannot be checked
   against anything.
 - **The contract's "reaches logfile" column was a straight yes/no on
   `ctx != NULL`**, which stopped being true the moment pre-log output began
   being replayed. It now reports that the answer depends on when the call
   runs and says so, rather than picking one and being wrong for half the rows.

Added
 - **`Read stalls:`** in the disc summary. A stall is a measurement of the disc
   that cannot be recovered afterwards -- put it back in the drive and it may
   read clean -- and until now the only record was the heartbeat on stdout. The
   2026-08-03 rig capture's two three-minute stalls survive only because the
   consumer happened to be recording 41180 lines of it. Three states, not two:
   `none (no read exceeded Ns)` is "we watched and saw none", and
   `unknown (stall reporting disabled with -k 0)` is "we did not watch".
 - **`-j` / `--diagnostics <path>`**, a JSON record written for every run,
   *including the ones that open no logfile*. Off unless asked for: an
   unconditional extra file would break a consumer asserting the exact set of
   files a rip produces. `main()` is now a wrapper so the exit code reaches the
   record from every route out, and `-j` is found by a pre-pass before genopt
   runs, because an argument-parsing failure writes no log either. No severity
   is claimed anywhere in it -- `cyanrip_log()` carries none, and classifying
   by wording is the defect the contract's fatal inventory already shipped
   once. Progress lines are collapsed by modelling the terminal (`\r` discards,
   `\n` commits) rather than by recognising their text.
 - **`docs/golden-reference.diagnostics.json`**, the companion reference for
   that surface, checked to describe the same build as the golden log.

Noted, not changed
 - **`cyanrip_ctx.success` is never assigned anywhere in the program.** The
   diagnostics record deliberately has no `success` field: emitting it would
   have put a permanent `"success": false` into the record of every rip that
   succeeded. A success verdict belongs downstream regardless.

0.9.4-rc1+platterpus.5-beta.1 (2026-08-04) -- PRE-RELEASE
=========================================================
**A beta, not a release, and the distinction is the point.** Round 7 is open, so
this build claims no joint verification: every logfile it writes says
`NOT a released build`, and `tools/release-gate.py --release-gate` still refuses
a stable release against this record. It exists so the rig session can run the
hardware evidence a round close requires -- which cannot be gathered without
installing the build under review.

**No git tag.** The git proxy in this environment refuses tag pushes with
`HTTP 403`, re-probed for this release, and no release-creation API is reachable
either. **The commit SHA is the identifier.**

Fixed
 - **A diagnosed abort exited 0.** The exit code tracked `total_error_count`,
   which counts *read* errors, so a refusal to start or a rip that failed
   outright printed its reason and returned success. Still within `{0,1}`.
   **Unverified by any test**: the affected paths are hardware-gated, so this is
   an item for the rig session rather than something a fixture retires.
 - **`-x` could hang with no heartbeat.** The cache probe ran before the stall
   watchdog started, so the one read most likely to wedge on real hardware --
   raw MMC reads on a path that has never executed anywhere -- was the one read
   with no liveness reporting. The watchdog now starts first and the probe
   brackets its own reads.
 - **The golden reference was regenerated from a dirty tree** and committed with
   a `-dirty` banner, naming a build nobody can reproduce. Caught by A9's own
   marker. Regenerated clean, and the reference scenario now refuses one.

Added
 - **Sample-peak cross-check** (H6), printed only when ebur128's figure and a
   direct scan of the same frames disagree.
 - **`--prerelease`** on the release gate: a stable release stays refused while
   a round is open; a beta is permitted after printing every open round.
   Adopted from Platterpus, whose artifact is an installable release rather than
   a tree.
 - **`HANDSHAKE-TEST-PIN`**, breaking the deadlock where a round could not close
   without evidence that could not be gathered without installing the build the
   round was reviewing.

0.9.4-rc1+platterpus.4 (2026-08-04)
===================================
**Not `0.9.5-rc1`.** That was asked for and is not what shipped, for the reason
`0.9.4-rc3` was withdrawn a release earlier: it mints a number in upstream's
namespace, which upstream can mint too. It would also assert a base that does not
exist — upstream is at `0.9.4-rc1` and this tree is that plus fork patches, so a
leading `0.9.5` would be a provenance claim nothing supports. The fork release
number is the only number that moves, and it moved.

Fixed
 - **`catalog` → `catalognumber`** for the MusicBrainz catalogue tag, the
   Hydrogenaudio/Picard standard spelling. Found in `q3cpma/cyanrip` `0896ff3`,
   proposed in r3 rather than shipped, and shipped now because Platterpus ruled
   they are unaffected: they run with `-N` and supply tags explicitly, so cyanrip
   never derives that tag on their rips. **Breaking for any consumer that lets
   cyanrip do the MusicBrainz lookup and reads the old key.**

Added
 - **Sample-peak cross-check** (H6). The peak is measured a second way — max
   |sample| over the same frames the ebur128 filter sees — and a line is printed
   **only when the two disagree**, naming which value came from which method:

       Sample peak disagreement: ebur128 X dBFS, direct scan Y dBFS (Z dB apart)

   Silent on agreement, at Platterpus's request and for their reason: two
   always-present numbers for one fact invite a consumer to pick one, and
   whichever it picks will occasionally be the wrong one silently.
   **The firing path is unreachable from any disc image** — two correct
   measurements of identical input agree — so the decision is a pure function
   with its own unit test, and the wiring was proved by perturbation rather than
   assumed.
 - **`-dirty` in the build tag when the tree has uncommitted changes** (A9).
   Two golden references have carried a banner naming a commit three behind
   their stated pin; both were provable from content, but a stale banner on a
   reference built from a tree that still has the defect is the one failure
   that looks like success. Verified against a genuinely clean and a genuinely
   dirty tree, not against the tree that happened to be present.
 - **Paranoia counter scope and denominator, in the generated contract** (A8,
   Q10). Per-track blocks cover the final `-Z` pass; the disc-level block is
   cumulative across every pass; they are equal only at `-Z 0`. Under `-l` the
   disc-level block counts only what that invocation read, never the whole TOC.
 - **The handshake wire header** — `HANDSHAKE-FROM`, `-APP-VERSION`,
   `-RIPPER-VERSION`, `-PIN` — adopted from Platterpus's independent proposal,
   which named the *producing* pair on every file rather than only the agreeing
   pair on a closing one. Protocol bumped to **v2**; a v1 gate reading a v2 file
   refuses, which is why the number moved.

Unchanged
 - **No logfile line changed its text, indentation, field order or units.** The
   metadata *key* `catalog` became `catalognumber`, which is a value change in
   the metadata block rather than a format change.

0.9.4-rc1+platterpus.3 (2026-08-03)
===================================
**One version string, and only one number to track.** Releases r1 and r2 both
shipped carrying upstream's bare `0.9.4-rc1`, which made two fork builds
indistinguishable by version and left two counters to reconcile -- "fork release
r2" against a string that said `rc1`. From here the fork release is *in* the
version: `0.9.4-rc1+platterpus.3` is upstream's 0.9.4-rc1, fork release 3.

Advancing our own rc number instead (`0.9.4-rc3`) was tried and reverted before
release. It mints identifiers in upstream's namespace: nothing stops upstream
tagging its own `0.9.4-rc3`, at which point two different trees answer to one
string. `+platterpus.N` cannot collide, because upstream will never mint one.

There is no r1 or r2 equivalent of this string; those builds carry bare
`0.9.4-rc1` and are told apart only by their git tag and commit.

For a consumer parsing only the leading number, `PROJECT_FORK_ID` remains the
answer to "is this the fork?" -- match on `platterpus-fork` or on the
`+platterpus.` suffix, never on `0.9.4-rc1` alone, which upstream also answers
to.

Fixed
 - **`Duration:` was one frame (13.3 ms) too long for interior tracks whenever
   `-s` was nonzero**, which is every real rip, since a drive read offset is
   almost never zero. It was sourced from `t->frames`, which `setup_track_lsn()`
   widens by a frame at whichever end the offset shifts into; the sample count
   is taken before that adjustment, so the same block printed
   `Samples: 176400` (exactly 00:04.00) directly above `Duration: 00:04.01`.
   The log contradicted itself, and the shorter of the two fields was the wrong
   one. Now derived from `nb_samples`.

   **The sign is not uniform, and this matters more than the size of the
   error.** The offset shifts *both* ends of a track's range; on a track clamped
   at the disc boundary the shift is removed at one end only, leaving the other
   end's shift uncompensated in the opposite direction. Measured on the fixtures:

   | | track 1 | last track |
   |---|---|---|
   | `-s +667` | **+1** | **−1** |
   | `-s -667` | **−1** | **+1** |

   So a downstream repair written as *"add one frame back"* is wrong on the
   boundary track, in the opposite direction, on every disc. **The repair is
   "recompute from `Samples:`", never "adjust by a frame."** Platterpus found the
   inverted sign on their own rig log (track 14 of 14, `-s 667`); it was
   reproduced here on `basic.cue` and `pregap.cue` before being accepted, which
   is also where the symmetric start-boundary case came from.

   **`-s 0` never showed it**, which is why no fixture caught it and why the
   golden reference is unchanged: it is generated at `-s 0`. Found in
   `bovinemagnet/cyanrip` commit `3eb6e22` during a survey of other forks, and
   reproduced here before being taken.

   Deliberately *not* sourced from `end_lsn_sig - start_lsn_sig`, which is what
   the `Frames:` line prints: those are captured from the raw TOC before pregap
   merging and lead-out padding move the LSNs, so for a merged pregap they
   describe a different span than was ripped.
 - **The read-liveness heartbeat added in r2 never fired on a real stall.** It
   was emitted from libcdio-paranoia's status callback, so it could only report
   a read that was still making progress -- while a drive grinding on a bad
   sector blocks inside a single SCSI command, where paranoia is not running and
   never calls back. Confirmed against a real rip on 2026-08-03: two
   three-minute stalls, a build provably containing the r2 heartbeat, 41180
   lines of captured stdout, and not one heartbeat line among them. The
   heartbeat now runs on its own thread, which keeps ticking while the rip
   thread is blocked in the kernel.

   Anyone who took r2's silence as "no stalls occurred" was reading an absence
   of evidence as evidence of absence. r2's heartbeat could not distinguish
   them; r3's can.

Changed
 - The two liveness lines are reworded. Both are **stdout-only** progress output
   and neither reaches the logfile, but a consumer capturing stdout will see it:

       old  Still reading track N at LSN L - Ts so far, C paranoia callbacks
            since the frame began
       new  Still reading track N - the read for LSN L has not returned after Ts

       old  Track N resumed after Ts
       new  Track N - the read for LSN L returned after Ts

   The callback count is gone: nothing counts callbacks for this purpose now.
   The LSN names the frame the read was *asked* to return, which is all that is
   known -- paranoia over-reads and re-reads around it, so the old wording
   implied a drive position that was never measured.

Added
 - `-x` / `--cache-probe` measures the drive's readback cache at rip time, on
   the disc actually in the drive, by timing re-reads against a known uncached
   cost. Off by default; costs seconds of drive time. Refuses to report a
   number for a disc image, which has no cache to measure.
   **Not verified on hardware** -- no drive exists in the environment it was
   written in, and no image can produce the timing signal the method needs.
 - `tests/rip_images.py` scenario **duration**, which rips two fixtures at five
   read offsets and asserts each track's `Duration:` against the `Samples:`
   field beside it -- an independently sourced number, not the one it was
   computed from. It also asserts the offset reached the rip, so the four
   nonzero cases cannot silently degenerate into four copies of `-s 0`.
 - `tests/stall.c` (`meson test` name **Stall watchdog**), which asserts the
   heartbeat fires while a read is outstanding *and nothing is calling back* --
   the property r2 lacked. Reverting to a caller-driven poll fails four of its
   checks.

Unchanged
 - **No logfile line changed in this release**, and no line changed at all at
   `-s 0`. `Duration:` now reports a different *value* for interior tracks at a
   nonzero offset -- a corrected measurement, not a format change. The golden reference differs
   from r2's only in the version string, the timestamps, the wall-clock timing
   fields, and the checksum over all of them.

platterpus-fork r2 (2026-08-03)
===============================
Fork release 2. Shipped carrying upstream's version string `0.9.4-rc1`; see
the r3 entry above for why that changed.

Fixed
 - **Disc-image rips returned silence at any paranoia level above 0.** One
   correct sector followed by 99.7% zeroed samples, reported as
   `Ripping errors: 0`. Upstream sets paranoia's cache model to 1 sector for
   image drivers, and that size doubles as the read chunk size, leaving the
   verification logic no overlap. Raised to 16, clear of both the corruption
   boundary (<=4) and the leadout over-read that costs errors (>=512).
   Affects upstream 0.9.4-rc1 and every earlier build of this fork. Real
   drives were never affected; `-P 0` was always byte-perfect.

Added
 - CD-TEXT is read from the disc, disc level and per track, kept verbatim in
   the log and used to fill metadata only where nothing else claimed it
   (user `-a`/`-t` > MusicBrainz > CD-TEXT > defaults)
 - Per-track paranoia status counters, summing exactly to the disc totals
 - `Encoder:` names the libavformat/libavcodec that wrote the audio
 - `Cache model:` reports the size paranoia models, and says the drive was not
   probed -- deliberately not phrased as a cache defeat, which is not measured
 - `Sample peak level:` and `True peak level:`, each saying which peak
 - `Integrated loudness (R128):` and `Loudness range (R128):`, fork-owned so a
   consumer need not scrape libavfilter's wording
 - Read-liveness reporting from inside paranoia's callback while a frame read
   stalls, so a slow read is distinguishable from a wedged one.
   **Superseded in r3: this did not work.** It could only report reads that were
   still progressing, which are not the ones that matter. See the r3 entry.
 - `-k` / `--stall-secs` sets that threshold (default 10, 0 disables)
 - `-V` accepted again as an alias for `--version`, after upstream's move from
   getopt to genopt dropped it and broke callers probing with it
 - Q sub-channel recovery for drives that return raw binary instead of BCD

Changed
 - `Cache defeat:` renamed to `Cache model:`, and `Peak level:` to
   `Sample peak level:`. Both labels claimed more than their values
   established. Consumers keying on the old names must update.
 - Log and cue files are line-buffered, so a cancelled rip leaves a partial
   record instead of an empty file
 - The log's first line and `Invoked as:` identify the fork and its arguments

platterpus-fork r1 (2026-08-02)
===============================
First tagged fork release, on upstream `0.9.4-rc1` (`958e1ad`), and shipped
carrying that version string.

 - Fork identifies itself as `platterpus-fork` in the version banner and on
   line 1 of every logfile; the version number stays upstream's
 - Pregap provenance reported per track, distinguishing `none` from
   `unknown (reason)`
 - `-Z` convergence verdict reported per track
 - Per-track extraction speed and elapsed time
 - Q sub-channel pregap detection for physical discs, carried from upstream
   PR #115 with three fixes
 - `tools/gen-provider-contract.py` generates the machine-readable interface
   contract consumed by Platterpus

Everything below this line is upstream cyanreg/cyanrip's changelog, unmodified.

0.9.4-rc1
=========
 - New option parser: long options, typed values, saner errors
 - CUE sheet-only mode, without ripping anything (-J)
 - Rip log checksum verification (-Y)
 - Label and catalog number tagging from MusicBrainz
 - MusicBrainz submission URLs are printed as a QR code (needs libqrencode)
 - Embedded cover art is now typed as a front cover, so file managers thumbnail it
 - Automatic deemphasis of preemphasized discs now actually happens
 - CUE sheets reference their files relative to the sheet
 - Naming schemes: stricter parsing, whitespace trimming, filename collision warnings
 - Data tracks no longer leave stray files
 - Fixed a crash on overlong filenames
 - FFmpeg 8.0+ compatibility

0.9.3
=====
 - FFmpeg 7.0 compatibility
 - Added an option to specify the image size for cover lookup
 - When only printing info, do not require an offset to be specified with -I
 - Fix default pregap action setting for tracks after the first
 - Better error handling

0.9.2
=====
 - ReplayGain 2.0 scanning and tagging
 - Preemphasis detection via both TOC and subchannel
 - Automatic deemphasis
 - CUE file writing
 - Repeat ripping mode for affirmation or badly damaged discs
 - Tagging improvements (setting the media_type tag)
 - Logfile reorganization and checksumming
 - Windows compatibility improvements
 - Migration to new FFmpeg 6.0 APIs

0.9.0
=====
 - Improve MusicBrainz query result handling and detect stub releases
 - For unknown discs, add an ID to the album name
 - Better error reporting when opening logfile
 - Fix crash when MCN is missing
 - Silence warning when writing cover art to a file
 - Fix compilation with FFmpeg 6.0

0.8.1
=====
 - __No need to rerip anything.__
 - Fix Musicbrainz album name setting.

0.8.0
=====
 - __No need to rerip anything.__
 - ETA printout
 - Minor bugfixes
 - Big endian fixes
 - Default bitrate for lossy files set to 256kbps
 - Fix minor compilation warnings
 - Fix compilation warnings with FFmpeg 5.0

0.7
===
 - __No need to rerip anything.__
 - Automated CD drive offset finding
 - Verification of partially damaged tracks
 - Tagging usability improvements
 - Even faster ripping
 - Arbitrary directory/file structure
 - Automatic cover art image downloading
 - ...and more

0.6.0
=====
 - __No change in actual audio data ripped, rerip if you want to verify with accurip.__
 - Fill disc count and disc number from musicbrainz.
 - Able to choose the MusicBrainz release to use for albums with multiple releases.
 - Tag improvements (discname is set when available for multi-disc releases).
 - Fix accurip v1 and v2 checksums (were calculated incorrectly). EAC CRC has always been correct.
 - Fix some minor and one large memory leak.

0.5.2
=====
 - __No need to rerip anything.__
 - Fix encoding while ripping from a real drive (broken by 0.5.0).

0.5.1
=====
 - __No need to rerip anything.__
 - Reduce FFmpeg library version requirements

0.5.0
=====
 - __No need to rerip anything.__
 - Rewritten audio muxing
       * Now properly sets the time base in all cases
 - Rewritten encoding code
 - Rewritten FIFO code
       * No longer deadlocks
 - Rewritten build system

Previous versions
=================
No history.
